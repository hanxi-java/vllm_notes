<span style="color:rgb(255,0,0)">报告主体为 AI 生成，需对照论文学习</span>

# 现代多级缓存与 AI Infra 前沿热度算法体系


## 1. 经典准入控制与轻量化缓存机制

传统缓存（如纯 LRU/LFU）侧重于**驱逐策略**。现代缓存（如 W-TinyLFU、S3-FIFO、ARC）则转向**准入策略**与**多级分层**，以应对高并发与“一次性高频”污染。

### 1.1 W-TinyLFU (Window TinyLFU)

* **来源文献**：TinyLFU: A Highly Efficient Cache Admission Policy

* **应用场景**：Java Caffeine、Meta CacheLib。

* **准入博弈 (Admission Battle)**：新数据不再无条件替换老数据。由 1% 容量的 Window Cache（LRU）作为突发流量缓冲；当数据申请进入 Main Cache（SLRU）时，需通过 **TinyLFU 频率预言机** PK。若候选人频次 $F_c > F_v$（受害者频次）则准入，否则直接抛弃候选人。

* **Count-Min Sketch & 全局衰减**：使用 4-bit 槽记录频次（上限 15）。每当累积插入达样本窗口 $W$ 时，全局计数器右移（除以 2），使过气热点频次呈指数衰减，彻底消除“历史包袱”。


### 1.2 S3-FIFO (Simple and Scalable Multi-Tier FIFO)

* **来源文献**：FIFO queues are all you need for cache eviction

* **设计动机**：解决高并发下 LRU/LFU 双向链表频繁移位引发的锁竞争。

* **三队列结构**：
	* **Small FIFO (10%)**：吸收新数据，快速过滤 One-Hit-Wonders。

	* **Main FIFO (90%)**：存放被二次访问的真正热数据。

	* **Ghost FIFO**：幽灵队列，仅存被 Small 淘汰的 Key 元数据。

* **2-Bit 计数器延迟处理**：命中时仅对 2-bit 计数器原子加一，对象在队列中位置不动（全过程 0 锁/0 链表重组）。只有尾部淘汰时才进行计数器扣减或晋升。



### 1.3 ARC (Adaptive Replacement Cache)

* **来源文献**：ARC: A Self-Tuning, Low-Overhead Replacement Cache

* **自适应目标 $p$**：划分为 Recency 链表（T1/B1）与 Frequency 链表（T2/B2）。通过 B1（幽灵命中 $\rightarrow$ 增大 $p$ 保护 Recency）与 B2（幽灵命中 $\rightarrow$ 缩小 $p$ 保护 Frequency）进行反馈调节，实现无参自适应。


## 2. 变长数据与变获取代价算法 (GDSF 及其衍生)

当缓存对象的大小（Size）与获取代价（Cost）非均匀时（如 Web 文件、KV Cache 块），GDSF 算法通过综合评估性价比来统一打分。

### 2.1 GDSF (Greedy-Dual-Size-Frequency) 核心机制

* **来源文献**：Improving WWW Proxies Performance with Greedy-Dual-Size-Frequency Caching Policy

* **热度评估公式**：

$$H(p) = L + \frac{F(p) \times C(p)}{S(p)}$$

系统天然偏好**高频 ($F$)、高获取成本 ($C$) 且体积小 ($S$)** 的对象。

* **加法膨胀老化因子 ($L$)**：$L$ 初始为 0，每次淘汰受害者时更新为 $L = H_{\text{victim}}$。新数据自带较高的 $L$ 加分；未被命中的老数据因分数无法刷新，会被不断膨胀的 $L$ 水面自动“淹没”并淘汰。


### 2.2 GDSF 演进与衍生

| 衍生分支 | 改进机制 | 系统价值 |
| --- | --- | --- |
| **GD-Wheel** | 引入时间轮（Timing Wheel）与离散代价队列，替代 Min-Heap。| 将复杂度由 $O(\log N)$ 降至 **$O(1)$**，适配线速交换机与高吞吐网络。|
| **LNC-R** | 将网络带宽、算力耗时、I/O 动态吞吐率引入 $C(p)$。| 动态响应系统网络拥塞与计算瓶颈。|



## 3. AI 基础设施与大模型服务 (AI Infra & LLM Serving)

在 LLM 推理场景中，KV Cache 的生命周期与重用模式极为特殊。热度评估已从 OS 黑盒发展为深度感知语义、任务与拓扑的 Workload-Aware (WA) 体系。

```
                  ┌── Model-Level (Attention): H2O, SnapKV (注意力权重/Heavy-Hitters)
                  │
WA 算法分层 ───────┼── Access/System-Level: SGLang (RadixTree+LRU/LFU), UniCache (两级 ROI)
                  │
                  ├── Workload-Aware Trace-Based: ATC '25 (KVCache in Wild), TokenStack
                  │
                  └── Global/Agent-Aware: Mooncake (拓扑/SLO), SmartCache/STEP (语义)

```

### 3.1 基于模型注意力机制的 Token 级热度

* **H2O (Heavy-Hitter Oracle)**：基于累积注意力分数（Cumulative Attention Score）保留高频 Heavy-Hitters 与局部窗口 Token。
	* **来源文献：** H2O: Heavy-Hitter Oracle for Efficient Generative Inference of Large Language Models (NeurIPS 2023\)

* **SnapKV**：在 Prefill 阶段聚类评估 Head 注意力特征，提前锁定关键 Token（Pivotal Tokens）常驻显存。
	* **来源文献：** SnapKV: LLM Knows What You are Looking for Before Generation (NeurIPS 2024\)


### 3.2 系统级访问模式与树形前缀缓存

* **SGLang (RadixAttention & 节点级 Eviction)**：
	* 将共享 Prefix KV Cache 组织为 Radix Tree，淘汰操作发生在**叶子节点**，自底向上释放物理内存（不影响父节点共享前缀）。

	* 支持 **LRU**（时间局部性）、**LFU**（将 System Prompt / Few-shot 锁定在显存）及 **Priority**（多租户高低优任务混排）。

    * **来源文献：** SGLang: Efficient Execution of Structured Language Model Programs


* **UniCache (两级动态 ROI 机制)**：
	* 提出会话重用（Session Reuse $\rightarrow$ 适配 LRU）与结构重用（Structural Reuse $\rightarrow$ 适配 LFU）存在强异构性。

	* **两级架构**：局部维护任务专属队列；全局全局协调器根据 **Hit Efficiency（命中数 / 占用容量）** 惩罚低效队列，再由低效队列调用局部策略淘汰 Block。

    * **来源文献：** UniCache: Unifying Prefix Cache Eviction for Heterogeneous LLM Serving Workloads


### 3.3 经验概率与异构硬件分层 (TokenStack)
* **TokenStack**：
	* **Category-Aware Lexicographic Score**：
    $$\text{Score}(b) = \langle -\text{ReuseProb}_{w}(\Delta t_b, \ell_{w}), \, +b.\text{offset} \rangle$$
    优先驱逐预估重用概率低的 Block；若概率相同，优先淘汰 Offset 大（靠近序列尾部、复用率低）的 Block，保留头部 Prefix。

	* **TokenStack (HBM-PIM 冷热流转)**：将同一个 HBM 划分为 **Compute Layers (热层)** 与 **Capacity Layers (冷层)**。借助 Base Die 执行栈内 DMA 传输，迁移过程中顺手完成 **K8V4 硬件级内联量化**。

	* **来源文献：** TokenStack: A Heterogeneous HBM-PIM Architecture and Runtime for Efficient LLM Inference



## 4. 跨领域硬件/OS层热度机制对照

| 领域 / 层级 | 存储介质 | 核心热度指标 (Hotness Metric) | 典型算法 / 机制 |
| --- | --- | --- | --- |
| **GPU / 硬件缓存** | L1 / L2 SRAM | 内存指令 PC 签名 / RRPV 间隔 | **Hawkeye** (模拟 Belady MIN)、**RRIP**、**Cache Bypassing**。|
| **操作系统 / 内存分层** | 本地 DRAM - CXL DRAM | NUMA Hinting Fault 采样频率 | **Linux TPP** (kswapd 冷页降级 + Hinting Fault 热页升级)、**KLOCs**。|
| **数据库 & 系统软件** | DRAM - NVMe SSD | Count-Min Sketch / 2-Bit 计数 / 幽灵探针 | **W-TinyLFU**、**S3-FIFO**、**ARC**。|
| **AI LLM 引擎** | HBM - Host DRAM | 累积注意力权重 / 序列 Offset / 任务 ROI | **H2O**、**SGLang**、**UniCache**、**TokenStack**。|


# 论文详细解读
<span style="color:rgb(30,136,229)">这里 learning-based 的 LeCaR 和离线的 OPT-Offline 就不看了</span>

## SGLang: Efficient Execution of Structured Language Model Programs
SGLang 的缓存淘汰策略与其底层的 RadixAttention 机制深度绑定。SGLang 将所有的 KV Cache 组织为一棵前缀基数树（Radix Tree），因此它的淘汰操作是在树的节点级别进行的。

### 1. 核心机制：基于树结构的叶子节点淘汰
当系统的 GPU 显存或配置的容量池达到水位线（OOM）时，SGLang 不会随意丢弃物理内存块，而是执行一套结构感知的释放流程：
- 锁定叶子节点 (Evictable Leaves)：系统只从当前没有任何活跃请求（Active Requests）引用的叶子节点开始考察。
- 堆排序挑选 (Heapify)：引擎根据选定的配置策略（如 LRU/LFU），为所有合法的叶子节点计算权重，并构建一个小顶堆（Min-Heap）。
- 自底向上释放：弹出优先级最低的叶子节点并释放物理内存。关键优势在于：淘汰一个叶子节点绝对不会影响共享相同前缀的父节点（Parent Nodes）。如果一个父节点的所有子代都被淘汰，且自身也不再被请求锁定，它才会转变为新的叶子节点，并在下一轮内存压力到来时进入候选池。

![BQACAgUAAyEGAASHRsPbAAEX2rlqZBAIbPN9NAABrz3LCz_cpycnpuoAAjIfAAKV3yBX37aBmA8jgEI9BA.png](https://img.remit.ee/api/file/BQACAgUAAyEGAASHRsPbAAEX2rlqZBAIbPN9NAABrz3LCz_cpycnpuoAAjIfAAKV3yBX37aBmA8jgEI9BA.png)

### 2. 核心淘汰策略 (Eviction Strategies)
SGLang 提供多种策略以适配不同的异构工作负载，可通过启动参数 --radix-eviction-policy 进行配置：
- LRU (Least Recently Used) - 默认策略
    - 指标：节点的最后访问时间 (last_access_time)。
    - 逻辑：优先淘汰最久未被访问的节点。适用于时间局部性强的一般对话和单轮推理任务。
- LFU (Least Frequently Used)
    - 指标：节点的命中总数 (hit_count)。
    - 逻辑：优先淘汰历史命中次数最少的节点。这一策略对 API 服务极为有效，可以强制将高频出现的 System Prompt 或 Few-shot 模板“钉”在显存中，即便它们在短时间内未被调用。
- Priority-Based (优先级驱动)
    - 指标：请求显式携带的优先级数值。
    - 逻辑：专为多租户或高低优任务混排设计。节点权重为其 priority（优先级越高，值越小，越不容易被 Min-Heap 弹出）。当不同请求命中相同节点时，节点继承最高优先级；若优先级相同，则退化为 LRU。
- 基础队列型
    - 包含 FIFO（按 insert_time 淘汰最早插入的）、MRU（淘汰最近使用的）等，适用于极简且高度可预测的长下文流式处理。

### 3. 进阶淘汰场景
在更复杂的部署架构中，SGLang 的 Eviction Policy 还有以下演进方向：

- 分层存储 (Hierarchical Cache / HiCache)：当开启 CPU 主机内存作为二级缓存时（--enable-hierarchical-cache），GPU 端的 Eviction 本质上变成了降级（Demotion）：
    - GPU 显存满载：根据 LRU 或 Priority 选出的叶子节点不会被直接销毁，而是被写回（Write-through 或 Evict）到 Host 内存中。

    - Host 内存满载：才会在 CPU 内存池中触发真正的物理删除。此时 Priority 策略尤为关键，可以确保高优先级的长上下文在 Host 中存活更久。

- 混合架构分离淘汰 (Mamba / Hybrid Models)：在支持线性/混合模型（如 Mamba + Attention）的 MambaRadixCache 中，RNN 状态内存与 KV 显存池是分离的：
    - 引擎维护了两套独立的 LRU 链表，分别追踪 Mamba States 和 KV Cache 的时间戳。
    - KV Cache 依然严格遵循“从叶子向根”的淘汰路径。
    - Mamba States 的特殊性在于，由于其隐状态是连续且独立存在的，因此它可以从树的任意节点被安全淘汰，不再局限于叶子节点。

- Workflow-Aware / Agent-Aware (演进中)：在复杂的 Agentic Workloads 中，纯 LRU 极易导致“即将被复用”的并发 Agent 分支缓存被误杀（Cache Thrashing）。SGLang 社区正在引入 agent_hints（如通过 Nvidia Dynamo 传递）：基于调度层构建的执行 DAG 图（Steps-to-execution），系统能预测哪些分支在未来最不可能被使用，从而主动干预 Radix 树的权重，实现感知执行流的提前淘汰与预取。


## UniCache: Unifying Prefix Cache Eviction for Heterogeneous LLM Serving Workloads

当前大模型推理引擎（如 vLLM、SGLang）在处理 Prefix Cache 时有一个核心盲区：用单一的启发式驱逐规则（如全局 LRU）去应对生产环境中高度异构的请求负载，并在任务类别之间应用相同的策略，隐式地假设工作负载是同质的。然而在实际应用中，现代LLM服务的是异构工作负载，这些负载混合了多轮对话流量和多样化的单轮API请求，从而导致根本不同的前缀复用模式。论文系统性地刻画代表性工作负载中的前缀复用模式，揭示了两种主导的复用模式——会话复用和结构复用——它们在不同任务类型之间存在显著差异。论文提出了UniCache，这是一种统一的驱逐策略，能够同时捕捉这两种复用模式，并动态地在任务之间平衡缓存分配。UniCache 的核心思想是放弃“一刀切”的缓存置换，转而采用“局部任务特定队列 + 全局 ROI 动态协调”的两级 Eviction 架构。

### 1. 核心洞察：两种截然不同的重用模式
异构负载主要呈现两种相互冲突的缓存重用模式，并且存在极强的任务局部性（Task Locality）：

- 会话重用 (Session Reuse)：
    - 场景：多轮对话（如 Web Portal 上的交互）。
    - 特征：后续请求不断追加前置上下文，历史轮次的 Prefix 被高概率重用。
    - 适配策略：LRU。时间局部性极强，最近交互的轮次最重要。

- 结构重用 (Structural Reuse)：
    - 场景：单轮 API 调用（如 Tool-use、文档提取、安全围栏）。
    - 特征：带有固定的系统提示词（System Prompt）或指令模板（Template），这些模板在海量相互独立的请求中反复出现。
    - 适配策略：LFU（最不常使用）。频率特征大于时间特征，高频模板即使近期未被访问，也应该常驻 HBM。
    - 纯 LRU 策略在处理“结构重用”时会导致高频 System Prompt 被大量长文本单轮请求冲刷掉（Cache Thrashing）；而单纯的 LFU 在“会话重用”中反应迟钝。

### 2. UniCache 两级 Eviction 策略设计
为了在一个共享 HBM 池中同时服务这两种模式，UniCache 设计了任务隔离与全局统筹的机制。

**机制一：任务级独立队列与局部优先级 (Local Policy)**
UniCache 将 HBM 中的 Prefix Cache 逻辑上划分为多个独立队列（Task-specific Queues），每个队列对应一类特定任务。在队列内部，系统会根据该任务主导的重用模式分配不同的局部优先级评估指标：
- 对于以会话重用为主的任务队列，采用 LRU 权重。
- 对于以结构重用为主的 API 任务队列，采用 LFU 权重。

**机制二：全局调度指标 —— 命中效率 (Global Hit Efficiency)**
当 GPU 显存告急，系统必须执行 Eviction 操作。由于 LRU 的“时间戳”和 LFU 的“访问频次”量纲不同，无法直接进行跨队列的大小比较。为了解决“该淘汰哪个队列的数据”这一问题，UniCache 引入了全局协调指标：**Hit Efficiency（命中效率）**。
- 计算逻辑：该指标量化了队列对整体缓存命中率的贡献与其占用的缓存容量之间的权衡（Hit Efficiency ∝ 缓存命中数 / 占用的缓存容量）。
- 驱逐执行链路：
    1. 当触发 Eviction 时，全局 Arbiter 首先找出当前 Hit Efficiency 最低的低效队列。这相当于惩罚那些“占用了大量 HBM 空间，但无法提供对等 Cache Hit 回报”的队列。
    2. 锁定低效队列后，系统调用该队列的局部策略，由该队列依据内部的 LRU 或 LFU 规则，选出最终要被驱逐的 KV Block 进行释放。

通过这种动态博弈，UniCache 实现了一种自适应的资源倾斜：不断将宝贵的 HBM 资源向“缓存投资回报率”最高的工作负载转移。

### 3. 系统实现与业务收益
性能提升：在集成到真实 vLLM 引擎后，针对混合异构负载，UniCache 相比 vLLM 原生的 LRU 以及此前提出的 WA (Workload-Aware) 隔离策略，命中率提升了 3.86% - 17.32%，而在大并发 Prefill 阶段极为关键的 TTFT 则降低了最高 3.63 倍。

![BQACAgUAAyEGAASHRsPbAAEX2rpqZBBMP2yog1n_3akcpmMQbnPgMAACMx8AApXfIFd-edmtvyFGPz0E.png](https://img.remit.ee/api/file/BQACAgUAAyEGAASHRsPbAAEX2rpqZBBMP2yog1n_3akcpmMQbnPgMAACMx8AApXfIFd-edmtvyFGPz0E.png)

<span style="color:rgb(30,136,229)">这里 learning-based 的 LeCaR 和离线的 OPT-Offline 就不看了</span>


## KVCache Cache in the Wild: Characterizing and Optimizing KVCache Cache at a Large Cloud Provider

这篇发表于 2025 年 USENIX ATC 的论文《KVCache Cache in the Wild: Characterizing and Optimizing KVCache Cache at a Large Cloud Provider》，通过对阿里云真实生产环境的大模型推理 Trace（包含 to-C 聊天和 to-B API 工作负载）进行分析，提出了一种负载感知（Workload-aware, WA）的 KV Cache 驱逐策略。

### 1. 核心动机：现有策略与真实负载的错位
现有的 LLM 推理引擎（如 vLLM）通常默认采用 LRU 或 LFU 来管理 KV Cache，但这与生产环境中的实际重用模式存在冲突。论文的关键洞察：
- 重用分布极度倾斜 (Skewed Reuse)：仅有 10% 的 KV Cache Block 贡献了高达 77% 的重用次数。
- 单轮对话同等甚至更重要：与直觉（多轮对话主导缓存重用）相反，在 to-B 的工作负载中，高达 97% 的 KV Cache 重用实际上发生于单轮请求（Single-turn requests）之间。
- 重用概率可预测：不同请求类型（API vs. Chat）的重用时间和重用概率差异巨大，但如果将其细分到具体的请求类别（类型 + 轮次），其历史重用模式呈现出极强的可预测性。
- 生命周期极短：KV Cache 的生命周期非常短暂，例如在 to-B 的工作负载中 P99 的生命周期仅为 97 秒。由于高频使用的缓存块可能会在短时间内迅速“死亡”，传统的 LFU 策略在这种场景下会严重失效。


### 2. 负载感知驱逐机制 (Workload-aware Eviction Policy)
基于上述特征，论文在 §4.2 节抛弃了单纯依赖“时间”或“频次”的盲目驱逐，转而直接利用经验采样的重用概率分布来计算缓存块的优先级。

优先级元组计算公式
系统在显存不足触发 Eviction 时，会计算每个 KV Block 的驱逐优先级。优先级的计算公式被定义为一个二维元组：

$$\text{Priority} = (\text{ReuseProb}_w(t, \text{Life}), -\text{Offset})$$

该元组采用字典序比较 (Lexicographically Compare)：
- 重用概率评估 $\text{ReuseProb}_w(t, \text{Life})$：系统会根据当前工作负载类型 $w$（例如 to-B API 或 to-C Chat），结合该 Block 的当前存活时间 $t$ 以及预测的生命周期 $\text{Life}$，通过查表（基于历史 Trace 采样的经验概率分布）得出该缓存块在未来被再次使用的概率。概率越低，越容易被优先驱逐。
- 空间局部性补偿 $-\text{Offset}$：当多个 Block 的重用概率相同时，系统引入了 Token 在序列中的 Offset 的负数进行比较。这意味着在同一个长序列中，越靠后（Offset 较大）的 KV 块会被优先淘汰。这一设计是为了尽可能保留靠近头部的 Prefix，因为它们惠及未来请求的概率远大于序列尾部的特定 Token。

### 3. 策略收益与工程启示
论文将该策略集成到 vLLM 引擎中并进行了真实回放评估：
- 性能飞跃：相比于主流的无感知策略（LRU、LFU、FIFO、S3-FIFO），该策略将缓存命中率绝对值提升了 1.5% 到 3.9%（相对基准提升 8.1% - 23.9%）。
- 排队延迟骤降：命中率的提升极大缓解了 Prefill 阶段的计算瓶颈，首字排队延迟 (QTTFT) 减少了 28.3% 到 41.9%。
- 硬件分层重估：该研究还指出，对于 API 主导的 to-B 负载，由于其短暂的生命周期特性，仅需配置约为 GPU HBM 容量 2 倍的小型显存缓存区，即可逼近无限容量下的理想命中率。这表明对于以 API 为主导的 to-B 工作负载，部署昂贵且复杂的 CPU-RDMA-SSD 多级卸载架构可能是不必要的。

<span style="color:rgb(30,136,229)">感觉不太好实现，要有很多实际 trace 才行（不过论文也指路了 trace 在 https://github.com/alibaba-edu/qwenbailian-usagetraces-anon），offset 其实跟 LRU 也有一些近似，越是前缀越容易被用到</span>


## TPP: Transparent Page Placement for CXL-Enabled Tiered Memory

TPP (Transparent Page Placement) 是针对 CXL (Compute Express Link) 异构内存分层架构的轻量级、透明化 OS 级内存管理机制。

在引入 CXL 扩展内存后，服务器呈现出非对称的多级内存拓扑：本地高带宽 DRAM (Fast Tier) 与 远端 CXL 内存 (Slow Tier)。传统的 Linux NUMA Balancing 是为对称的多路 CPU 架构设计的（期望把内存页迁移到执行该页计算的 CPU 所在节点），这在面对无 CPU 的纯内存节点 (CPU-less Memory Node，即 CXL 节点) 时会彻底失效。

TPP 的核心贡献在于，它在内核层面对 kswapd (页面回收守护进程) 和 NUMA Hinting Fault (NUMA 提示缺页) 进行了重构，构建了一套完整的 Promote (热页晋升) 和 Demote (冷页降级) 体系。

### 1. 架构抽象：基于 CPU-less NUMA 节点的层级划分
Linux 内核天然支持 NUMA 架构，TPP 巧妙地利用了这一点，将 CXL 内存设备抽象为一个不包含任何 CPU 核心的独立 NUMA 节点。

- Top-tier Node (Fast Node): 包含 CPU 和本地直连 DRAM，提供最低延迟和最高带宽。
- Lower-tier Node (Slow Node): CPU-less 节点，通过 CXL.mem 协议挂载的扩展内存（如 CXL-DRAM 或 NVDIMM），延迟较高（增加几十到上百纳秒的 PCIe/CXL 互联开销），但容量巨大。

所有新分配的匿名页 (Anonymous Pages) 和文件页 (File Pages) 默认优先在 Fast Node 分配（Allocation Path）。只有当 Fast Node 内存告急时，才会 Fallback 到 CXL Node。

### 2. 换出机制 (Demotion)：基于水位的非阻塞冷页降级
当本地 DRAM 即将耗尽时，系统必须将冷数据换出以腾出空间。传统 Linux 会触发 Swap 将匿名页写入 SSD，导致极高的延迟断崖。

- 水位线触发: TPP 挂载在 Linux 现有的页面回收内核线程 kswapd 上。当 Fast Node 的空闲内存低于特定的低水位线时，kswapd 被唤醒。
- LRU 链表扫描: TPP 扫描本地节点的非活跃 LRU 链表，精准定位那些近期最少被访问的“冷页”。
- 降级迁移 (Demote to CXL): TPP 拦截了原本的 Shrink_page_list 逻辑。它不再将冷匿名页 Swap 到磁盘，而是调用 migrate_pages()，利用 DMA 引擎（如 Intel DSA）将这些页异步批量迁移到 CXL Node。
- 只有当 CXL Node 也满载时，才会真正触发落盘 Swap。

### 3. 换入机制 (Promotion)：基于 NUMA Hinting 的热度采样
如果被降级到 CXL 内存的页面突然变“热”（被高频访问），系统必须将其拉回本地 DRAM。但硬件无法直接告诉 OS 哪些页变热了，TPP 采用了轻量级的软件采样机制。

- PTE 降权与断链 (Unmapping): 内核的 task_work 机制会定期（如每隔几秒）扫描 CXL 节点上的页表项 (PTE)，并故意清除其 PRESENT 位（或修改访问权限），人为制造一种“缺页”假象。
- NUMA Hinting Fault (提示性缺页异常): 当 CPU 再次尝试访问这个位于 CXL 内存的页时，会触发一个 Minor Page Fault。内核捕获该异常，并将其记录为一次 Hinting Fault。
- 解耦的热度评估: 在原生的 NUMA Balancing 中，任何一次 Hinting Fault 都可能触发页面迁移，这会导致极其严重的乒乓效应 (Ping-pong) 和总线拥塞。TPP 引入了独立的热度阈值 (Hotness Threshold)。
- Promotion (晋升迁移): 只有当 CXL 页面在短时间窗口内累积了足够次数的 Hinting Faults，TPP 才将其判定为“热页”，并将其加入一个后台迁移队列，由专用内核线程异步将其 Promote 迁回 Fast Node。

### 4. 在 AI 基础设施与分布式大模型中的意义
TPP 这套机制对大模型推理的内存池化有直接的工程价值：

- KV Cache 的多级卸载 (Offloading): 在 Prefix-Decode 分离架构中，海量的 KV Cache 极易撑爆 HBM 和 Host DRAM。利用 TPP，可以将超长上下文的历史 KV 自动降级到 CXL 内存。当解码阶段再次频繁 Attend 到这些历史 Token 时，TPP 的 Promotion 机制能将其拉回 Host DRAM 甚至 DMA 回 HBM。
- 避免 Swap 死亡螺旋: AI 工作负载的特征是巨大的内存连续读写。如果 Host DRAM 耗尽触发传统的 SSD Swap，整个推理引擎会立刻发生 IO Hang。TPP 将这个降级目标替换为带宽高出几个数量级的 CXL 节点，能将延迟劣化控制在微秒级，而不是毫秒级。
- 大页支持 (HugePages): TPP 完全支持 Transparent Huge Pages (THP, 2MB 大页) 的整体迁移，这对于动辄按 GB 吞吐矩阵的 AI 任务至关重要，它能大幅降低 TLB Miss 率。


## ARC: A Self-Tuning, Low-Overhead Replacement Cache
<span style="color:rgb(30,136,229)">Gemini 评价：存储领域的不朽名作</span>

ARC (自适应替换缓存)算法优雅地解决了 LRU（只看重近期性，易被全表扫描污染）和 LFU（只看重频率，难以适应阶段性热点切换）的缺陷，并且完全不需要人工调参。它被广泛应用于 ZFS 文件系统、PostgreSQL 数据库以及各种底层存储架构中。

ARC 的核心精髓在于它引入了“幽灵缓存（Ghost Cache）”和“动态自适应参数 $p$”。

### 1. 核心架构抽象：四个 LRU 链表

假设系统总物理缓存容量为 $c$。ARC 不仅仅追踪这 $c$ 个物理页，它还利用极少的内存额外追踪了 $c$ 个已被驱逐的页面的元数据（Metadata）。
整个 ARC 将缓存目录划分为两个目录（L1 和 L2），每个目录又分为上半部（物理数据）和下半部（幽灵元数据）：
- L1 目录 (追踪近期性 Recency)：记录只被访问过 1 次的数据。
	- T1 (Top 1)：存放真正在物理缓存中的 L1 数据。
    - B1 (Bottom 1)：幽灵缓存，存放从 T1 被驱逐出去的数据的元数据（不包含实际数据）。
- L2 目录 (追踪频率 Frequency)：记录被访问过 2 次及以上的数据。
	- T2 (Top 2)：存放真正在物理缓存中的 L2 数据。
    - B2 (Bottom 2)：幽灵缓存，存放从 T2 被驱逐出去的数据的元数据。
容量约束：$\vert{}T1\vert{} + \vert{}T2\vert{} \le c$，且所有追踪的目录总和 $\vert{}T1\vert{} + \vert{}T2\vert{} + \vert{}B1\vert{} + \vert{}B2\vert{} \le 2c$。

### 2. 晋升与流入机制 (Promotion & Insertion)
当一个数据页被 CPU 请求时，ARC 会根据它命中的位置执行不同的晋升动作：
- 完全 Miss (新数据第一次进入)：
	- 流入：数据被从后端存储读取到物理缓存，并插入到 T1 链表的 MRU（最近最多使用）端。
- 命中 T1 或 T2 (物理缓存 Hit)：
	- 晋升：该数据已被访问了至少 2 次，展现出了“频率”特征。系统直接将其移动到 T2 链表的 MRU 端。
- 命中 B1 (幽灵缓存 Hit - 近期性胜利)：
	- 说明：这个数据曾经在 T1 中，被驱逐后不久又被访问。这表明当前的物理缓存给“近期性 (T1)”的空间太小了。
    - 晋升：从后端存储将数据读取到物理缓存，并放入 T2 链表的 MRU 端（因为它现在被访问了第 2 次）。
- 命中 B2 (幽灵缓存 Hit - 频率胜利)：
	- 说明：这个数据曾经在 T2 中，被驱逐后又被访问。这表明当前的物理缓存给“高频重用 (T2)”的空间太小了。
    - 晋升：从后端存储读取数据，放入 T2 链表的 MRU 端。

### 3. 驱逐与降级机制 (Eviction & Demotion)
当物理缓存满（$\vert{}T1\vert{} + \vert{}T2\vert{} = c$）且需要装入新数据时，必须执行物理驱逐。
ARC 引入了一个自适应目标参数 $p$（$0 \le p \le c$）。$p$ 代表了系统当前认为 T1 (近期性) 应该占据的物理缓存目标大小。相应地，$c - p$ 就是 T2 (频率) 应该占据的目标大小。
核心驱逐子程序 (REPLACE)：
- 判断条件：系统会比较当前 T1 的实际大小 $|T1|$ 与目标大小 $p$。
- 驱逐 T1 降级到 B1：如果 $|T1| \ge p$，说明当前倾向于 Recency 的数据超过了目标水位。系统选择将 T1 链表的 LRU 端的页面的物理内存释放，将其元数据降级到 B1 的 MRU 端。
- 驱逐 T2 降级到 B2：如果 $|T1| < p$，说明当前 T1 还没达到目标水位，需要压缩 T2 的空间。系统选择将 T2 链表的 LRU 端的物理内存释放，将其元数据降级到 B2 的 MRU 端。
(注：如果由于连续驱逐导致 B1 或 B2 的元数据总和超出了 $c$ 的限制，系统会直接彻底删除 B1 或 B2 的 LRU 端的元数据，不再追踪。)

### 4. 灵魂机制：幽灵缓存驱动的自适应学习 (Self-Tuning)
为什么 ARC 能够适应任何复杂的 Workload？参数 $p$ 的动态演进。它把 B1 和 B2 当作了两个“探针（Probes）”或“后悔药”。
- 如果在 B1 中发生命中 (Hit in B1)：说明系统刚把一个“新数据”驱逐掉，它就立刻被访问了。系统感到“后悔”，意识到当前的工作负载具有很强的突发单次扫描或时间局部性。
	- 操作：扩大参数 $p$（通常 $p = \min(p + \Delta_1, c)$）。使得后续的驱逐动作更倾向于保护 T1（驱逐 T2）。
- 如果在 B2 中发生命中 (Hit in B2)：说明系统刚把一个“老高频数据”驱逐掉，它就立刻被访问了。系统感到“后悔”，意识到当前的工作负载具有很强的稳态高频重用性。
	- 操作：缩小参数 $p$（通常 $p = \max(p - \Delta_2, 0)$）。使得后续的驱逐动作更倾向于保护 T2（驱逐 T1）。

### 总结：与现代缓存算法的呼应
如果你将 ARC 放在多级缓存演进的宏观视角下来看，会发现它深刻影响了后世的设计：
- 分级与升维：ARC 的 T1 和 T2 设计，实质上是对数据“冷热”特征的解耦，这与前文提到的 S3-FIFO（Small, Main, Ghost 队列机制）在精神上高度一致。
- 幽灵目录的价值：用极小的内存（Meta-data）来记录“曾经来过的数据”，是进行精准命中率预测的关键。后来的 W-TinyLFU（Meta / Caffeine 使用）虽然用 Count-Min Sketch 替换了精确的幽灵链表来节省内存，但“参考被驱逐数据的历史”这一理念，正是由 ARC 奠定的。


## FIFO queues are all you need for cache eviction

这篇论文打破了过去几十年“高级缓存必须依赖复杂算法（如 LRU、LFU）”的思维定势，提出了一个极简且极高效的缓存算法：S3-FIFO。

它的核心出发点是：传统的 LRU/LFU 在每一次 Cache Hit 时，都需要通过加锁来移动双向链表中的节点（将节点移到头部）。在现代极高并发的 Web/CDN/KV 存储场景下，这种频繁的锁竞争会导致严重的 CPU 瓶颈。

S3-FIFO 的设计哲学是：用最古老的 FIFO 队列来避免 Hit 时的锁竞争，通过多级队列的流转来过滤 One-Hit-Wonders，从而达到甚至超越 ARC 和 W-TinyLFU 的命中率。

### 1. 多级队列架构 (Multi-Tier Architecture)
系统彻底摒弃了复杂的双向链表，只维护三个最简单的单向 FIFO 队列：

- S 队列 (Small FIFO): 占据 10% 的物理缓存空间。用于吸收所有突发的新数据，快速过滤掉绝大多数只会访问一次的 One-Hit-Wonders。
- M 队列 (Main FIFO): 占据 90% 的物理缓存空间。用于长期存放真正的高频热数据。
- G 队列 (Ghost FIFO): 幽灵队列，不存物理数据，只存曾被 S 队列淘汰对象的元数据（Metadata / Key）。其容量上限等于 M 队列的大小。

### 2. 极轻量级的热度追踪 (2-Bit Counter)
S3-FIFO 不像 LFU 那样精确计算每个对象的访问次数，而是为每个对象分配一个极小的 2-Bit 计数器（取值范围 0 到 3）。

- Cache Hit (命中时): 计数器原子递增，最高到 3（freq = min(freq + 1, 3)）。
- 核心优势: 发生 Cache Hit 时，对象在队列中的物理位置不发生任何移动，无需加锁，也无需重构链表。这种“延迟处理”极大地提升了并发吞吐量。

### 3. 换入与晋升机制 (Insertion & Promotion)
当发生 Cache Miss，需要从后端（如数据库或下层存储）拉取新数据进入缓存时：

- 查验幽灵队列 (Check G): 系统首先检查这个 Key 是否存在于 G 队列中。
- 彻底的新数据 (Miss in G): 如果不在 G 中，说明是纯新数据。系统将其插入 S 队列的头部。
- 死而复生 (Hit in G): 如果在 G 中，说明这个数据曾经来过，被无情淘汰后很快又被访问了。这展现出了强烈的重用特征，系统直接对其进行 Promote，将其插入 M 队列的头部。

### 4. 驱逐与降级机制 (Eviction & Demotion)
当缓存池满，必须腾出空间时，S3-FIFO 只在队尾进行纯粹的 FIFO 弹出操作。

S 队列的驱逐 (过滤冷数据)
当 S 队列满时，系统检查 S 队列尾部（最老）的对象：

- 高频晋升: 如果其热度计数器 freq > 0（在 S 队列期间被再次访问过），则将其从 S 队列物理移出，晋升插入到 M 队列头部，并将其 freq 清零。
- 无情淘汰: 如果其热度计数器 freq == 0（进入后从未被访问，属于典型的一次性流量），则直接释放其物理内存。同时，将其元数据（Key）降级压入 G 队列头部，等待未来可能的“复活”。

M 队列的驱逐 (老年代淘汰)
当 M 队列满时，系统检查 M 队列尾部（最老）的对象。这里采用了类似 CLOCK 算法的“重新插入 (FIFO-Reinsertion)”机制：

- 扣减热度，再给机会: 如果 freq > 0，系统将其热度减 1（freq = freq - 1），并将其重新插入 M 队列的头部，免于物理淘汰。
- 物理淘汰: 如果 freq == 0，说明它在漫长的流转周期内已经彻底失宠，系统将其彻底物理删除（且不再进入 G 队列）。

### 总结
S3-FIFO 的精妙之处在于它在“时间局部性”和“频率局部性”之间找到了一个极低成本的平衡点。

- S 队列保护了 M 队列不被大批量的冷数据冲刷（Cache Thrashing）。
- M 队列配合 2-Bit 热度扣减，让高频数据能够在一轮轮的 FIFO 循环中存活下来。
- 全过程 0 链表重组开销。


## TinyLFU: A Highly Efficient Cache Admission Policy（W-TinyLFU）
W-TinyLFU（Window TinyLFU） 算法目前被 Java 领域最高效的缓存库 Caffeine 以及 Meta 的 C++ 缓存引擎 CacheLib 所采用。
传统缓存（如 LRU）主要关注驱逐策略 (Eviction Policy)，即“当空间满时，我该把谁赶走”。而 W-TinyLFU 将重心放在了准入策略 (Admission Policy) 上，即“当一个新数据想进来时，它是否有资格挤掉老数据”。

### 1. 核心统计器：TinyLFU (Frequency Oracle)
为了实现准入控制，系统必须知道任何一个对象的历史访问频次。但为所有访问过的对象维护精确计数的内存开销极大。TinyLFU 设计了一个极低开销的“频率预言机”。
- Count-Min Sketch (近似计数)：TinyLFU 使用了一种概率数据结构来记录频率。它将对象的 Hash 值映射到多个计数器槽中。
- 极致的内存压缩：由于缓存中的访问频次往往是高度 skewed 的，TinyLFU 为每个槽只分配 4-bit 的计数器，最大计数值为 15。研究表明，知道一个对象被访问了 15 次还是 100 次，在缓存驱逐决策中并没有区别，只要知道它“很热”就足够了。这使得每个缓存项的统计元数据开销仅需不到 1 Byte。

### 2. 动态衰减机制 (Freshness & Aging)
传统 LFU 最大的痛点是“历史包袱”：一个过去极热但现在已经不再访问的过气数据，会因为积累了巨大的历史计数而永远霸占缓存。
TinyLFU 通过一种极其轻量的全局衰减机制解决了这个问题：
- 系统维护一个全局计数器，记录总的插入操作次数。
- 当总插入次数达到一个样本窗口大小 $W$（通常等于缓存总容量）时，系统遍历 Count-Min Sketch 中的所有 4-bit 计数器，执行全局右移操作（即全部除以 2）。
- 效果：这使得 TinyLFU 记录的不再是绝对的历史总频次，而是近期滑动窗口内的相对频次。过气数据的频率会随着时间呈指数级衰减，最终被新晋的热点数据超越。

### 3. W-TinyLFU 的分层架构 (Window + Main Cache)
TinyLFU 统计器的缺陷：突发稀疏流量无法建立频次。如果一个全新对象刚被读取（频次为 1），它在尝试进入缓存时，面对的对手都是频次远大于 1 的老数据，这会导致新数据永远无法进入缓存，直接被丢弃。为了解决“新人冷启动”问题，论文提出了 W-TinyLFU 架构，将物理缓存空间硬性划分为两部分：
- Window Cache (窗口缓存)：通常只占总容量的 1%。
	- 由纯粹的 LRU 管理。
	- 任何新访问的数据（Cache Miss 后从底层拉取的数据）都会无条件先进入这里。
	- 作用：充当突发流量的“避风港”，让新数据有一定的时间来积累被二次访问的次数（建立 TinyLFU 计数）。
- Main Cache (主缓存)：占据剩余的 99% 容量。
	- 通常由 SLRU (Segmented LRU) 组织，分为 Probation（考察期）和 Protected（保护期）两段。用于存放真正经过频次考验的高热度数据。

### 4. 准入博弈机制 (The Admission Battle)
W-TinyLFU 中，当 Window Cache 满了之后，触发驱逐，但数据不会被直接物理销毁，而是会触发一次准入拦截：
- 确定候选人 (Candidate)：Window Cache 的 LRU 尾部淘汰出一个数据，它希望晋升进入 Main Cache。
- 确定受害者 (Victim)：Main Cache（具体为 SLRU 的 Probation 段）也已经满了，它的 LRU 尾部也选出了一个准备物理淘汰的数据。
- 频率 PK (Frequency Comparison)：
	- 系统查询 TinyLFU 统计器，获取 Candidate 的频次 $F_c$ 和 Victim 的频次 $F_v$。
    - 如果 $F_c > F_v$：候选人胜出。受害者被物理驱逐出内存，候选人成功进入 Main Cache。
    - 如果 $F_c \le F_v$：受害者胜出（老数据依然更热）。候选人被直接物理驱逐，受害者免于淘汰，被重新放回 Main Cache 的头部。

W-TinyLFU 通过 Window 区域保护了新数据的成长，通过 TinyLFU Sketch 实现了极低开销的历史频率追踪，又通过全局衰减和准入博弈，在突发流量（Burst）和长尾长周期高频流量（Heavy-hitters）之间实现了近乎完美的平衡。


## TokenStack: A Heterogeneous HBM-PIM Architecture and Runtime for Efficient LLM Inference

基于 HBM4 逻辑底座（Logic-die substrate）的垂直异构 HBM-PIM（近存计算）架构。其核心理念是：并非所有 KV Cache 都值得使用昂贵的 PIM 逻辑单元，只有“热数据”才需要。围绕这一架构，TokenStack 设计了一套紧密结合硬件底层的 负载感知驱逐与流转机制 (Workload-aware Eviction)。

### 1. 硬件级异构分层 (Storage Tiering)
TokenStack 摒弃了将整个 HBM 做成统一 PIM 的做法，而是将同一个 HBM 堆叠（Stack）在物理和逻辑上划分为两层：
- Compute Layers (计算层 / 热层)：带有 PIM 处理单元的 HBM 层，用于存放高热度 KV Cache（Working Set）。数据在这里可以直接与 Attention 引擎交互，实现极高的近存计算带宽。
- Capacity Layers (容量层 / 冷层)：传统的高密度 HBM 层，不含计算逻辑。用于存放模型权重（Weights）、激活值（Activations）以及被判定为冷数据的 KV Cache。

### 2. 换出/降级机制 (Eviction & Demotion)
当 Compute Layers 的空间占用达到高水位线时，系统必须将部分 KV 块驱逐（降级）到 Capacity Layers。
- Stack-internal DMA (栈内直连传输)：TokenStack 利用 HBM4 的逻辑基础芯片（Logic Base Die）作为局部控制器。冷热层之间的数据迁移完全在 HBM 内部完成，不需要经过 GPU 主机内存总线，从而避免了传统 Offloading（卸载到 CPU DRAM）造成的总线带宽争抢。
- Inline Quantization (内联量化压缩)：在 DMA 将数据从热层搬运到冷层时，底座控制器会“顺手”执行硬件级的 K8V4 压缩（将 Key 和 Value 压缩为 8-bit 和 4-bit）。这使得被驱逐的冷数据在容量层中占据的空间进一步缩小。

### 3. 热度评估算法 (Category-Aware Hotness Score)
为了决定哪些 KV 块应该被驱逐到冷层，TokenStack 引入了**负载感知热度评估算法**。系统会追踪每个 KV Block 的元数据（包括请求类别、重用近期性、以及它在 Prompt 中的位置），并计算短期内的重用概率（Short-horizon reuse probability）。

它的热度打分机制采用字典序排序 (Lexicographic Score)，其核心公式如下：
$$\text{Score}(b) = \langle -\text{ReuseProb}_{w}(\Delta t_b, \ell_{w}), +b.\text{offset} \rangle$$
- 主干比较 (Reuse Probability)：首先比较该模块属于特定类别 $w$ 工作负载时的预估重用概率。该概率是基于距上次访问的时间间隔 $\Delta t_b$ 和序列长度 $\ell_w$ 动态计算的。前面加上负号意味着：重用概率越低，Score 越小，越容易被优先驱逐。
- 决胜比较 (Prompt Offset)：如果两个 Block 的重用概率相同，则比较它们在输入序列中的偏移量（$+b.\text{offset}$）。Offset 越大（即越靠近序列尾部、越晚生成的 Token），其 Score 越小，越优先被淘汰。这与前缀缓存（Prefix Caching）的理念一致：越靠近头部的 Token，被不同请求共享命中的概率越高，越应该留在热层。

<span style="color:rgb(30,136,229)">这篇与我们相关的只有热度评估算法部分，这个热度打分机制跟 KVCache Cache in the Wild: Characterizing and Optimizing KVCache Cache at a Large Cloud Provider 是基本一样的</span>

### 4. 补充机制：有界复制 (Bounded Replication)
除了上下层的垂直流转，TokenStack 还针对分布式 Attention 设计了水平向的预取和复制。系统只在“预估的因缓存命中省下的延迟收益，大于跨 Bank/跨层数据复制带来的硬件开销”时，才会为热数据创建副本。这种机制配合架构感知的调度，能最大化 Compute Layers 内的局部 Attention 命中率。


## Improving WWW Proxies Performance with Greedy-Dual-Size-Frequency Caching Policy

1998 年，GDSF (Greedy-Dual-Size-Frequency) 缓存替换算法。

与针对固定大小内存块（如 OS 内存页、KV Cache Block）的算法不同，GDSF 专门为了解决 Web 代理缓存中对象大小不一和获取成本不同的复杂场景而设计。

### 1. 核心挑战：非均匀对象缓存
在 Web 网络场景中，文件（如 HTML、图片、视频）的大小差异巨大。如果沿用纯 LRU 或 LFU，系统可能会为了保留一个 10MB 的极低频视频，而驱逐 1000 个 10KB 的高频网页，导致整体缓存命中率（Hit Rate）雪崩。
GDSF 的设计目标是通过一个综合的数学公式，将近期性 (Recency)、频率 (Frequency)、大小 (Size) 和 获取成本 (Cost) 统一降维成一个单一的“优先级/热度分”。

### 2. 热度评估算法：动态老化代价公式
系统中维护一个优先级队列，每个缓存对象 $p$ 的热度值（Key Value）$H(p)$ 计算公式如下：
$$H(p) = L + \frac{F(p) \times C(p)}{S(p)}$$
- $F(p)$ (Frequency)：对象 $p$ 被访问的历史频率。
- $C(p)$ (Cost)：将对象 $p$ 从源站拉取到缓存的成本（可以设定为常数 1，或者真实的下载延迟时间）。
- $S(p)$ (Size)：对象 $p$ 的文件大小。
- $L$ (Aging Factor 动态老化因子)：这是整个算法的灵魂。初始值为 0，随着系统的驱逐动作不断单调递增。

**热度逻辑**：系统天然偏好高频、高获取成本且体积小的对象（即缓存性价比高的数据）。体积越大的文件，分母越大，热度基础分越低，越容易被淘汰。

### 3. 流转与驱逐机制 (Eviction & Promotion)
GDSF 不像现代缓存体系那样维持多级物理队列，而是通过巧妙更新全局变量 $L$ 来实现自适应的缓存更替：
- 换入 (Insertion / Cache Miss)：当一个新对象 $p$ 被拉入缓存时，其初始频率设为 $F(p) = 1$。
- 晋升 (Promotion / Cache Hit)：当对象 $p$ 再次被命中时，其频率 $F(p)$ 加 1。系统使用当前的全局老化因子 $L$ 重新计算它的热度值 $H(p)$。由于 $L$ 随时间不断增长，再次命中会显著抬升该对象的优先级，使其远离淘汰边缘。
- 驱逐 (Eviction)：当缓存空间不足以容纳新对象时，系统挑选当前优先队列中 $H(p)$ 最小的对象作为“受害者”进行物理驱逐。核心更新动作：在驱逐完成后，系统将全局老化因子 $L$ 更新为该受害者的热度值（即 $L = H_{min}$）。

### 4. 跨时空的算法对比与现代启发
1998 年的 GDSF 与 2015 年的 W-TinyLFU），解决了同一个痛点——“历史包袱污染（Cache Pollution）”。在纯粹的 LFU 算法中，一个过去极热的数据即使现在无人问津，也会因为其积攒的极高频次永远霸占缓存。
- W-TinyLFU 的解法（乘法衰减）：定期将所有缓存对象的频次除以 2（全局右移），主动压低老数据的分数。
- GDSF 的解法（加法膨胀）：GDSF 不主动修改老数据的分数，而是通过在驱逐时不断抬高基础门槛 $L$，让新插入的数据自带一个巨大的“时代加分”。老数据如果不再被访问（意味着无法用新的 $L$ 刷新自己的分数），它们原本看起来很高的得分，很快就会被水涨船高的 $L$ 淹没，从而自动跌入队列尾部被自然淘汰。

## GDSF 算法有哪些衍生
衍生算法主要为了解决 GDSF 的两个核心痛点：优先队列的计算复杂度（$O(\log N)$ 瓶颈），以及静态频率/代价公式无法适应更复杂的现代网络和计算系统。

### 1. 算法复杂度优化分支：GD-Wheel
GDSF 每次插入、命中或驱逐对象时，都需要维护一个全局的优先级队列（通常是 Min-Heap），其时间复杂度为 $O(\log N)$。在极高吞吐量的数据中心网络中，这种开销是致命的。

- GD-Wheel (GreedyDual-Wheel)：
	- 核心改进：彻底抛弃了 $O(\log N)$ 的堆结构，借鉴了操作系统中的“时间轮 (Timing Wheel)”和 S3-FIFO 的分级思想。
    - 机制：将连续的优先级空间划分为多个离散的代价区间，每个区间对应一个 FIFO 队列。对象根据其 $H(p)$ 映射到特定的队列中。当系统老化因子 $L$ 推进时，直接通过拨动“时间轮”的指针来批量降级或淘汰对象。
    - 系统价值：将缓存管理的复杂度从 $O(\log N)$ 降维打击到了 $O(1)$。这使得 GDSF 级别的智能淘汰算法能够被直接下沉到高吞吐的网络拓扑中，甚至能够配合硬件级拥塞控制协议（如 RoCEv2、DCQCN）在交换机队列调度中实现线速（Line-rate）的包/流缓存管理。

### 2. 代价因子 (Cost) 演进分支
原始 GDSF 的获取代价 $C(p)$ 通常指代网络延迟或跳数。随着存储层次的丰富，代价因子被赋予了更多维度的物理意义。
- LNC-R (LRU-K with Non-uniform Cost)：
	- 核心改进：将 GDSF 的单一代价扩展为“带宽代价”和“计算代价”的综合体。
    - 机制：在评估对象优先级时，不仅考虑该对象占用多少字节 $S(p)$，还引入了系统的实时网络带宽和 I/O 吞吐率。如果当前网络极度拥塞，从远端拉取数据的动态代价 $C(p)$ 会急剧飙升，算法会倾向于锁定本地已有的大文件。
- GDS-HitRate & GDS-ByteHitRate：
	- 核心改进：通过调整公式权重，允许系统在“最大化命中次数（Hit Rate）”和“最大化节省字节数（Byte Hit Rate）”之间平滑切换，以适应不同的 SLA 要求。



