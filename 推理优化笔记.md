# 推理优化笔记：TP/EP 通信原语与 AllReduce 优化

## 目录

- [壹：AllReduce ReduceScatter和AllGather](#壹：AllReduce ReduceScatter和AllGather)
- [贰：SFA Lightning Indexer稀疏注意力及其TP不亲和问题](#贰：SFA Lightning Indexer稀疏注意力及其TP不亲和问题)
- [叄：flashcomm1与-sfa-的并行改造](#叄：flashcomm1与-sfa-的并行改造)
- [肆：冗余权重消除zero-3-式分片--预取让-cp16-显存与-tp16-相当](#肆：冗余权重消除zero-3-式分片--预取让-cp16-显存与-tp16-相当)
- [伍：deepseek-v3-mtp-模块图解草稿模型推理流程投机解码](#伍：deepseek-v3-mtp-模块图解草稿模型推理流程投机解码)
- [陆：IndexCache跨层复用top-k索引加速稀疏注意力](#陆：IndexCache跨层复用top-k索引加速稀疏注意力)
- [柒：图模式静态图编译减少decode调度与kernel启动开销](#柒：图模式静态图编译减少decode调度与kernel启动开销)
- [捌：FUSED_MC2 MoE通信-计算融合](#捌：FUSED_MC2 MoE通信-计算融合)
- [玖：BalanceScheduling DP负载均衡调度](#玖：BalanceScheduling DP负载均衡调度)
- [拾：AsyncScheduling 异步调度与 num_output_placeholders 占位机制](#拾：AsyncScheduling 异步调度与 num_output_placeholders 占位机制)

---
## 壹：AllReduce ReduceScatter和AllGather
### 一、为什么 TP/EP 每层都要 AllReduce 整合各卡结果？传统方案的两个瓶颈

**原问题：**

> 解释一下下面的话：TP / EP 并行的每一层都以 AllReduce 整合各卡结果。传统 AllReduce 方案在大并发、长序列下存在两个瓶颈：
>
> 通信量大：每次前向都传输完整激活值。而 AllReduce 在通信原理上等价于 ReduceScatter + AllGather，直接调用 AllReduce 相当于把两者绑定，丧失了"在二者之间插入降维 / 量化以缩减通信对象"的灵活性。
>
> 冗余计算：AllReduce 完成后整个通信域各卡持有相同数据，其后的 RMSNorm、Dynamic Quant、MLA 的 QKV 降维等算子在各卡上重复计算，浪费算力与时延。

**解答：**

这段话讲的是：在 Tensor Parallel（TP）/ Expert Parallel（EP）并行推理中，传统通信方式"每层一次 AllReduce"的做法为什么低效。

#### 背景

TP/EP 并行时，每张 GPU 只计算矩阵乘法（或专家）的一部分，得到**部分结果**。要让结果完整，必须把各卡的部分和加起来 —— 传统做法是在每层的 Attention/MLP 输出后调用一次 **AllReduce**（规约 + 广播，让每张卡都拿到完整的求和结果）。

#### 瓶颈一：通信量大

**AllReduce 在数学上等价于两步：**

1. **ReduceScatter**：各卡交换数据求和，最后每张卡只保留**求和结果的 1/N 分片**（N = 卡数）；
2. **AllGather**：各卡把自己那 1/N 分片广播给其他卡，拼回完整数据。

```
AllReduce = ReduceScatter + AllGather
```

两步各自都要传完整数据量的激活值，所以通信总量 ≈ 2 × 激活值大小。当 batch 大（大并发）、token 多（长序列）时，激活值 = batch × seq_len × hidden_dim，通信量爆炸，成为延迟瓶颈。

**关键问题在"绑定"**：如果你直接调一个 `AllReduce` 原语，这两步是焊死在一起的，中间什么都插不进去。但如果**显式拆成 ReduceScatter + AllGather**，你就能在中间那个"每卡只有 1/N 数据"的时刻做文章：

- **降维**：先对分片做线性投影（比如 MLA 的 KV 降维到低秩隐空间），再 AllGather，传输的对象就变小了；
- **量化**：先把分片从 FP16 量化成 FP8/INT8，再 AllGather，通信量直接减半。

这就是"在二者之间插入降维/量化以缩减通信对象"的含义。

#### 瓶颈二：冗余计算

AllReduce 结束后，**每张卡手里是同一份完整的激活值**。而接下来要做的 RMSNorm、动态量化（Dynamic Quant）、MLA 的 QKV 降维（W_DKV/W_DQ 投影）等算子，特点是**逐元素或逐 token、与并行无关** —— 在每卡上算出来的结果完全一样。

也就是说，N 张卡对同样的数据重复算了 N 遍同样的 RMSNorm/Quant/降维。这不是必要的并行计算，是纯浪费：

- 浪费算力（多卡做一份活）；
- 这些算子虽然小，但在算力没被打满时是**纯串行延迟**，长序列下层层累积很可观。

#### 对应的优化思路

把 AllReduce 拆开重排：

```
传统:  GEMM(分片) → AllReduce(传完整数据) → RMSNorm → Quant → QKV降维   ← 每卡重复算
优化:  GEMM(分片) → ReduceScatter → 每卡只对自己的 1/N 分片做 RMSNorm/Quant/降维 → AllGather(传缩小后的数据)
```

这样一举两得：

1. **计算不重复**：RMSNorm、Quant、降维各卡只算自己的 1/N 分片，总计算量除以 N；
2. **通信对象变小**：AllGather 传的是量化后、降维后的数据，而不是原始 FP16 完整激活。

这类优化在 vLLM/SGLang 等框架中常见，例如 DeepSeek-V3/R1 的 MLA 推理里把 AllReduce 拆开、在 ReduceScatter 和 AllGather 之间塞入量化或低秩投影，通常被称为 **"sequence parallelism 化"** 或 **communication-computation fusion** 的优化。

---

### 二、为什么 TP 和 EP 需要 AllReduce？原理是什么？

**原问题：**

> 先仔细讲一下为什么TP和EP会需要AllReduce，这个原理是什么？

**解答：**

核心一句话：**每张卡只算出了结果的一部分"和"，必须把各卡的部分和加起来，数学上才是正确的结果。而"各卡求和 + 人人拿到结果"这个操作，就是 AllReduce。**

#### 2.1 TP（张量并行）为什么需要 AllReduce

##### 出发点：单层矩阵乘太大，一张卡放不下/算不动

Transformer 的核心计算是矩阵乘：`Y = X · W`

- X 是激活值，形状 `[tokens, hidden_dim]`
- W 是权重，形状 `[hidden_dim, ffn_dim]`（FFN 中间维度常常是 hidden 的 4 倍，大模型里 W 有上亿参数）

TP 的思路是：**把 W 切开，N 张卡各存 1/N**。问题是——X 不变，Y 必须和单卡算出来一模一样，怎么切才对？

##### 两种切法：列切 和 行切

**① 列并行（Column Parallel）—— 按输出维度切，不需要通信**

把 W 按列切成 `[W₁, W₂]`，各卡算 `Yᵢ = X · Wᵢ`：

```
X · [W₁ | W₂] = [X·W₁ | X·W₂]
     卡0算的 ↑    ↑ 卡1算的
```

每卡得到 Y 的**不同列**，互不重叠，结果本身就是对的（只是各拿一半）。如果要完整 Y，用 AllGather 拼起来即可；如果下一层接行并行，甚至不用拼。

**② 行并行（Row Parallel）—— 按输入维度切，结果天然是"部分和"**

把 W 按行切，同时 X 也按列对应切开：

```
Y = X · W = [X₁ | X₂] · [W₁]  =  X₁·W₁  +  X₂·W₂
                          [W₂]     ↑卡0      ↑卡1
                                   部分和    部分和
```

关键来了：**矩阵乘法的定义就是"对应元素相乘再求和"**。按输入维度切开后，每张卡算出的 `Xᵢ·Wᵢ` 只是这个求和里的**一项**。数学上：

$$Y = \sum_{i=1}^{N} X_i W_i$$

只有把所有卡的结果**逐元素相加**，才等于单卡算的 Y。这就是"规约（Reduce，求和）"的来源。

##### 为什么 Megatron 式 TP 每层必然触发一次求和

实际 TP 把列切和行切**配对使用**，这样第一层不用通信：

```
MLP:      X → [列并行 Linear] → GeLU → [行并行 Linear] → ⚠️必须求和 → Y
Attention: X → [列并行 QKV]   → Attn → [行并行 O投影] → ⚠️必须求和 → Y
```

每个 Transformer 层有 Attention 输出投影和 MLP 下行投影**两个行并行矩阵**，所以每层有 **2 次求和**点。

##### 为什么求和用 AllReduce 而不是 Reduce

- **Reduce**：求和后只有**一张卡**（根卡）拿到结果，其他卡没有。
- **AllReduce**：求和后**每张卡**都有完整结果。

TP 里每张卡都要继续算下一层（下一层的列并行需要完整的 X 作为输入），所以选 AllReduce。它内部就是 `ReduceScatter（求和并各拿1/N分片）+ AllGather（拼回完整结果）`。

**一句话总结 TP：按输入维度切矩阵 → 每卡只算出部分和 → 必须跨卡相加 → 且每卡都要拿结果继续算 → AllReduce。**

#### 2.2 EP（专家并行）为什么需要类似的通信

##### MoE 的结构

MoE 层把一个大 FFN 换成 **E 个小 FFN（专家）+ 一个路由器（Router）**：

```
每个 token → Router 打分 → 选 Top-K 个专家 → 该 token 只被这 K 个专家处理 → 加权求和输出
```

比如 DeepSeek-V3：256 个专家，每个 token 只激活 8 个。

##### EP 的切法：按"专家"切，不按矩阵维度切

EP 把 E 个专家分到 N 张卡上，每卡只存 E/N 个专家的权重。问题来了：**token 和专家不在同一卡上**。

- 卡 0 上的某个 token，可能被路由到卡 3、卡 5 上的专家；
- 所以必须先把 token **发送**到对应专家所在的卡，算完再**送回来**。

这就是 MoE/EP 著名的 **All-to-All 通信**（严格说 EP 的主通信是 All-to-All 而不是 AllReduce）：

```
Dispatch (All-to-All):  各卡把 token 按路由发往专家所在卡
        ↓
各卡本地算自己拥有的专家 FFN
        ↓
Combine (All-to-All):   把专家输出送回原 token 所在卡
        ↓
对 Top-K 个专家的输出做加权求和  ← 这步是 Reduce 语义
```

##### 为什么说 EP 也需要"AllReduce 式"的整合

两点：

1. **Combine 阶段的本质是"跨卡求和"**：一个 token 被 K 个专家（可能在 K 张不同卡上）处理，最终输出是这 K 份结果的加权和——和 TP 的部分和求和是同一种数学需求，只是通信模式表现为 All-to-All（token 按路由搬运）+ 本地加权加，而不是规整的 AllReduce。

2. **混合并行时的交叉点**：实际部署常是 TP × EP 混合（例如 TP=8 内再做 EP，或 Attention 用 TP、MoE 用 EP）。MoE 层输出后要回到 TP 的数据布局、共享专家（shared expert）走 TP 路径、以及 DP 域内同步等场景，都会再次用到 AllReduce/ReduceScatter+AllGather 原语。混合并行框架（如 vLLM、Megatron、SGLang）里，**把通信域的整合统一用 AllReduce 类原语实现**。

**一句话总结 EP：token 要找专家、专家在别的卡上 → 先 All-to-All 搬运 → 各专家算完 → 输出必须跨卡合并回每个 token（求和语义）→ 同样需要一次全局整合通信。**

#### 2.3 把两者统一起来看

| | 切分对象 | 每卡算出的是什么 | 为什么必须通信 | 通信原语 |
|---|---|---|---|---|
| **TP** | 矩阵的维度 | 部分和（行并行）或部分列（列并行） | 数学上 Y = Σ 部分和，且下层需要完整 X | AllReduce（= RS + AG） |
| **EP** | 专家（整个 FFN） | 各自专家对部分 token 的输出 | token 与专家异地，且同一 token 的 K 份输出要加权合并 | All-to-All + 求和（混合并行中也用 AllReduce） |

**共同根源**：并行把"一次完整计算"拆到了多卡上，但**数学上正确的结果要求把分散在多卡上的贡献重新聚合**。聚合 = 求和 + 广播，这正是 AllReduce 的定义。所以只要做 TP/EP，通信就不是可选项，而是矩阵乘法分配律（`Σ XᵢWᵢ`）和 MoE 路由语义强制的代价 —— 这也是为什么"优化 AllReduce"是大模型推理的核心问题。

---

### 三、ReduceScatter 详解：各卡交换数据求和，每卡只保留 1/N 分片

**原问题：**

> 请详细解释一下这个过程：ReduceScatter：各卡交换数据求和，最后每张卡只保留求和结果的 1/N 分片（N = 卡数）

**解答：**

#### 3.1 场景设定

假设有 **N = 4 张 GPU**（记作卡 0~3），通过 TP 行并行各自算出了一个**部分和向量**，每个向量有 **8 个元素**：

```
卡0 的部分和: [a₀ a₁ a₂ a₃ a₄ a₅ a₆ a₇]
卡1 的部分和: [b₀ b₁ b₂ b₃ b₄ b₅ b₆ b₇]
卡2 的部分和: [c₀ c₁ c₂ c₃ c₄ c₅ c₆ c₇]
卡3 的部分和: [d₀ d₁ d₂ d₃ d₄ d₅ d₆ d₇]
```

数学上正确的结果是 4 个向量**逐元素相加**：

```
Y = [a₀+b₀+c₀+d₀,  a₁+b₁+c₁+d₁,  ...,  a₇+b₇+c₇+d₇]
```

记每个求和后的元素为 `Sᵢ = aᵢ+bᵢ+cᵢ+dᵢ`。

**ReduceScatter 的目标**：求和完成后，不追求每卡拿到完整的 8 元素 Y，而是**每卡只拿 Y 的 1/4（2 个元素）**：

```
卡0 → [S₀ S₁]      （第 0 分片）
卡1 → [S₂ S₃]      （第 1 分片）
卡2 → [S₄ S₅]      （第 2 分片）
卡3 → [S₆ S₇]      （第 3 分片）
```

注意两个动作合一了：**Reduce（跨卡求和）+ Scatter（结果按分片散给各卡）**。这就是名字的由来。

#### 3.2 Ring（环形）算法

工程上最常用的实现是**环形 ReduceScatter**。思想：4 张卡排成一个环 `0→1→2→3→0`，数据沿环流动，边流动边累加，共走 **N-1 = 3 步**。

##### 准备：每卡先把自己的向量也切成 4 份

```
卡0: [A0 | A1 | A2 | A3]     每份 2 个元素
卡1: [B0 | B1 | B2 | B3]
卡2: [C0 | C1 | C2 | C3]
卡3: [D0 | D1 | D2 | D3]
```

分片 `i` 的最终归属是卡 `i`。每张卡负责"护送"别人的分片路过自己时顺手累加。

##### Step 1：每卡把"目标分片是下下站的"那块发给右邻居

```
卡0 发 A1 → 卡1      卡1:  A1 + B1        （累加进自己的对应块）
卡1 发 B2 → 卡2      卡2:  B2 + C2
卡2 发 C3 → 卡3      卡3:  C3 + D3
卡3 发 D0 → 卡0      卡0:  D0 + A0
```

##### Step 2：把刚累加完的那块继续传给右邻居

```
卡1 发 (A1+B1) → 卡2       卡2:  A1+B1+C1
卡2 发 (B2+C2) → 卡3       卡3:  B2+C2+D2
卡3 发 (C3+D3) → 卡0       卡0:  C3+D3+A3
卡0 发 (D0+A0) → 卡1       卡1:  D0+A0+B0
```

##### Step 3：再走一步

```
卡2 发 (A1+B1+C1) → 卡3    卡3:  A1+B1+C1+D1 = 完整分片1 ✔
卡3 发 (B2+C2+D2) → 卡0    卡0:  B2+C2+D2+A2 = 完整分片2 ✔
卡0 发 (C3+D3+A3) → 卡1    卡1:  C3+D3+A3+B3 = 完整分片3 ✔
卡1 发 (D0+A0+B0) → 卡2    卡2:  D0+A0+B0+C0 = 完整分片0 ✔
```

##### 结束状态

```
卡0: 分片2 完整  → [S₄ S₅]  ……（例子中分片归属与起步约定有关，可轮换）
卡1: 分片3 完整  → [S₆ S₇]
卡2: 分片0 完整  → [S₀ S₁]
卡3: 分片1 完整  → [S₂ S₃]
```

（具体哪个卡拿到哪片取决于环的起点约定，NCCL 里固定映射；重点是**每卡恰好拿到一个求和完毕的分片**。）

每个分片在环上"环游"了 3 张卡，每到一站累加一次，集齐 4 张卡的贡献后正好抵达它的归属卡。

#### 3.3 通信量分析（这是它比 AllReduce 省的关键）

设完整数据大小为 `M`（这里 M = 8 个元素），卡数 N = 4。

| 操作 | 每卡发送量 | 结果 |
|---|---|---|
| **ReduceScatter** | `(N-1)/N · M ≈ M`（发了 3 步 × M/4） | 每卡只有 M/N |
| **AllGather** | `(N-1)/N · M ≈ M` | 每卡拼回完整 M |
| **AllReduce = RS + AG** | `≈ 2M` | 每卡有完整 M |

对比一下：

- 如果只做 **Reduce**（求和到一张根卡）：根卡拿到完整结果，其他卡两手空空；
- **ReduceScatter**：求和 + 结果均分，**每卡只有 1/N**；
- **AllReduce**：ReduceScatter 之后再接一次 AllGather，每卡恢复完整数据 —— 所以 AllReduce 的通信量是 ReduceScatter 的两倍。

拆开后，ReduceScatter 结束的那个瞬间，每张卡手里**只有 1/N 的数据**——这是整个优化链条里最"瘦"的时刻，量化、降维、以及"只算自己那片"的 RMSNorm，都插在这个缝隙里。

#### 3.4 放回 TP 的上下文里

TP 行并行输出 `Y = Σ XᵢWᵢ` 之后走完整流程：

```
各卡部分和 XᵢWᵢ（完整大小，比如 [4096 tokens, 4096 hidden]）
        │
        ▼ ReduceScatter     ← 通信 ≈ M，跨卡求和
每卡持有 [4096 tokens, 4096/N] 的求和结果分片
        │
        ▼ （优化点：每卡只对自己的 1/N 分片做 RMSNorm / 量化 / 降维）
        │
        ▼ AllGather         ← 通信 ≈ M，拼回完整数据
每卡恢复完整 Y，进入下一层
```

**一句话总结**：ReduceScatter = "各卡把数据切块沿环传递、边传边加，求和完成的同时结果已经被均分，每卡只落 1/N 分片"。它用约一半的通信量完成了 AllReduce 里"求和"的那一半职责，同时留下了一个数据最小的中间态——这就是它被从 AllReduce 里拆出来单独利用的原因。

---

### 四、AllGather 详解：每卡拼回完整数据，通信量 (N-1)/N·M ≈ M

**原问题：**

> 请解释一下AllGather，(N-1)/N · M ≈ M，每卡拼回完整 M。这个过程是怎样的

**解答：**

AllGather 就是 ReduceScatter 的"下半场"，而且更简单，因为**没有加法了，纯搬运**。

#### 4.1 场景设定：ReduceScatter 刚结束的时刻

4 张卡，每卡手里只有求和结果的 1/4 分片（2 个元素）：

```
卡0: [S₄ S₅]        ← 分片2
卡1: [S₆ S₇]        ← 分片3
卡2: [S₀ S₁]        ← 分片0
卡3: [S₂ S₃]        ← 分片1
```

**AllGather 的目标**：每张卡都把其他 3 张卡的分片收集过来，最终**每卡都拥有完整的 8 元素向量**：

```
所有卡 → [S₀ S₁ S₂ S₃ S₄ S₅ S₆ S₇]
```

注意和 Reduce 类操作的区别：**AllGather 没有任何求和**，每个分片在它诞生的卡上已经是最终值，剩下的事只是"复制给所有人"。本质是**多对多的广播**。

#### 4.2 Ring AllGather 的过程：N-1 = 3 步

还是排成环 `0→1→2→3→0`。规则只有一条：**每一步，每卡把自己手里"最新收到的分片"传给右邻居**。

##### 初始状态（每卡 1 个分片）

```
卡0: {分片2}
卡1: {分片3}
卡2: {分片0}
卡3: {分片1}
```

##### Step 1：把自己的分片发给右邻居

```
卡0 发 分片2 → 卡1      卡1: {分片3, 分片2}
卡1 发 分片3 → 卡2      卡2: {分片0, 分片3}
卡2 发 分片0 → 卡3      卡3: {分片1, 分片0}
卡3 发 分片1 → 卡0      卡0: {分片2, 分片1}
```

##### Step 2：把**刚收到的那个**分片继续转发

```
卡1 发 分片2 → 卡2      卡2: {分片0, 分片3, 分片2}
卡2 发 分片3 → 卡3      卡3: {分片1, 分片0, 分片3}
卡3 发 分片0 → 卡0      卡0: {分片2, 分片1, 分片0}
卡0 发 分片1 → 卡1      卡1: {分片3, 分片2, 分片1}
```

##### Step 3：再走一步

```
卡2 发 分片2 → 卡3      卡3: {分片1, 分片0, 分片3, 分片2} ✔ 集齐
卡3 发 分片3 → 卡0      卡0: {分片2, 分片1, 分片0, 分片3} ✔ 集齐
卡0 发 分片0 → 卡1      卡1: {分片3, 分片2, 分片1, 分片0} ✔ 集齐
卡1 发 分片1 → 卡2      卡2: {分片0, 分片3, 分片2, 分片1} ✔ 集齐
```

##### 结束：每卡把 4 个分片按序号拼好

```
所有卡: [S₀ S₁ | S₂ S₃ | S₄ S₅ | S₆ S₇]   ← 完整的 Y
```

可以直观理解为：**每个分片沿环"环游"一圈，每经过一张卡就留下一份拷贝**，N-1 步后所有分片在所有卡上都留下了拷贝。

#### 4.3 通信量：为什么是 (N-1)/N · M ≈ M

算一下每张卡的**发送量**：

- 总共 N-1 = 3 步；
- 每一步，每卡发送**一个分片**，大小是 M/N = 2 个元素；
- 所以每卡总发送量 = `(N-1) × M/N = (N-1)/N · M`。

代入数字：每卡发 3 步 × 2 元素 = **6 个元素 = 3/4 · M**。

当 N 很大时，`(N-1)/N → 1`，所以约等于 **M**。直觉解释：

> 每卡要把自己那 1/N 分片复制给其余 N-1 张卡。直接点对点发的话是 `(N-1)·(M/N)`；环形算法让数据"顺路捎带"，每卡总发送量同样是 `(N-1)/N·M` —— **和你拥有的分片大小无关，约等于一份完整数据 M**。

同理，每卡**接收量**也是 `(N-1)/N · M ≈ M`（要收齐其他 N-1 张卡的分片）。

#### 4.4 和 ReduceScatter 对照着看

两者结构完美对称，唯一的区别是"路过时干什么"：

| | ReduceScatter | AllGather |
|---|---|---|
| 每卡初始数据 | 完整 M（部分和） | 1/N 分片（最终结果的一片） |
| 环上传递时 | **累加**（receive → add → 转发） | **纯转发**（receive → 存下 → 转发） |
| 步数 | N-1 | N-1 |
| 每卡发送量 | (N-1)/N · M ≈ M | (N-1)/N · M ≈ M |
| 每卡结束数据 | 1/N 分片 | 完整 M |
| 通信效果 | 数据**收缩** M → M/N | 数据**膨胀** M/N → M |

把两者拼起来就是 AllReduce：

```
完整M ──ReduceScatter──► M/N ──AllGather──► 完整M
        通信 ≈ M              通信 ≈ M
        └────── 合计 ≈ 2M ──────┘
```

这也再次印证了最初那个优化点：**AllGather 每卡必须传约 M 的数据，所以 M 越小越好**。如果在 ReduceScatter 和 AllGather 之间先做了 FP16→FP8 量化，AllGather 的 M 直接减半；如果先做了 MLA 的低秩降维（比如 4096 维 → 512 维），M 缩到 1/8 —— AllGather 的通信量跟着同比例缩小。这就是"拆开 AllReduce、在中间插算子"的全部收益来源。

**一句话总结**：AllGather = 每个分片沿环环游一圈、沿途留拷贝，N-1 步后每卡集齐所有分片拼回完整数据；每卡收发各约一份完整数据量 M，它的通信成本正比于"要传播的东西有多大"——所以先把东西变小再 Gather，是通信优化的核心手法。

---

### 五、直接做 AllReduce 的过程和通信量

**原问题：**

> 直接做AllReduce的过程和通信量是怎样的？

**解答：**

结论先行：环形 AllReduce 内部就是顺序执行 ReduceScatter 再执行 AllGather，共 **2(N-1) 步**，每卡通信量 **2(N-1)/N·M ≈ 2M**。

#### 5.1 场景设定（回到最初的例子）

4 张卡，每卡一个 8 元素的**部分和**向量（M = 8 元素）：

```
卡0: [a₀ a₁ ... a₇]
卡1: [b₀ b₁ ... b₇]
卡2: [c₀ c₁ ... c₇]
卡3: [d₀ d₁ ... d₇]
```

目标：**每张卡**都拿到完整求和结果 `Y = [S₀ S₁ ... S₇]`，其中 `Sᵢ = aᵢ+bᵢ+cᵢ+dᵢ`。

#### 5.2 先看两种"朴素做法"，理解为什么工程上不用

##### 做法 A：All-to-All 全互换

每张卡把自己的完整向量发给其余 3 张卡，收齐 4 份后本地相加。

- 每卡发送量：**(N-1) · M = 3M**（完整向量发 3 份）
- 缺点：通信量随 N 线性增长，N=64 时每卡要发 63M，完全不可行。

##### 做法 B：先 Reduce 到主卡，再 Broadcast

- 第 1 步：所有卡把数据发给卡 0，卡 0 求和 → 每卡发送 **M**，但卡 0 收 3M，**单点瓶颈**（可以用树形 Reduce 缓解到 log N 跳）；
- 第 2 步：卡 0 把结果广播给所有人 → 又是 ≈ M 量级；
- 总量也是 ≈ 2M，但根卡收发严重不均，且树形结构延迟跳数高。

##### 做法 C：Ring AllReduce（NCCL 等库的主流选择）

把"求和"和"扩散"都摊到环上，每张卡收发完全均匀，总步数固定。**它内部就是两阶段：**

```
阶段1: ReduceScatter   (N-1 步，每卡发 (N-1)/N·M)
阶段2: AllGather       (N-1 步，每卡发 (N-1)/N·M)
```

#### 5.3 Ring AllReduce 完整过程（4 卡，2×3 = 6 步）

##### 阶段一：ReduceScatter（Step 1~3）—— 边传边加，数据收缩

每卡先把向量切成 4 块，沿环传递、路过即累加：

```
Step 1:  卡0发A1→卡1, 卡1发B2→卡2, 卡2发C3→卡3, 卡3发D0→卡0   (各累加)
Step 2:  部分和继续右传，再各累加一次
Step 3:  再走一步，每个分片集齐 4 卡贡献
```

**阶段一结束**（每卡只有一个求和完毕的分片，共持有 M/N）：

```
卡0: [S₄ S₅]      卡1: [S₆ S₇]      卡2: [S₀ S₁]      卡3: [S₂ S₃]
```

##### 阶段二：AllGather（Step 4~6）—— 纯转发，数据膨胀

分片沿环环游，沿途留拷贝：

```
Step 4:  各卡把自己的分片发给右邻居
Step 5:  把刚收到的分片继续转发
Step 6:  再转一次，每卡集齐 4 个分片
```

**最终状态**：

```
卡0 = 卡1 = 卡2 = 卡3 = [S₀ S₁ S₂ S₃ S₄ S₅ S₆ S₇]  ✔
```

#### 5.4 通信量逐笔账

| | 步数 | 每步每卡发送 | 每卡总发送 |
|---|---|---|---|
| 阶段一 ReduceScatter | N-1 = 3 | M/N = 2 元素 | 3 × 2 = 6 元素 = (N-1)/N·M |
| 阶段二 AllGather | N-1 = 3 | M/N = 2 元素 | 3 × 2 = 6 元素 = (N-1)/N·M |
| **AllReduce 合计** | **2(N-1) = 6** | | **12 元素 = 2(N-1)/N·M ≈ 2M** |

每卡**发送** ≈ 2M，**接收**也 ≈ 2M。当 N 很大时系数 2(N-1)/N → 2，与 N 无关 —— 这是 Ring 算法的精髓：**不管多少卡，每卡通信量恒定 ≈ 2M**，所以它能扩展到几百上千卡。

#### 5.5 放回大模型的尺度感受这个数字

以 DeepSeek-V3 级别模型为例（hidden = 7168，FP16）：

- 假设单卡 micro-batch 有 4096 个 token，一次 AllReduce 的数据量 M = 4096 × 7168 × 2 字节 ≈ **56 MB**；
- 每卡通信量 ≈ 2M ≈ **112 MB**；
- 每层 2 次（Attention 输出 + MLP 输出）× 61 层 = **122 次 AllReduce / 每个 forward**；
- 单卡每 forward 通信总量 ≈ **13.7 GB**。

NVLink（900 GB/s）上约 15 ms，跨机 RDMA（如 400 Gb/s ≈ 50 GB/s）上就要 **270+ ms** —— 远超计算时间。这就是为什么：

1. 长序列、大并发（M 随 token 数线性涨）时通信成为主瓶颈；
2. 把 AllReduce 拆成 RS + AG、在中间把 M 变小（FP16→FP8 减半、MLA 降维 7168→512 缩到 1/14），省的是**最痛的阶段二 AllGather**（也省阶段一的部分），收益直接按比例兑现。

**一句话总结**：直接调 AllReduce = 环形执行"ReduceScatter + AllGather"两个阶段，共 2(N-1) 步、每卡通信 ≈ 2M；它对调用者是个黑盒，中间那个"每卡只有 M/N"的最瘦时刻被锁在库里，量化/降维插不进去，只能老老实实传 2 份完整激活 —— 这正是传统方案在大并发长序列下通信量爆炸的根源。

---

## 贰：SFA Lightning Indexer稀疏注意力及其TP不亲和问题

**原问题：**

> 请帮我详细解释一下下面的话："SFA（Sparse Flash Attention）配合 Lightning Indexer，将每个 Q 实际参与计算的 KV 限定为 2048，注意力复杂度从平方降至线性。但目前vllm-ascend的SFA后端对张量并行（TP）不亲和, 主要原因是Indexer 的 TopK 难以沿 head 维张量并行, 需要在 Lightning Indexer前all gather TP, 导致冗余计算."

**解答：**

这段话讲的是 DeepSeek-V3.2 引入的稀疏注意力（DSA，在 vllm-ascend 中叫 SFA 后端）在张量并行下的一个工程痛点。

### 6.1 背景：SFA + Lightning Indexer 在做什么

#### 要解决的问题：注意力的平方复杂度

标准注意力里，每个 query token 要和**所有历史 KV** 计算：序列长 L 时，prefill 的计算量是 O(L²)，长序列（64K/128K）下这是推理的主要成本。

#### Lightning Indexer：先粗筛，再细算

DeepSeek-V3.2 的做法（DSA = DeepSeek Sparse Attention）是给注意力加一个**轻量"筛选器"**——Lightning Indexer：

```
第1步：Indexer 打分
  对每个 query token t，用一个很小的打分器给所有历史位置 s 算"索引分"：
      I(t,s) = Σⱼ w(t,j) · ReLU( qᴵ(t,j) · kᴵ(s) )      j 遍历 Indexer 头
  （Indexer 头数少、头维度小，所以这一步比真正的注意力便宜得多）

第2步：TopK 筛选
  对每个 query，只保留索引分最高的 2048 个 KV 位置

第3步：稀疏主注意力（SFA kernel）
  主注意力（MLA）只 gather 这 2048 个 KV 来算，其余全部跳过
```

#### 为什么复杂度从平方变线性

```
稠密注意力：  每个 query 看 L 个 KV      → O(L²)
稀疏注意力：  每个 query 只看 k=2048 个  → O(L × 2048)
```

k 是**固定常数**（2048），不随 L 增长，所以总成本随序列长度**线性**增长。Indexer 打分本身虽是 O(L²) 形式，但它的计算密度（头数×维度）远小于主注意力，且是连续 GEMM 可以打满算力，常数小得多——这就是"平方降至线性"的实际含义。

### 6.2 TP 的常规切法：沿 head 维切

张量并行切注意力的标准做法（Megatron 式，也是 vLLM 里 MLA 的做法）：

```
128 个注意力头，TP=8 → 每卡持有 16 个头
每卡：只算自己那 16 个头的 Q/K/V/O，最后 AllReduce 整合
```

注意 MLA 的特殊性：所有头**共享同一份压缩后的 latent KV cache**，所以 KV cache 在每卡上是**完整复制**的，只有"头"被切开。

### 6.3 核心问题：为什么 Indexer 的 TopK 难以沿 head 维 TP

这是整段话的关键，有三个环环相扣的原因：

#### 原因一：索引分是"跨 Indexer 头求和"的

回看打分公式：

$$I(t,s) = \sum_{j=1}^{H^I} w_{t,j} \cdot \text{ReLU}(q^I_{t,j} \cdot k^I_s)$$

注意那个 **Σⱼ —— 最终分数是把所有 Indexer 头的贡献加在一起的一个标量**。

如果按常规思路把 Indexer 头沿 TP 切开（TP=8，每卡 8 个 Indexer 头），那么：

```
每卡只能算出 部分分数:  I_partial(t,s) = Σⱼ∈本卡的头 (...)
真实分数需要:           I(t,s) = Σ所有卡 I_partial(t,s)   ← 必须跨卡 AllReduce
```

而要 AllReduce 的对象是**分数矩阵 `[query数 × KV长度]`** —— 长序列下这正是你千方百计想避免的 O(L²) 大小的东西！比如 128K 上下文、8K 个 query，这个矩阵有 10⁹ 个元素。为了省通信去切分，结果要通信一个平方级矩阵，完全本末倒置。

#### 原因二：TopK 是全局选择，必须看到完整分数

TopK 选"分数最高的 2048 个位置"，前提是**拿到求和完毕的完整分数行**。部分分数上做 TopK 是错的（每卡选出的候选互不包含对方的高分项）。所以 TopK 之前必须完成跨头求和——这就回到了原因一的死结。

#### 原因三：选出的 2048 个位置是所有主注意力头共享的

DSA 里稀疏掩码是 **per-token 一份、所有头共用**（不是每个头各选各的）。这意味着每卡上那 16 个主注意力头需要的是**同一份 TopK 索引**。既然索引必须全局一致，就没有"各卡各选自己头的索引"这种省事切法。

**三个原因合起来**：Indexer 的分数定义（跨头求和）+ TopK 的全局性 + 掩码的头间共享，使得它**无法像主注意力那样干净地沿 head 维切分**。

### 6.4 现状的 workaround：AllGather + 冗余计算

既然切不开，vllm-ascend 目前的做法是"**不切**"：

```
TP 各卡 qᴵ 分片 ──AllGather──► 每卡拿到完整的 qᴵ（全部 Indexer 头）
                                    │
                                    ▼
              每卡各自跑完整的 Lightning Indexer（全头打分 + TopK）
                                    │
                                    ▼
              每卡得到一模一样的 2048 索引，喂给自己的主注意力头
```

这带来两层代价：

1. **冗余计算**（原话的核心）：N 张卡拿着相同输入、算相同的 Indexer 分数、做相同的 TopK，得到相同结果——**同一份工作重复了 N 遍**。Indexer 打分是 O(T×L×Hᴵ×d) 的 GEMM，prefill 长序列时这不是小数目；TP 越大，浪费的算力越多（TP=8 就是 8 倍重复）。

2. **额外的 AllGather 通信和同步**：虽然 AllGather 的对象（Indexer 的 qᴵ，形状 `[T, Hᴵ, d]`）不算大，但它在每层、每个 forward 的关键路径上，且是一个全员同步点，TP 域内任何一张卡慢都得等。

这就是所谓的"**对 TP 不亲和**"：TP 的精神是"把计算分摊到各卡"，而 Indexer 这里被迫"每卡全量复制计算"，TP 度越高反而浪费越大——扩展性是反的。

### 6.5 可能的优化方向

| 方向 | 做法 | 难点 |
|---|---|---|
| **分工计算 + 广播索引** | 不按头切，而按 query/token 维把 Indexer 打分分摊到各卡，TopK 后把索引（`[T, 2048]` 的 int32，很小）AllGather | 需要跨卡规约分数或重新组织 TopK；索引广播虽小但仍是同步点 |
| **规约压缩后的分数** | 各卡算部分头分数后 AllReduce，但先对分数做降采样/量化 | 分数矩阵 O(L²) 大小，压缩有精度风险 |
| **单卡/单组专职算 Indexer** | 指定一个 rank 算完全部，把索引广播给其他卡 | 负载不均，该 rank 成为短板 |
| **计算与通信重叠** | AllGather 与上一层计算流水重叠 | 治标，冗余计算还在 |

**一句话总结**：本质上这是个"**求和顺序 vs 切分维度**"的矛盾：Indexer 的分数定义把 head 维"锁死"在跨头求和里（Σⱼ），而 TP 恰恰想沿 head 维切——切了就要 AllReduce 一个 O(L²) 大小的分数矩阵，不切就得 AllGather 输入让每卡重复全量计算。和前面"拆开 AllReduce 找最瘦中间态"是同一种思维，这里要找的是 Indexer 计算图里真正可以下刀切分的那个维度（比如 token 维，即 Context Parallel 的思路）。
                                                                                                                                                                  

---

## 叄：flashcomm1与-sfa-的并行改造
### FlashComm1（TP→CP）：结合流程图分析 SFA 的并行改造

![img.png](img.png)
**原问题：**

> 请结合上面图中的流程，分析下面的文字：为什么需要
>
> SFA（Sparse Flash Attention）配合 Lightning Indexer，将每个 Q 实际参与计算的 KV 限定为 2048，注意力复杂度从平方降至线性。但目前vllm-ascend的SFA后端对张量并行（TP）不亲和, 主要原因是Indexer 的 TopK 难以沿 head 维张量并行, 需要在 Lightning Indexer前all gather TP, 导致冗余计算
>
> 工作原理
>
> Flashcomm1（TP->CP）:
> 将 Embedding 之后 的 All-Reduce 修改为 Reduce-Scatter，这样就自动将序列中所有 Token 按 TP 域均分至每张卡上了；此外，参考 FlashComm1，将 MOE 最后的 All-Reduce 也修改为 Reduce-Scatter，使得 Attention 模块的输入与输出均是 1/N 的 Token。
> Attention 模块中 down 矩阵、up 矩阵、吸收矩阵及 O 矩阵的权重是单卡全量加载，不涉及 TP 通信。
> 每张卡仅对自己持有的 1/N 序列进行Indexer 模块 TopK 计算以及 Sparse Flash Attention 计算，每张卡处理所有 128 个头。
> 每张卡根据自己持有的 1/N 序列产生新的 KV 后，新增 All-Gather 操作收集所有卡本次新产生的 KV，并补充一步操作原地更新本卡的 KV Cache，使得 KV Cache 正确地包含所有 Token 本次与历史的信息（如 Chunk Prefill 场景会用到历史值）。同理需对 Indexer 模块中新产生的 K 值如法炮制：。

**解答：**

（分析的流程图见上方插图 `img.png`，逐步走图见第八章。）

#### 1 回顾问题：为什么需要这个方案

第六章的结论：传统 TP 沿 **head 维**切，而 Indexer 的 TopK 沿 head 维切不开（分数是跨头求和 Σⱼ、TopK 要完整分数、掩码全头共享），只能 AllGather 后**每卡重复计算全量 Indexer**，TP 越大浪费越大。

FlashComm1 的破法只有一句话：**既然头切不开，那就别切头，改切 token**。

```
TP（切头）:    每卡 = 全部 token × 1/N 的头    → Indexer 死在跨头求和
CP（切token）: 每卡 = 1/N 的 token × 全部 128 个头 → 每卡的 Indexer 完整且互不重叠
```

切 token 之后，Indexer 的三个"死结"全部自动解开：

1. **跨头求和 Σⱼ** → 64 个 Indexer 头都在本卡，求和是纯本地操作；
2. **TopK 需要完整分数行** → 本卡自己 token 的分数行完全在本地算出，TopK 是本地操作；
3. **掩码全头共享** → 掩码本来就是 per-token 的，token 恰好是分片维度，每卡选的掩码天然只属于自己的 token，**各卡做的是真正不同的工作，零冗余**。

#### 2 逐句解读

##### "Embedding 之后的 All-Reduce 改为 Reduce-Scatter；MoE 最后的 All-Reduce 也改为 Reduce-Scatter"

常规 TP 里，Embedding（词表并行）和每个行并行层输出都要 AllReduce，让每卡拿到**完整的 [X, 7168]**。这里把它降级为 ReduceScatter：**求和照做，但结果不再人人全量，而是按 token 维散开**，每卡只落 `[X/N, 7168]`。

- 对应图中入口的 `pad && Reduce-Scatter [X/N, 7168]`（pad 是把 X 补齐到 N 的整数倍）；
- MoE 内部仍然是 TP（专家/矩阵照切），但**出口**同样改成 ReduceScatter，于是"Attention 模块的输入与输出均是 1/N 的 token"——整条链路从 Embedding 到每层之间，激活始终以 **[m, 7168] 的 CP 形态**流动，不再有 7168 维激活的全量复制；
- 通信量上 ReduceScatter ≈ AllReduce 的一半（见第三章），顺手还省了一笔。

##### "down 矩阵、up 矩阵、吸收矩阵及 O 矩阵的权重单卡全量加载，不涉及 TP 通信"

因为不切头了，注意力内部所有 GEMM 都是"本卡 m 个 token × 全部头"，权重必须全量才能算出全部头——图中满屏的 `replicated linear` 就是这个意思。**计算时零通信**，把原 TP 里每层 2 次 AllReduce 彻底消灭。代价：权重显存从 1/N 变成全量（用显存换通信和算力，MLA 的投影矩阵相对 MoE 权重较小，这个交换是划算的）。

##### "每张卡仅对自己持有的 1/N 序列进行 Indexer TopK 以及 SFA 计算，每张卡处理所有 128 个头"

对应图中 `[m, 24576]`（= 128 头 × 192 维）和 Indexer 蓝框。这是方案的核心收益：

- Indexer 计算量从"每卡全量 × N 卡冗余"变成"每卡 1/N，总量恰好一份"——**冗余彻底消除**；
- SFA kernel 同理，每卡只对自己 m 个 query 做稀疏注意力。

##### "新增 All-Gather 收集各卡新产生的 KV，原地更新本卡 KV Cache；Indexer 的 K 如法炮制"

这是方案的**唯一新增通信**，也是设计最巧的地方。问题在于：每卡只产生自己 m 个 token 的新 KV，但**注意力要看全序列**（自己的 query 要 attend 到所有 token 的 KV），TopK 也要在全序列范围内选 2048 个。所以每层必须补一次：

```
本卡新 KV [m, 576] ──All-Gather──► 全序列新 KV [X, 576] → 原地写入本卡 KV Cache
本卡新 indexer K [m, 128] ──All-Gather──► [X, 128]      → 同理更新
```

图中就是 `All-Gather KV [m*N, 576]` 和 Indexer 框里的 `All-gather [X, 128]`。

**为什么这笔通信便宜**——这正是第三~五章"缩减通信对象"原则的极致体现：

| 通信对象 | 维度 | 对比 |
|---|---|---|
| 全量激活（传统 TP 传的东西） | 7168 | 基准 |
| MLA latent KV | **576** | 只有激活的 1/12 |
| Indexer K | **128** | 只有激活的 1/56 |

MLA 把 KV 压缩进 576 维隐空间、Indexer 的 K 只有 128 维——**需要跨卡同步的恰好是这两个最"瘦"的张量**，而 7168 维的激活全程以 RS 形态流动、从不 AllGather。稀疏注意力、MLA 低秩压缩、CP 切分三者在这里严丝合缝地咬合上了。

##### "使得 KV Cache 正确包含所有 Token 本次与历史的信息（Chunk Prefill 场景会用到历史值）"

每层 AllGather 后**就地写入本卡 cache**，保证每卡的 KV Cache 始终是"全序列、含历史"的完整视图：

- **Decode**：新 KV 只有每卡几个 token，AllGather 量极小；
- **Chunked Prefill**：当前 chunk 的 KV 经 AllGather 补齐，历史 chunk 的 KV 在此前几轮的 AllGather 中已经落进 cache——所以 Select 时面对的就是完整的 `[历史 + 本次, 576]`，TopK 语义与单卡完全一致。

#### 3 整体对照：问题 → 解法 → 代价

| | 传统 TP + SFA（现状痛点） | FlashComm1（TP→CP，图中所示） |
|---|---|---|
| 切分维度 | head 维 | **token 维** |
| Indexer | AllGather 后每卡全量重复算 ×N | 每卡只算 1/N token，零冗余 |
| 每层注意力通信 | 2 次 AllReduce（[X,7168] 级别） | **0 次**（权重全量，纯本地） |
| 新增通信 | — | 2 次 AllGather：KV [m→X, 576] + indexer K [m→X, 128]，都很小 |
| Embedding/MoE 出口 | AllReduce | ReduceScatter（量减半，且天然完成 token 均分） |
| 权重显存 | 1/N 分片 | 注意力部分全量复制（主要代价） |

**一句话总结**：FlashComm1 把"沿 head 切、被 Indexer 卡死"的 TP，换成"沿 token 切、Indexer 天然可分"的 CP——用 ReduceScatter 让激活始终以 1/N token 形态流动，注意力权重全量加载换取层内零通信，只为新产生的 latent KV（576 维）和 Indexer K（128 维）各付一次小额 AllGather 来维持全序列 KV 视图。它治的正是第六章的病：不再有全量 Indexer 冗余计算，通信对象也从 7168 维激活换成了 MLA 压缩后最小的那两个张量。

---

### FlashComm1 流程图逐步详解：每一步在做什么、张量怎么变

**原问题：**

> 我觉得我们应该先解释图中的流程。每一步在做什么，以及为什么这样做，每一步的张量是怎么样变换的。

**解答：**

**符号约定**：X = 本 TP 域内总 token 数，N = 卡数，m = X/N（每卡分到的 token 数）。模型按 DeepSeek-V3.2 的维度：hidden = 7168，128 个注意力头，MLA 的 q 低秩维度 1536，KV 压缩维度 576（512 latent + 64 rope），Indexer 64 头 × 128 维。

#### 第一步：Embedding → pad && Reduce-Scatter

```
x (token ids)  →  embedding  →  [X, 7168]        每卡本有全量激活
                 →  pad && Reduce-Scatter  →  [X/N, 7168] = [m, 7168]   每卡只留 1/N
```

- **在做什么**：Embedding 层是词表并行（vocab parallel）的，每卡只算了部分词表的贡献，输出本来要 AllReduce 求和。这里把 AllReduce **降级成 ReduceScatter**：求和照做，但结果按 token 维均分，每卡只落 X/N 个 token 的激活。
- **为什么**：这是整个方案的"定调"一步——从网络入口就确立 **token 维切分（CP 布局）**，之后所有层间流动的激活都保持 `[m, 7168]` 形态，再也不出现全量 [X, 7168]。
- **pad 是干嘛的**：X 不一定是 N 的整数倍，先补几个无效 token 凑整，RS 才能均分；后续计算忽略 padding 即可。

#### 第二步：层输入 [m, 7168]（图中 last layer 输出汇入点）

上一层（或 MoE）的出口同样是 ReduceScatter，所以每层拿到的输入都是**本卡专属的 m 个 token**。从这往下的所有操作，默认都是"每卡独立视角"，卡与卡之间只有两处通信（后面标出）。

#### 第三步：Q 支路 —— 两级投影，本地算出全部 128 个头

```
[m, 7168]  →  q_down_proj (replicated)  →  [m, 1536]     q 压缩到低秩隐空间
[m, 1536]  →  q_up_proj (shard→broadcast 加载) →  [m, (128+64)×128] = [m, 24576]
```

- **在做什么**：这是 MLA 的标准两步。q_down 把 7168 维压到 1536 维低秩（省显存/省计算）；q_up 再展开成 **128 个头 × 192 维**（128 维 nope + 64 维 rope），192×128 = 24576。
- **为什么**：注意和传统 TP 的区别——**没有切头**。每卡对自己 m 个 token 算出**全部 128 个头**的 Q。因为 token 已经切开了，"每卡的 Q 各不相同"，头留全不会产生冗余；而后面 Indexer 需要全头在本地，这一步是前提。
- **replicated / shard+broadcast 标注**：权重在存储时可以分片存放，加载时广播成每卡全量（省加载时间），**计算时零通信**。

#### 第四步：KV 支路 —— 压缩、写 cache、第一次 All-Gather

```
[m, 7168]  →  kv_down_proj (replicated)  →  [m, 576]     ← 本卡新产生的 latent KV
[m, 576]   →  save kv（写入本卡 cache 暂存）
[m, 576]   →  All-Gather KV  →  [m*N, 576] = [X, 576]    ← ★ 通信点 1
```

- **在做什么**：MLA 不存完整 KV，而是把 KV 压缩成 576 维（512 latent + 64 rope）的向量。每卡只算出了自己 m 个 token 的新 KV。
- **为什么需要 All-Gather**：注意力是**沿序列全局**的——本卡的 query 要 attend 到**所有** token 的 KV，而各卡手里只有 1/N。所以每层每卡把新 KV 广播出来，拼成全序列 `[X, 576]`，**原地更新本卡 KV Cache**。加上历史 chunk 留下的部分，本卡 cache 始终是"全序列、含历史"的完整视图（chunked prefill 正确性靠这个保证）。
- **为什么这笔通信便宜**：传的是 576 维压缩 KV，只有激活维度 7168 的 1/12。MLA 的低秩压缩在这里恰好成了通信优化器。

#### 第五步：Indexer 支路 —— 三个小矩阵 + 第二次 All-Gather

图中左侧蓝框，从 [m, 7168] 分出三个 replicated linear：

```
Wq_up:  [m, 7168] → [m, 64×128] = [m, 8192]   Indexer 的 q：64 头 × 128 维
Wk:     [m, 7168] → [m, 128]                   Indexer 的 k（每 token 一个 128 维向量，无头）
              → save k → All-gather → [X, 128]              ← ★ 通信点 2
W_proj: [m, 7168] → [m, 64]                    每个 Indexer 头的打分权重 w(t,j)
```

- **在做什么**：为 Lightning Indexer 准备三件材料——本卡 m 个 query 的索引 q（64 头）、全序列的索引 k（先 save 本卡的，再 AllGather 收齐）、以及逐头权重 w。
- **为什么 k 也要 All-Gather**：Indexer 要给"本卡 query × **全序列**位置"打分，k 必须覆盖全序列，和主 KV 同理。它只有 128 维，比主 KV 还小，是整张图里最便宜的通信。

#### 第六步：Lightning Indexer → top-k indices

```
qᴵ [m, 64, 128]  ×  kᴵ [X+历史, 128]  ×  w [m, 64]
   →  I(t,s) = Σⱼ w(t,j)·ReLU(qᴵ(t,j)·kᴵ(s))     本卡 m 行 × 全序列列 的分数
   →  每行取 Top-2048  →  top-k indices [m, 2048]
```

- **在做什么**：对本卡的每个 query token，在全序列范围内挑出索引分最高的 2048 个位置。
- **为什么这步现在是"干净"的**（对照第六章的痛点）：
  - 64 个 Indexer 头**全在本卡**，跨头求和 Σⱼ 是本地运算；
  - 每个 query 的完整分数行在本地，TopK 是本地运算；
  - 各卡的 query 互不重叠 → **各卡的 Indexer 计算互不重复**，总算力恰好一份，TP 不亲和问题根治。

#### 第七步：Select —— 按索引 gather 稀疏 KV

```
top-k indices [m, 2048]  +  全量 KV cache [X+历史, 576]
   →  Select  →  每个 query 对应的 [2048, 576]
```

- **在做什么**：用索引从完整 KV cache 里把选中的 2048 条 latent KV 抽出来。这是个 gather（按索引取行）操作，把"全序列"裁剪成"每 query 2048 条"。
- **为什么**：主注意力不再看全部 X 个 KV，只看这 2048 个——复杂度从 O(m·X) 降到 O(m·2048)，这就是"平方变线性"落地的位置。

#### 第八步：Sparse Flash Attention → kv_up_proj → o_proj

```
q [m, 128头, 192]  ×  选中的 KV [m, 2048, 576]      ← 速写：kernel 内先做吸收升维（见下）
   →  Sparse_flash_attention  →  [m, 128头, 512]   注意力输出（仍在 latent 空间）
   →  kv_up_proj (replicated)  →  v 升维/吸收展开
   →  o_proj (shard→broadcast 加载)  →  [m, 7168]
```

- **在做什么**：SFA kernel 只对选中的 2048 个 KV 做 FlashAttention；输出经 kv_up/o_proj 投影回 7168 维隐空间。
- **形状疑问：192 维和 576 维怎么直接乘？** 上面第一行是速写，"×"指"做注意力"而非原始 matmul。kernel 内部做的是 MLA 矩阵吸收：q 的 192 维 = 128 nope + 64 rope，K 的 576 维 = 512 latent + 64 rope；打分按结合律改写 score = q_nope·(W_UK·c_kv) = (W_UKᵀ·q_nope)·c_kv，把升维矩阵 W_UK 吸进 q，得到 q̂ = [q̂_nope(512) | q_rope(64)] = **576 维**，与 K 严格同维后点积（内容 512 维 + 位置 64 维两段相加）；V 侧取 latent 的前 512 维加权求和，故输出是 512 维/头。吸收放在 q 侧是每 query 一次的小投影（O(1)），放在 K 侧则是每 cache 行一次的升维（O(X)）——且 cache 只需存 576 维 latent，K/V 一身二任（打分时全 576 维是 K，求和时前 512 维是 V）。
- **为什么**：全程在本卡完成——q 是本卡的、选中的 KV 已 gather 到本卡、权重全量在本卡。**整个 Attention 内部零 TP 通信**（传统方案这里要一次 AllReduce）。
- **张量形状的闭环**：输出 `[m, 7168]` 与输入形状一致，仍是 1/N token 的 CP 布局，直接交给残差连接和下一个 MoE 层；MoE 出口再 ReduceScatter，布局延续到最后一层。

#### 全图张量变换一览

| 步骤 | 操作 | 形状变化（每卡视角） | 通信 |
|---|---|---|---|
| 1 | embedding → pad && RS | [X,7168] → **[m,7168]** | RS（1/N 激活） |
| 3 | q_down / q_up | [m,7168] → [m,1536] → [m,24576]（128头×192） | 无 |
| 4 | kv_down → save → AG | [m,7168] → [m,576] → **[X,576]** | ★AG（576维） |
| 5 | Wq_up / Wk / W_proj | → [m,8192] / [m,128]→**[X,128]** / [m,64] | ★AG（128维） |
| 6 | lightning indexer | [m,X+hist] 分数 → top-k [m,2048] | 无（全本地） |
| 7 | Select | 全量 KV → [m,2048,576] | 无 |
| 8 | SFA → kv_up → o_proj | [m,128,512] → **[m,7168]** | 无 |

**一句话总结全图**：激活从入口起就是 token 分片形态 `[m,7168]`；每卡用全量权重、全部 128 头、只算自己的 m 个 token；唯二的跨卡通信是把新产生的 **576 维 latent KV** 和 **128 维 Indexer K** 各 AllGather 一次补齐全序列——之后 Indexer TopK、稀疏注意力、投影全部本地闭环。整张图只动了两个最小张量，就把传统 TP 里每层 2 次 AllReduce 和全量 Indexer 冗余计算同时消掉了。

---

### 第五步与第六步详解：Indexer 支路的三个小矩阵与 Lightning Indexer TopK

**原问题：**

> 上面提到的 第五步：Indexer 支路 —— 三个小矩阵 + 第二次 All-Gather 和 第六步：Lightning Indexer → top-k indices 需要再详细解释一下。

**解答：**

这两步是整个方案的"题眼"——稀疏注意力到底怎么筛、以及为什么换了 CP 布局之后它变得"干净"。

#### 1 第五步：为什么 Indexer 需要"另起炉灶"的三个矩阵

主注意力（MLA）有自己的 q/k/v 投影，但那套投影是为"精细计算"设计的（128 头 × 192 维、576 维 latent KV），很贵。Lightning Indexer 的定位是**粗筛器**：它只需要回答一个问题——"对 query t 来说，全序列里哪 2048 个位置值得细看？"回答这个问题不需要主注意力那么高的表达精度，所以 DeepSeek-V3.2 给它配了一套**独立的、刻意做小的投影**。

三个矩阵从同一份层输入 `[m, 7168]` 分出来，各管一件事：

```
输入 [m, 7168] ─┬─ Wq_up ─► qᴵ [m, 64×128]   每个 query 的"检索意图"，64 头
                ├─ Wk    ─► kᴵ [m, 128]      每个位置 s 的"档案标签"，无头
                └─ W_proj ─► w  [m, 64]       每个 query 对 64 个头的"信任度"
```

逐个看：

**① Wq_up：Indexer 的 query（有头）**
`[m, 7168] → [m, 8192]`，即 64 头 × 128 维。为什么要 64 个头而不是 1 个？因为"相关"是多义的——有的头负责语法邻近、有的头负责指代、有的头负责语义主题，多头让筛选器能从多个角度同时打分。但注意对比主注意力：**128 头 × 192 维 vs 64 头 × 128 维**，每 token 的 q 体量只有主注意力的 1/3，这就是"Lightning"的第一层含义。

**② Wk：Indexer 的 key（无头，每 token 只有一个 128 维向量）**
`[m, 7168] → [m, 128]`。这是设计上很妙的一点：**k 不分头**。打分公式里，64 个 q 头共用同一个 kᴵ(s)。直觉上，kᴵ 是全序列每个位置的"公共档案标签"，所有检索头都来和这份标签做点积——存一份就够。这让 indexer K cache 的存储和通信都降到了极致（128 维，全图最瘦）。

**③ W_proj：逐头权重（每 query 一个 64 维向量）**
`[m, 7168] → [m, 64]`。输出的是打分公式里 Σⱼ 前面的 **w(t,j)**——第 t 个 query 对第 j 个 Indexer 头的标量权重。它让模型可以按 query 动态调节"这次检索更信哪个头"：比如代码场景多信语法头、长距离依赖场景多信指代头。没有这个权重，64 个头就只能等权相加，表达能力差一截。

**为什么三个矩阵都是 replicated（每卡全量权重、零通信计算）？**
因为它们都很小。粗略算参数量：7168×8192 + 7168×128 + 7168×64 ≈ 6000 万，相比主注意力/MoE 是零头。小权重复制到每卡，远比切开后每层通信划算——这是"小东西复制、大东西切分"的通用原则。

#### 2 第五步：第二次 All-Gather —— 补齐全序列的 kᴵ

```
本卡新 kᴵ [m, 128] → save k（写本卡 indexer cache）→ All-Gather → [X, 128]
```

- **为什么要补齐全序列**：第六步要给"本卡的 m 个 query × **全序列所有位置**"打分。kᴵ 是打分公式的另一半，必须覆盖每一个候选位置 s，而各卡手里只有 1/N 的新 token——所以和第四步的主 KV 完全同理，广播拼全。
- **为什么这笔通信几乎免费**：传的是 128 维向量，是主 KV（576 维）的 1/4.5、激活（7168 维）的 1/56。每卡每层只发 `m × 128` 个元素。全图两次 All-Gather 恰好都落在 MLA/Indexer 压缩出来的最小张量上，这不是巧合，是架构协同设计的结果。
- **cache 视角**：和主 KV cache 一样，All-Gather 结果原地更新本卡的 indexer K cache，叠加上历史 chunk 的部分，本卡始终持有"全序列、含历史"的完整 kᴵ 视图。chunked prefill 时历史 token 的 kᴵ 不用再算，直接从 cache 读。

#### 3 第六步：打分公式逐项拆解

$$I(t,s) = \sum_{j=1}^{64} w(t,j)\cdot \mathrm{ReLU}\big(q^I(t,j)\cdot k^I(s)\big)$$

对**本卡的每个 query token t** 和**全序列每个位置 s**（含历史 cache 里的），算一个标量分数：

1. **`qᴵ(t,j) · kᴵ(s)`**：第 j 个检索头下，query t 和位置 s 的 128 维点积——原始相关度。
2. **`ReLU(·)`**：负数相关度直接归零。效果是每个头只做"投票赞成"，不做"投票反对"——分数天然稀疏、可加性好，也让后面的求和不会被负分互相抵消搅浑。
3. **`× w(t,j)`**：乘上第五步 W_proj 给出的逐头权重，把 64 个头的投票按本 query 的信任度加权。
4. **`Σⱼ`**：加权求和成一个标量 I(t,s)——位置 s 对 query t 的最终"入选资格分"。

**算力账**：分数矩阵形状 `[m, X+历史]`，每个元素成本 ≈ 64 头 × 128 维 × 2 flops，所以总量约 `m·X·64·128·2`。形式上是 O(m·X)（平方族），但对比主注意力的 `m·X·128头·192维`，**常数小了 3 倍以上**，而且 ReLU 点积可以走 FP8/整型、是连续稠密 GEMM 能打满算力。这就是第六章说的"Indexer 打分虽也是平方形式，但常数小得多"。实际 kernel 里通常也不会把 `[m, X+hist]` 整张分数矩阵物化，而是边算边做 top-k（fused kernel）。

**别忘了因果掩码**：严格说只有 s ≤ t 的位置有资格被打分（未来不可见），top-k 前先按因果掩掉 s > t 的列。因为各卡的 token 分片是**连续的一段**，本卡 token 的合法历史 = 前缀各卡的 token + 本卡排在前面的 token，掩码按全局位置索引生成即可，不需要额外通信。

#### 4 第六步：Top-2048 —— 把平方变线性的那一刀

```
分数行 I(t, :)  [X+历史]  →  取前 2048 名  →  top-k indices [m, 2048] (int32)
```

- 输出是一张**索引表**：对本卡每个 query，记下它选中的 2048 个全局位置编号。注意输出**不是分数、不是 KV**，只是 int32 索引——第七步拿这张表去 KV cache 里 gather。
- **2048 是固定常数**，不随序列长度 X 增长。于是第七步之后主注意力的成本是 O(m·2048)——X 涨到天荒地老，每个 query 也只看 2048 条。"平方降线性"就是在这一步落地的，而不是在打分步。

#### 5 为什么第六步在 CP 布局下是"干净的"（对照第六章的痛点）

第六章讲过传统 TP 下这一步的三个死结，现在逐条对照看它们怎么解开的：

| 死结（TP 切头时） | CP（切 token）下的状态 |
|---|---|
| ① 分数要**跨 Indexer 头求和** Σⱼ，头被切到不同卡上，求和要跨卡 AllReduce | 64 个 Indexer 头**全在本卡**（第三步 Q 支路特意不切头就是为这里铺路），Σⱼ 是纯本地加法 |
| ② **TopK 要看到完整的分数行**，每卡只有 1/N 的头 → 只有部分分数，没法选 | 每个 query 的完整分数行 `[X+hist]` 就在本卡，TopK 是纯本地排序 |
| ③ 绕不开 → 只能 All-Gather 后**每卡重复算全量**，N 卡算 N 遍同一份 | 各卡的 query **互不重叠**（各算各的 m 行），全 TP 域加起来恰好算一遍，**零冗余** |

通信栏里第六步标的是"**无（全本地）**"——它需要的三样输入（qᴵ 本卡算的、kᴵ 第五步已 AllGather 补全、w 本卡算的）在进入这步之前就都到位了，打分和 TopK 本身一次跨卡操作都不需要。通信被前置压缩成了第五步那次 128 维的小 All-Gather。

#### 6 把两步串起来看

第五步是"**备料**"：用三个刻意做小（64头×128维 / 无头128维 / 64维权重）的 replicated 矩阵，从同一份激活里榨出粗筛所需的全部原料，并用全图最便宜的一次 All-Gather（128 维）把 kᴵ 补到全序列；第六步是"**筛选**"：多头加权 ReLU 打分 + 本地 TopK，产出 `[m, 2048]` 索引表。前者把通信压到最小张量上，后者因为 token 分片布局而彻底本地化——两步合起来，就是"TP 不亲和"被根治的具体机理。

---

### 第七步与第八步详解：Select 稀疏 KV 与 Sparse Flash Attention

**原问题：**

> 再详细解释一下 第七步：Select —— 按索引 gather 稀疏 KV 和 第八步：Sparse Flash Attention → kv_up_proj → o_proj

**解答：**

这两步是"稀疏"真正兑现收益的地方——第七步把全量 KV 裁成每 query 2048 条，第八步只对这 2048 条做注意力并投回隐空间。

#### 1 第七步在做什么：一次"按表取货"的 gather

第六步产出的索引表 `[m, 2048]`（int32，全局位置编号），这一步拿着它去本卡的完整 KV cache 里**按行取货**：第 t 个 query 选中位置 {s₁, s₂, …, s₂₀₄₈}，就把 cache 里这 2048 行的 576 维 latent 抽出来，堆成它私人的 KV 块。

两个关键观察：

**① 取一次等于 K、V 都取了。** 这是 MLA 的妙处：传统注意力 K 和 V 是两个张量，要 gather 两次；MLA 的 576 维 latent（512 维 c_kv + 64 维 rope）**一身二任**——做 K 时用全部 576 维，做 V 时用其中 512 维。所以一次 gather 就把稀疏注意力的全部原料备齐了。

**② 每个 query 的 KV 是"私人定制"的，不能共享。** 稠密注意力里，一片 KV 被所有 query 复用（FlashAttention 的分块流式读取就建立在这上面）；稀疏之后，query t₁ 选的 2048 个位置和 t₂ 选的**不一样**，没有共享性可言。这是稀疏注意力 kernel 设计的根本难点，也是"Select"必须作为一个显式步骤存在的原因。

#### 2 第七步的代价：不规则访存（"稀疏税"）

稠密注意力读 KV 是**顺序流式**的：连续地址、可预取、带宽打满。Select 是**随机跳读**：2048 个索引在 cache 里东一条西一条，Cache 局部性差。这就是稀疏付出的"税"——

- 访存量从 `m × X × 576` 降到 `m × 2048 × 576`，X 越大省得越多（128K 上下文就是 1/64）；
- 但每字节的"取用效率"下降了，因为是不规则 gather。

**净账仍然血赚**：注意力是访存受限操作，把要搬运的字节数砍掉 1~2 个数量级，远比 gather 的局部性损失值钱。工程上还有两层缓解：

- **索引有局部性**：top-k 选中的位置往往成簇（邻近 token、文档开头、特殊标记），不是完全均匀的随机；
- **Select 可以融进 SFA kernel**：实际实现（如 vLLM/SGLang 的 sparse kernel）往往**不真正物化** `[m, 2048, 576]` 这个中间张量，而是在 FlashAttention 的主循环里拿着索引直接从 paged KV cache 的 block table 里取块——"select"逻辑上是一步，物理上被吸进了下一步的 kernel 里，省掉一次 `[m, 2048, 576]` 的显存写读（这个张量其实不小：m=1024 时约 1.2 GB @bf16）。

#### 3 第七步为什么零通信

索引是本卡第六步算的，cache 是第四步 All-Gather 后本卡的完整视图——取货全程不出本卡。第八章总表里这步通信栏标"无"，原因就在这。

#### 4 第八步 SFA kernel：对"私人 KV"做 FlashAttention

对每个 query t，SFA 做的事和普通注意力一模一样，只是作用对象从全序列换成选中的 2048 条：

$$\text{scores} = q(t)\cdot K_{\text{sel}}^\top \in \mathbb{R}^{2048},\quad p = \text{softmax}(\text{scores}),\quad o(t) = p\cdot V_{\text{sel}} \in \mathbb{R}^{512}$$

几个值得注意的点：

**① 维度的对应关系（MLA 吸收后的形态）。** q 的 192 维 = 128 nope + 64 rope；K 侧用 576 维（512 latent + 64 rope）——打分是"128 维内容 + 64 维位置"两段点积相加（128 维内容段经 W_UK 吸收升维为 512 维后与 c_kv 点积，详见第八章第八步的吸收说明），和稠密 MLA 完全一致，只是对手只有 2048 个。**V 侧用 latent 的前 512 维**，所以每个头的输出是 512 维——注意输出**还在压缩隐空间里**，没有回到 128 维/头的 v 形态，这直接决定了下一步 kv_up_proj 的存在。

**② 不需要因果掩码了。** 稠密 FlashAttention 要在 kernel 里做三角掩码；这里第六步 top-k 时已经只从历史合法位置里选，选出来的 2048 个天然全部合法——kernel 里的掩码逻辑被"前置"到 Indexer 里消化掉了，主循环更干净。

**③ 复杂度账。** 每 query 的注意力成本从 O(X) 变成 O(2048)：打分 `2048 × 576`、加权求和 `2048 × 512`。128 头 × m 个 query 合计约 `m × 128 × 2048 × (576+512) × 2` flops——**与 X 无关**。128K 上下文下，这是稠密注意力的 1/64。第六章说的"平方降线性"，打分步（Indexer）贡献了"便宜但仍是平方"的部分，真正的线性化在这里兑现。

#### 5 第八步 kv_up_proj：把 latent 输出展开回 v

SFA 输出 `[m, 128头, 512]` 还在 512 维压缩空间，而标准的 v 头维度是 128。kv_up_proj（即 MLA 的 W_UV）逐头把 512 → 128 展开：`[m, 128头, 512] → [m, 128头, 128]`。

（工程上常见反过来做——把 W_UV **吸收**进 o_proj，省掉显式展开；但概念流程上这一步就是"v 从隐空间升维"。图中标注 replicated：矩阵不大，每卡全量，纯本地 GEMM。）

#### 6 第八步 o_proj：投回 7168，形状闭环

```
[m, 128头 × 128] = [m, 16384]  →  o_proj  →  [m, 7168]
```

o_proj 是 `[16384, 7168]` 的投影，把 128 个头的输出拼接后投回隐空间。**传统 TP 里这是行并行层，出口必须 AllReduce**；这里因为：

- 输入是本卡 m 个 token 的**全部 128 头**（16384 维完整，无需求跨卡的部分和）；
- 权重全量在本卡（shard→broadcast 只是加载优化）；

所以出口**什么都不用通信**——传统方案每层 2 次 AllReduce 里的一次（注意力出口那次）就死在这里。

**闭环：出口形态 = 入口形态。** `[m, 7168]` 和本层入口形状完全一致，仍是"本卡 1/N token × 全隐维"的 CP 布局：

- 直接加残差（残差也是 `[m, 7168]` 形态，本地加法）；
- 交给下一个 MoE 层（MoE 内部仍可按专家/矩阵切，出口 ReduceScatter 回到 `[m, 7168]`）；
- 如此循环到最后一层。

**整张注意力子层的通信审计**：入口零通信 → q/kv/indexer 投影零通信 → 两次小 All-Gather（576 + 128 维，补 cache）→ Select/SFA/投影全本地 → 出口零通信。传统 TP 的每层 2 次 7168 维 AllReduce，被换成了 2 次"最瘦张量"的 All-Gather，而稀疏化省下的算力（1/64 级别）是纯赚。

#### 7 七、八两步合看的定位

| | 第七步 Select | 第八步 SFA→投影 |
|---|---|---|
| 角色 | **裁剪**：全序列 → 每 query 2048 条 | **细算**：只对选中者做注意力并投回隐空间 |
| 关键操作 | 按索引 gather（不规则访存，可融进 SFA kernel） | 2048 长度 FlashAttention + 两个本地 GEMM |
| 成本特征 | 访存量 ÷(X/2048)，付"稀疏税"（随机读） | 算力 ÷(X/2048)，与 X 解耦 |
| 通信 | 无 | 无（o_proj 出口的 AllReduce 被消灭） |

一句话：第六步决定"看谁"，第七步把"要看的"取到手，第八步只对这些细算并干净地回到 CP 布局——"平方变线性"的收益在第八步兑现，而它成立的前提（cache 完整、权重全量、头不切）全在前面几步铺好了。

---

## 肆：冗余权重消除zero-3-式分片--预取让-cp16-显存与-tp16-相当

**原问题：**

> 解释一下下面的话"冗余权重消除（解决单卡全量权重的显存压力）：借鉴 FSDP / ZeRO-3 分片权重 + 异步通算掩盖，对 o_proj（6.67G）、q_up_proj（2.14G）消除冗余使 CP16 显存与 TP16 相当；将所有层的 O 矩阵权重做TP切分，分别保存在不同卡上，在执行某一层 forward 的同时，传输当前层的 O 矩阵权重（也可以预取后 k 层的权重）。"

**解答：**

这段话是在解决第七章留下的那个**代价**——"注意力权重单卡全量加载"。FlashComm1 为了层内零通信，让每卡复制了全部注意力权重，用显存换了通信；这段话讲的就是怎么把这份多花的显存再省回来。

### 11.1 问题从哪来：CP 的显存账

| 方案 | 每卡注意力权重 | 倍率 |
|---|---|---|
| TP16（切头） | 全量的 1/16 | 1× |
| CP16（不切头，replicated） | **全量 ×16 份** | 16× |

CP16 在计算和通信上赢了，但权重显存是 TP16 的 16 倍。对 DeepSeek-V3.2 这个量级的模型，注意力投影矩阵平铺到几十层后是**好几个 GB 每卡**——原文点名的两个大户：

- **o_proj**（`[16384, 7168]`，每层约 235 MB @bf16，全模型合计 **6.67 GB**）——最大的那个；
- **q_up_proj**（`[1536, 24576]`，合计 **2.14 GB**）。

这两个矩阵加起来 ~9 GB，是"replicated"策略里最肥的两块肉。要削显存，先削它们。

### 11.2 借鉴 FSDP / ZeRO-3：权重"用时取、用完扔"

FSDP/ZeRO-3 的核心思想一句话：**权重分片存，算之前临时拼全，算完就扔**。

```
存储态：  o_proj 沿某维切成 16 片，每卡只持有 1/16（显存回到 TP 水平）
使用时：  算到这一层前，All-Gather 把 16 片拼成完整权重 → 做 GEMM → 释放
```

于是每卡的**持久**显存是 1/16 分片，**临时**显存只有"正在算的这一层的完整权重"这一份缓冲。峰值显存 ≈ `W/16 + 1层完整权重`，而不是 `W 全量`——这就是"消除冗余使 CP16 显存与 TP16 相当"的算术：

$$\text{CP16 改造后} \approx \underbrace{W/16}_{\text{分片持久存}} + \underbrace{\text{少数层的完整权重缓冲}}_{\text{很小}} \;\approx\; \underbrace{W/16}_{\text{TP16 的水平}}$$

注意一个关键区别：**ZeRO-3 是训练方案，这里是推理**。推理没有反向传播、没有梯度/优化器状态，权重又小（相对训练态），所以只需要借鉴它"分片存储 + 按需聚合"这一半思想，实现轻得多。

### 11.3 异步通算掩盖：把取权重的通信藏进计算里

朴素地"用到才取"会把 All-Gather 卡在关键路径上——每层先等权重到齐再算，等于把 CP 省掉的通信又请回来了。解法是**预取（prefetch）**，原文两句话对应两个层次：

**"执行某一层 forward 的同时，传输当前层的 O 矩阵权重"**——通信与计算并行：比如 q/kv/indexer 的 GEMM 在跑的时候，o_proj 的权重片在后台 All-Gather，等注意力算完要用它时，权重刚好到齐。

**"也可以预取后 k 层的权重"**——更深的水线：层 L 的整个 forward 期间，提前把 L+1, …, L+k 层的权重也陆续取来。这个能成立是因为推理的层序是**完全确定的**（不像数据依赖的分支），未来要用哪些权重、什么时候用，调度器一清二楚。

预取深度的权衡：

- k 越大，掩盖越充分，但**临时缓冲**越大（k 层完整权重同时在卡上），显存优势被侵蚀；
- k 越小越省显存，但要求单层计算时间 ≥ 一次权重 All-Gather 的传输时间。

这个条件在 **prefill** 很容易满足（每层是大 GEMM，计算时间 ms 级，而一层 o_proj 只有 235 MB，HCCS/NVLink 几百 GB/s 的带宽下传输是亚 ms 级）；**decode** 计算时间短，掩盖更难，但 decode 时往往 batch 撑大了 GEMM，且权重可以跨 step 驻留复用，工程上有缓和手段。

### 11.4 为什么只切 o_proj 和 q_up_proj，而不是所有权重都 ZeRO 化？

成本收益比：

- **down 投影类**（q_down 7168×1536、kv_down 7168×576、indexer 的三个小矩阵）本来就小，复制 16 份也没多少——**不值得**为它们引入通信；
- **o_proj / q_up_proj** 是最大的两块，切掉它们就把 replicated 显存的增量削掉了大头（原文数据：9 GB 里的几乎全部）；
- **MoE 专家权重**本来就在 EP 下分片存放，天然没有这个问题。

所以这是一个**外科手术式**的优化：只对最大的两个矩阵做分片+预取，其余维持 replicated 零通信策略不动。

### 11.5 和前面章节的关系：这不是开倒车

CP 费了半天劲消灭通信，这里又引入 All-Gather，矛盾吗？不矛盾，因为通信的**性质**不同：

| | 被消灭的激活通信 | 新引入的权重预取 |
|---|---|---|
| 数据 | 每步、每请求都不同的激活 | **只读、跨请求不变**的权重 |
| 时机 | 卡在关键路径上，全员同步点 | 可提前任意时间发起，**完全可掩盖** |
| 调度 | 被动（算到这步必须等） | 主动（调度器按层序预取） |

激活通信是"等米下锅"，权重预取是"提前备料"——前者是延迟，后者只要带宽够就**零代价**。

### 11.6 一句话总结

CP 方案"权重全量复制"的显存代价，通过对**最大的两个矩阵**（o_proj 6.67G、q_up_proj 2.14G）做 **ZeRO-3 式分片存储 + 层序预取（当前层/后 k 层）+ 通算异步掩盖**来消除：持久显存回到 1/16 分片（≈TP16），代价只是一层级的临时缓冲和一笔可被计算完全掩盖的只读权重通信。至此 CP16 相对 TP16：**算力上消除 Indexer 冗余（第六章）、访存上并集缩小、显存上权重冗余消除（本章）**——三块短板全部补齐。

---
## 伍：deepseek-v3-mtp-模块图解草稿模型推理流程投机解码

### 前置概念：投机解码 / 草稿模型 / MTP 三者关系

> 理解本章"草稿模型推理流程"之前，先厘清三个概念。它们是"范式 → 角色 → 实现"的递进关系。

**一句话总结：**

- **投机解码（Speculative Decoding）** 是一个**框架/范式**，定义"小模型快速猜 + 大模型并行验"的协作方式；
- **草稿模型（Draft Model）** 是这个框架里的**角色（proposer）**，负责提出候选 token；
- **MTP（Multi-Token Prediction）** 是**草稿模型的一种具体实现/来源**，也是投机解码里 `method="mtp"` 这一种。

关系：**投机解码 ⊃ 草稿模型 ⊃ MTP**（MTP 只是草稿模型的诸多形态之一，与 EAGLE、Medusa、DSpark 并列）。

**1. 投机解码（框架/范式）**

由 Leviathan et al. 2023《Fast Inference from Transformers via Speculative Decoding》提出。动机：自回归里大模型每生成一个 token 都要完整 forward 一遍（memory-bound 瓶颈）；若让一个"小且快"的模型先猜出 K 个 token，大模型再**一次 forward 并行验证这 K+1 个 token**，就能用一次大模型前向换 K 个 token。

流程（proposer-verifier）：

1. **草稿**：小模型自回归猜 K 个候选 token，连同其概率 p_i；
2. **并行验证**：大模型对这 K 个 token 做一次 forward，得到目标概率 q_i；
3. **接受/拒绝**：从左到右按 min(1, q_i/p_i) 接受；首个被拒的 token 用残差分布 max(0, q_j-p_j) 重采样，其后全部丢弃；
4. 若 K 个全被接受，再额外采一个 bonus token。

关键保证：残差分布技巧让最终输出分布与直接采样大模型**完全等价（无损加速）**。加速由接受率 α 与草稿长度 γ 决定。

**2. 草稿模型（角色）**

投机解码里负责提出候选 token 的 proposer。它不必与大模型输出一致，只要"够接近"（接受率够高）即可带来加速；但必须与目标共享同一 tokenizer/vocab（logits 形状一致）才能比较概率。

按"草稿从哪来"分两类：

| 来源 | 例子 | 说明 |
|---|---|---|
| 独立小模型 | EAGLE/2/3、draft_model | 单独的轻量模型，复用目标 hidden states 或独立前向 |
| 内嵌在目标模型里 | **MTP**、Medusa | 训练时挂在主模型上的额外预测头/模块 |

**3. MTP（一种草稿模型实现）**

思想来自 Gloeckle et al. 2024（Meta）《Better & Faster Large Language Models via Multi-token Prediction》：训练时让模型同时预测未来多个 token，提供更密集的训练信号。DeepSeek-V3 将其工程化并内置到旗舰模型：主干上追加 D 个顺序的 MTP 模块（每个是完整 transformer block，含 attention + MoE），模块间形成因果链，并与主模型共享 embedding 和输出头（lm_head）。

MTP 双重身份（最易混淆）：

- **训练视角**：一个训练目标（L_MTP），用于提升主模型质量；
- **推理视角**：训练后的 MTP 模块可丢弃不用，也可**重新用作投机解码的草稿模型**（接受率常 >80%）。

**三者关系（一张图）：**

```
投机解码（框架）
  ├── proposer（草稿模型）──── 提出候选 token
  │      ├── 独立小模型：EAGLE / EAGLE-3 / draft_model ...
  │      └── 内嵌模块：MTP ★ / Medusa
  └── verifier（目标大模型）── 并行验证 + 接受/拒绝采样
```

补充：MTP 与 Medusa 都是"内嵌式草稿"，但 Medusa 只挂几个独立输出头（轻量），MTP 是完整 transformer block + 因果链（更重但接受率更高）；EAGLE 是"独立式草稿"主流，复用目标 hidden states 构建轻量草稿。

**来源：**
- [Fast Inference from Transformers via Speculative Decoding（Leviathan et al., 2023）](https://arxiv.org/abs/2211.17192)
- [Better & Faster Large Language Models via Multi-token Prediction（Gloeckle et al., 2024）](https://arxiv.org/abs/2404.19737)
- [DeepSeek-V3 技术报告（MTP 工程化实现）](https://arxiv.org/abs/2412.19437)

#### 流程细节：小模型快速猜 + 大模型并行验（阶段归属辨析）

下面把"小模型快速猜 + 大模型并行验"的具体流程讲透，并厘清一个关键点——**"阶段"这个词有歧义**：它既指"请求生命周期"（prefill vs decode），也指"计算形态"（一次并行前向 vs 逐 token 前向）。

**一、具体流程（token 级别）**

假设已解码出前缀 context，长度 L。投机解码一个 step 是这样：

**① 草稿阶段（draft）**：小模型以自回归方式、逐 token 猜出 K 个候选 token `d₁, d₂, …, d_K`，同时记下它给每个 token 的概率 `p₁…p_K`。这一步要跑 **K 次**小模型前向（便宜、串行）。

**② 验证阶段（verify）**：大模型把 `[context(长度 L) + K 个 draft]` 拼成一条序列，**只做一次前向**。靠 Transformer 的**因果 mask**，每个位置只看到它前面的 token，于是这一趟一次性算出 **K+1 个位置的分布**：

| 位置 | 预测的是 | 条件 |
|---|---|---|
| L+1 | 第一个 token | 只看 context |
| L+2 … L+K | 后续 token | context + 前面被接受的 draft |
| L+K+1 | "bonus" token | context + 全部 K 个 draft |

关键：position L+K+1 那个分布是"白赚"的——它对应"如果 K 个 draft 全对，下一个该是什么"。

**③ 接受/拒绝（accept/reject）**：从左到右逐位比较。Greedy 下就是 `draft[i] == target_argmax[i]` 的逐位相等；随机采样下按 `min(1, q_i/p_i)` 概率接受。找到第一个不匹配的位置 j 就停，j 之后的 draft 全丢。

**④ 状态回退 + bonus**：把 KV cache **回卷/裁剪**到不匹配点；如果 K 个全对，则额外采一个 bonus token 作为本 step 的第 K+1 个产出。然后 bonus token（或重采样的那个 token）成为下一轮草稿的锚点。

一个中文例子（K=3）：

```
context = "人工智能是"
草稿:  小模型串行猜 → [的, 未, 来]
验证:  大模型一次前向，输入 [人工智能是 的 未 来]
       → 得 4 个位置的 argmax：是 / 的 / 来 / 方
接受:  "的"√ "未"✗(应为"来") → 接受长度 1，丢弃"未""来"，从位置2重采样
产出:  "的" 确认，本轮净增 token
```

**二、"小模型快速猜"是在 decode 阶段吗？**

**是。** 明确无误。草稿模型的自回归猜测发生在**生成阶段（decode loop）**——此时 prompt 已经 prefill 完、KV 已就位，草稿模型在已有 context 上逐 token 延伸。它本身就是 decode 阶段的一部分。

（唯一例外：像 EAGLE/DFlash 这类草稿需要目标模型的 hidden states 作为输入，而这个 hidden states 是目标模型前向的副产品，但草稿的"猜 token"动作仍发生在 decode loop 内。）

**三、"大模型并行验"也是在 decode 阶段吗？**

**要分两个层面看，这是关键：**

**① 从请求生命周期/调度层面：是 decode 阶段。** 验证发生在投机解码的 decode loop 里，是 prefill 完成之后、生成过程中的一环。代码里 `use_spec_decode = len(scheduler_output.scheduled_spec_decode_tokens) > 0`（`gpu_model_runner.py:4485`）——这个门控只在有 decode 请求时才打开，纯 prefill 阶段根本没有验证动作。

**② 从计算形态/attention 模式层面：它更像一次"迷你 prefill"，而不是 decode。** 因为：

- 典型 **decode**：每个序列**只处理 1 个 token**（当前要生成的那个），做 paged attention；
- 典型 **prefill**：一次处理**多个 token**，用变长因果 mask，把所有位置的 KV 写进 cache；
- **验证这一步**：大模型一次处理 **K+1 个 token**（B 个请求 × (K+1) 个位置的张量），用因果 mask，**一次性写 K+1 个位置的 KV**——这正是 prefill（或说 chunked prefill）的形态，而不是"1 token/step"的 decode。

所以精确的表述是：

> **"小模型猜" = 真 decode（逐 token、串行 K 次）；"大模型验" = 发生在 decode 阶段的、一次 prefill-like 的并行前向（一次性 K+1 个位置）。**

这也正好解释了投机解码为什么快：大模型本要跑 K+1 次"1-token decode"，现在合并成**一次**"K+1-token 的 prefill 式前向"——前向次数从 O(K) 降到 O(1)，而这一次前向因为 token 变多、计算密度提高，边际开销远小于跑 K+1 次 decode。

**来源：**
- [Fast Inference from Transformers via Speculative Decoding（Leviathan et al., 2023）](https://arxiv.org/abs/2211.17192)
- [Speculative Decoding 验证阶段一次前向得到 K+1 个分布的原理（含因果 mask 解释）](https://wenku.csdn.net/answer/epu7sx2ens5n)
- [Prefill and Decode Execution（nano-vllm-speculative-decoding 的 prefill/decode 阶段划分）](https://deepwiki.com/limei1221/nano-vllm-speculative-decoding/3.4.2-prefill-and-decode-execution)

---

### DeepSeek-V3 MTP 模块图解：草稿模型推理流程（投机解码）

**原问题：**

> 请阅读图片"C:\files\vllm_notes\草稿模型推理流程.jpg"，并做出详细的解释。

**解答：**

这张图出自 **DeepSeek-V3 Technical Report**，画的是其 **MTP（Multi-Token Prediction，多 token 预测）模块的结构**，而 MTP 模块在推理阶段正是被当作**"草稿模型"（draft model）**用于投机解码（Speculative Decoding）的。下面分三个层次详细解释。

![草稿模型推理流程](草稿模型推理流程.jpg)

核心算法分三步。

第一步：草稿生成。一个小型草稿模型（比如 Llama-3 8B）以自回归方式快速生成 K 个候选 token。小模型只占 GPU 显存的一小部分，单 token 生成速度比大模型快约 10 倍。

第二步：验证。大型模型（Llama-3 70B）在一次前向传播中处理全部 K 个草稿 token。验证是并行的，目标模型同时为每个位置计算下一个 token 的概率分布，一次前向传播完成 K 个 token 的校验，而自回归方式需要 K 次。

第三步：接受或拒绝。逐个比较草稿模型与目标模型给出的概率分布：一致则接受，不一致则拒绝并从目标模型在该位置的分布中重新采样，同时丢弃后续所有草稿 token。

#### 1 图的整体结构

图中从左到右是三个级联的预测单元，底部是输入 token，顶部是各自要预测的目标 token：

| 模块 | 输入 | 预测目标 | 损失 |
|---|---|---|---|
| **Main Model**（主模型） | $t_1$ | $t_2$（Next Token） | $\mathcal{L}_{\text{Main}}$ |
| **MTP Module 1** | $t_2$ + 主模型隐状态 $h_1^0$ | $t_3$（Next² Token，即隔一个位置的 token） | $\mathcal{L}_{\text{MTP}}^1$ |
| **MTP Module 2** | $t_3$ + Module 1 隐状态 $h_1^1$ | $t_4$（Next³ Token） | $\mathcal{L}_{\text{MTP}}^2$ |

注意图中的三条 **"Shared"** 虚线：**Embedding Layer（词嵌入层）和 Output Head（输出头/LM Head）在主模型与所有 MTP 模块之间共享**。每个 MTP 模块自己独有的参数只有：两个 RMSNorm、一个 Linear Projection 和一个 Transformer Block——参数量非常小，这是它适合做轻量草稿模型的关键。

#### 2 单个 MTP 模块的内部数据流（以 Module 1 为例）

自下而上看中间那个虚线框：

1. **两路输入**：
   - 下一位置 token $t_2$ 经过（共享的）**Embedding Layer** 得到词向量；
   - 主模型在对应位置输出的**隐状态** $h_1^0$（即主模型最后一层 Transformer Block 的输出）。
2. **两路各过一个 RMSNorm**：把 embedding 和隐状态归一化到同一尺度。
3. **Concatenation（拼接）**：把两个向量在特征维度上拼成一个 $2d$ 维向量。
4. **Linear Projection（线性投影）**：把 $2d$ 维压回 $d$ 维。用公式表示即：

$$h'^{\,k}_i = M_k\big[\,\text{RMSNorm}(h^{k-1}_i);\ \text{RMSNorm}(\text{Emb}(t_{i+k}))\,\big]$$

5. **Transformer Block**：模块自己的单层 Transformer，融合"上一个模块的上下文信息"与"新 token 的词信息"。
6. **共享 Output Head**：映射到词表，输出概率分布，预测 $t_3$。

Module 2 的结构完全对称，只是它的隐状态输入换成了 Module 1 的输出 $h_1^1$，输入 token 换成了 $t_3$，预测目标变成 $t_4$。图中右侧的"…"表示理论上可以继续级联 Module 3、4……

**为什么采用链式（顺序）结构而不是并行多头预测？** 因为 Module 2 以 Module 1 的输出为条件，显式建模了"第 2 个未来 token 依赖第 1 个未来 token"的因果链，预测更准确，训练时也更接近真实自回归分解。

#### 3 训练与推理（草稿模型）两个阶段怎么用这张图

**训练阶段**：图中顶部的三个 Cross-Entropy Loss 同时存在，总损失为

$$\mathcal{L} = \mathcal{L}_{\text{Main}} + \frac{\lambda}{D}\sum_{k=1}^{D}\mathcal{L}_{\text{MTP}}^k$$

（DeepSeek-V3 中 $\lambda$ 前 10T token 取 0.3，之后取 0.1）。MTP 损失在这里主要起**训练信号增强**的作用：强迫主模型的隐状态 $h^0$ 编码更远的未来信息，让表示更有"规划性"。

**推理阶段（也就是标题所说的"草稿模型推理流程"）**：训练完成后，MTP 模块被复用为投机解码的草稿模型，流程是：

1. 主模型正常前向，产出下一个 token $t_2$，同时顺手得到隐状态 $h^0$；
2. **MTP Module 1 作为草稿模型**：把刚采样的 $t_2$ 的 embedding 和 $h^0$ 输入，跑自己那一个小的 Transformer Block，通过共享 Output Head **一次前向就提出草稿 token** $\hat{t}_3$；
3. 主模型用一次并行前向**验证**草稿 token：若与主模型自身的预测一致则接受，不一致则从第一个分歧点重新采样；
4. 实际部署时 DeepSeek-V3 **只保留 MTP Module 1**（Module 2 被丢弃），其第二 token 接受率高达 **85%–90%**，使解码吞吐（TPS）提升约 **1.8 倍**。

#### 4 一句话总结

这张图的核心思想是：**主模型负责"准"，轻量 MTP 模块负责"快"**。通过共享 Embedding 和 Output Head，MTP 模块以极小的参数代价（一个 Transformer Block + 一个投影层）学会了利用主模型的隐状态来"猜"下一个再下一个 token；推理时它充当草稿模型提出候选 token，再由主模型一次性并行验证，从而在保证输出分布不变（无损加速）的前提下显著提升解码速度。

---

### 主模型验证流程：Greedy 投机解码（temperature=0 的拒绝采样退化）

**原问题：**

> 请基于上面草稿模型的流程，继续详细解释下面的过程：主模型验证流程：
> 流程 A：Greedy 投机（temperature=0，all_greedy=True）
>
> 验证阶段走 Path 1（rejection_greedy_sample_\*），没有重采样——拒绝处直接用 target 的 argmax。
>
> - target 前向（一次性吃 [上步验证位 + γ 个 draft token]）→ target_logits [B·γ, vocab]
> - target_argmax = argmax(target_logits)
> - 逐位置比较 draft_token_ids vs target_argmax：
>   - pos 0: draft[0]==target_argmax[0]? 是 → 接受 draft[0]
>   - pos 1: draft[1]==target_argmax[1]? 是 → 接受 draft[1]
>   - ...
>   - pos k: 首个"否" → 停止
> - 输出构造 [B, γ+1]：
>   - [0..k-1] = 接受的 draft（= target_argmax）
>   - [k] = target_argmax[k] ← "recovered"在此退化为 target 的 argmax（确定性，无重采样）
>   - [k+1..] = PLACEHOLDER(-1)
> - 若全部 γ 个都接受（无 mismatch）：
>   - bonus = argmax(bonus_logits)

**解答：**

下面接着第十二章的 MTP 草稿模型，详细拆解 **Greedy 投机解码的主模型验证流程**（即 vLLM 中 `rejection_greedy_sample_*` 的 Path 1）。

#### 1 验证阶段的输入从哪来：一次 target 前向吃 γ+1 个位置

回顾上一轮衔接：草稿模型（MTP Module）提出了 γ 个候选 token $d_1, d_2, \dots, d_\gamma$。验证时，target（主模型）的输入序列是：

```
[ t_last,  d₁,  d₂,  ...,  dγ ]
   ↑           ↑____________↑
上步验证位      γ 个 draft token
```

- `t_last` 是**上一步验证结束后最后确定的那个 token**（上一步的 recovered 或 bonus token）——自回归链条的"锚点"，草稿是接在它后面提的，验证也必须从它开始；
- 一次前向产生 **γ+1 个位置的 logits**，按职责分成两块：

| logits 位置 | 含义 | 用途 |
|---|---|---|
| `logits[0]` | target 看完 `t_last` 后的预测 | 验证 $d_1$ |
| `logits[1]` | target 假设 $d_1$ 正确后的预测 | 验证 $d_2$ |
| … | … | … |
| `logits[γ-1]` | target 假设 $d_{\gamma-1}$ 正确后的预测 | 验证 $d_\gamma$ |
| `logits[γ]` | target 看完 $d_\gamma$ 后的预测 | **bonus_logits**，全部接受时才用 |

这就是 `target_logits [B·γ, vocab]` 的来源：B 个请求 × 每请求 γ 个验证位，**展平**成一个张量（kernel 按位置并行处理）；`bonus_logits` 是单独的 `[B, vocab]`。

**关键点**：每个验证位的 logits 都是在"前面的 draft 全对"的假设下算的（teacher forcing），所以无论第 k 位是否被拒绝，前 k 位的验证结果都是有效的——这就是一次前向能并行验证 γ 个 token 的数学基础。

#### 2 逐位置比较：找第一个分歧点 k

Greedy（temperature=0）下 target 的预测是确定性的：

```
target_argmax[i] = argmax(target_logits[i])    i = 0 .. γ-1
```

然后逐位比较 `draft_token_ids[i] == target_argmax[i]`：

```
pos 0:   draft[0] == target_argmax[0] ?  是 → 接受 draft[0]
pos 1:   draft[1] == target_argmax[1] ?  是 → 接受 draft[1]
...
pos k:   首个"否" → 停止，后面的 draft 全部作废
```

用一个具体例子（γ=3）：

```
draft:        [的,  是,  一]
target_argmax:[的,  是,  个]      ← pos 2 首次不一致，k = 2
```

- pos 0、pos 1 接受：输出的就是 draft 原文（此时它恰好等于 target 自己的选择）；
- pos 2 拒绝：draft 的"一"作废。

#### 3 输出构造 [B, γ+1]：accepted + recovered + placeholder

每个请求的输出槽位是 γ+1 个，按首个分歧点 k 填充：

```
[0 .. k-1]  = 接受的 draft token        （k 个）
[k]         = target_argmax[k]          （1 个，"recovered" token）
[k+1 .. γ]  = PLACEHOLDER(-1)           （作废位，后续被掩掉）
```

上面的例子输出就是 `[的, 是, 个, -1]`——这一步实际净产出 **k+1 = 3 个新 token**（2 个白捡的 + 1 个 target 自己的）。

##### 为什么拒绝处直接填 target_argmax——"recovered 退化为 argmax"是什么意思

这要对照**随机采样（temperature > 0）的拒绝采样**来理解。标准投机解码为了保证输出分布与 target 严格一致（无损），拒绝一个 draft token 后不能简单拿 target 的预测顶上去，而要从**修正分布**里重采样：

$$p_{\text{residual}}(x) = \text{norm}\big(\max(0,\ p_{\text{target}}(x) - p_{\text{draft}}(x))\big)$$

即"target 想要、但 draft 没给到足够概率"的那部分质量。这就是 `rejection_sample` 路径里 recovered token 要做一次采样、还要生成均匀随机数的原因。

而 **greedy 是这个一般过程的退化情形**：temperature=0 时两个分布都是 one-hot——

- $p_{\text{target}}$：全部质量压在 `target_argmax` 上；
- $p_{\text{draft}}$：全部质量压在 draft token 上。

拒绝意味着 draft ≠ target_argmax，即 target 在 draft token 上的概率是 **0**。代入修正分布：

$$\max(0,\ p_{\text{target}} - p_{\text{draft}}) = p_{\text{target}} = \text{one-hot}(\text{target\_argmax})$$

从 one-hot 分布里"采样"就是**确定性地取 target_argmax**。所以：

> 没有重采样——拒绝处直接用 target 的 argmax。

这正是 Path 1（`rejection_greedy_sample_*`）比随机路径便宜的原因：**不需要随机数、不需要算 p/q 接受率、不需要构造修正分布**，整个 kernel 就是 `argmax + 相等比较 + 找首个 False`。这也是为什么 vLLM 要求**整个 batch 全是 greedy（`all_greedy=True`）** 才走这条快路径，batch 里只要混了一个带温度的请求就得回落到通用拒绝采样路径。

#### 4 全部接受的特例：bonus token

如果 γ 个 draft **全部命中**（无 mismatch），γ 个槽位全被 draft 填满，还差一个"白赚"的机会——`bonus_logits` 还没用：

```
bonus = argmax(bonus_logits)      ← target 看完最后一个 draft 后的预测
输出 = [d₁, d₂, ..., dγ, bonus]   ← γ+1 个新 token，一个 placeholder 都没有
```

这一步是投机解码收益的上限：一次 target 前向产出 γ+1 个 token。MTP 接受率 85–90%（第十二章的数字）意味着大部分 step 要么全中吃 bonus，要么只错最后一位，平均每前向产出接近 2 个 token，对应 ~1.8× 的解码加速。

#### 5 闭环：这一步的输出如何喂给下一轮

```
本轮输出 [d₁, d₂, 个, -1]  →  scheduler 统计每请求有效长度 (k+1)，提交 token，丢弃 -1
        ↓
最后那个有效 token（"个"）成为下一轮的 t_last
        ↓
MTP 草稿模型拿 t_last 的 embedding + 本次 target 前向对应位置的隐状态 h⁰，提出下一轮 γ 个 draft
        ↓
回到 13.1 节的 target 前向，循环
```

注意最后一环和第十二章图的衔接：MTP 模块需要"主模型在对应位置的隐状态 $h^0$"作为一路输入，这个隐状态**恰好就是本轮验证前向的副产品**——验证用的 γ+1 个位置里，最后一个有效位置（recovered/bonus 位）的隐状态被留给草稿模型，主模型前向一次，验证和下一步的草稿原料同时到手，没有额外计算。

#### 6 全流程总览（γ=3，greedy）

```
① 草稿:   MTP 模块 → draft [的, 是, 一]
② 前向:   target 吃 [t_last, 的, 是, 一] → 4 份 logits（3 验证位 + 1 bonus）
③ 比较:   argmax = [的, 是, 个]  → 首个分歧 k=2
④ 构造:   [的, 是, 个, -1]      → 本 step 净得 3 token
   （若全中: [的, 是, 一, bonus] → 净得 4 token）
⑤ 提交:   有效前缀入队，"个" 作为下轮锚点，其隐状态喂给 MTP 提下轮草稿
```

#### 7 一句话总结

Greedy 验证就是"target 一次前向并行批改 γ 份草稿"——argmax 逐位对答案，对到第 k 位为止；因为温度为零时修正分布退化成 target 自己的 one-hot，拒绝处无需重采样、直接抄 target 的答案（recovered = argmax）；全对则用 bonus_logits 再白赚一个。整步的产出是 k+1 个与纯 target 自回归**逐 token 完全一致**的结果，加速来自"一次前向出多个 token"，而非任何近似。

---

### 追问：T=0 的场景、draft/target 取词过程与 one-hot 退化详解

**原问题：**

> 对于上面的回答，我有以下问题：
> 1. temperature=0是怎样的场景？为什么要做拒绝采样退化？
> 2. draft_token_ids 和 target_argmax 分别是怎样得到的？
> 3. 没有重采样——拒绝处直接用 target 的 argmax这里怎么解释？ 什么是one-hot？

**解答：**

三个问题其实是同一条线上的：**T=0 让"概率分布"退化成"唯一答案"，于是一切随机性都消失了**。下面逐个展开。

#### 1 temperature=0 是怎样的场景？为什么会"退化"？

##### temperature 在做什么

模型最后一层输出的是 logits（每个词一个原始分数），变成概率要经过 softmax；temperature T 是 softmax 前对 logits 的缩放：

$$p(x_i) = \frac{e^{z_i / T}}{\sum_j e^{z_j / T}}$$

- **T 大** → 分数被压平 → 概率分布更均匀 → 输出更多样、更"有创意"，但也更容易跑偏；
- **T 小** → 分数差距被放大 → 概率更集中在最高分词上；
- **T = 0** → 不走采样了，**直接取 argmax**：每次固定选分数最高的那个 token，输出完全确定（同样输入永远得到同样输出）。

##### temperature=0 的典型场景

| 场景 | 为什么要 T=0 |
|---|---|
| 代码生成、数学推理 | 答案是"对错分明"的，要的是最可能的那个解，不是多样性；采样还可能引入语法错误 |
| 结构化输出（JSON、工具调用、SQL） | 格式错一个字符就解析失败，必须走概率最高的"安全路径" |
| 评测基准（benchmark） | 结果必须可复现，采样会让每次跑分不一样 |
| 线上确定性服务 | 同一输入要求同一输出（缓存命中、审计、调试都依赖这点） |

所以"T=0"不是边缘场景，恰恰是生产环境最常见的配置之一，vLLM 才值得为它单开一条 Path 1 快路径。

##### 为什么叫"拒绝采样退化"

投机解码的**通用算法**（Leviathan et al. 2023）是为带温度的采样设计的，规则是：

1. 草稿提出 token $x$，以概率 $\min(1,\ \frac{p_{\text{target}}(x)}{p_{\text{draft}}(x)})$ 接受它；
2. 若拒绝，从修正分布 $\text{norm}(\max(0, p_{\text{target}} - p_{\text{draft}}))$ 里**重新采一个**。

这套规则能保证输出分布和 target 模型逐点相等（无损加速）。但它内部要做除法、生成随机数、构造并归一化修正分布——都是为"分布有宽度"准备的。

当 T=0 时，两个分布都塌缩成只有一个非零点的分布（one-hot，见 14.3 节），把这套通用公式代进去算一遍，会发现：

- 接受概率 $\min(1, p(x)/q(x))$ 只剩两种取值：draft 和 target 选的同一个词 → $1/1 = 1$（必接受）；不同 → $0/1 = 0$（必拒绝）——**除法和随机数都不需要了，一次相等比较就等价于"按概率掷骰子"**；
- 修正分布 $\max(0, p - q)$ 算出来就是 $p_{\text{target}}$ 本身——**"重采样"采到的必然是 target 的 argmax**。

通用算法的每一步都在，只是每一步的"随机"都变成了"必然"。这就是"退化"的含义：**不是换了一个算法，而是同一个算法在 T=0 时的特例形式**，就像"圆是椭圆的特例"。工程上为这个特例单独写一个只做 argmax + 比较的 kernel，就是 `rejection_greedy_sample_*`。

#### 2 draft_token_ids 和 target_argmax 分别是怎样得到的？

两者来自**两个不同的模型、两种不同的前向方式**——这正是投机解码"一快一慢"的分工。

##### draft_token_ids：草稿模型 γ 次"便宜的串行"前向 + argmax

接第十二章的 MTP 结构，逐步生成（以 γ=2 为例）：

```
第1个草稿 token d₁：
   Emb(t_last) + 主模型隐状态 h⁰
   → RMSNorm → 拼接 → Linear → 1层Transformer → 共享Output Head
   → logits over vocab → 【argmax】→ d₁

第2个草稿 token d₂：
   Emb(d₁) + MTP Module 1 自己的隐状态 h¹
   → （若部署了 Module 2 则过 Module 2；单模块时则用同一模块链式推进）
   → logits → 【argmax】→ d₂
```

要点：

- 每次只过**一个 Transformer Block**（而不是主模型的 61 层），参数量小、延迟低，串行跑 γ 次也比主模型一次前向便宜得多；
- 因为整个 batch 是 `all_greedy`，草稿侧取词也是 **argmax**——草稿模型自己输出的是概率分布，但 T=0 下"从草稿分布采样"同样退化为取最大；
- 最终得到 `draft_token_ids [B, γ]`：B 个请求，每个请求 γ 个候选。

##### target_argmax：主模型 1 次"贵的并行"前向 + 逐位 argmax

```
输入 [t_last, d₁, d₂, ..., dγ]   （γ+1 个位置一次喂入）
→ 主模型 61 层完整前向（teacher forcing：每个位置的预测都建立在"前缀原样照抄"上）
→ 每个位置一份 logits over vocab
→ 前 γ 份 logits 逐位置【argmax】→ target_argmax [B, γ]
→ 最后一份 logits 留作 bonus_logits [B, vocab]
```

对照表：

| | draft_token_ids | target_argmax |
|---|---|---|
| 来源 | MTP 草稿模块（1 层小 Transformer） | 主模型（全部 L 层） |
| 前向次数 | γ 次串行（每次很便宜） | **1 次**并行（一次很贵） |
| 取词方式 | argmax（greedy 草稿） | 逐位置 argmax |
| 角色 | 猜得快、可能错 | 判得准、是标准答案 |
| 张量形状 | [B, γ] | [B, γ]（+ bonus [B, vocab]） |

验证的全部逻辑就是把这两个 `[B, γ]` 张量**逐元素比相等**。

#### 3 什么是 one-hot？为什么"拒绝处直接用 target 的 argmax"？

##### one-hot 是什么

词表假设有 V 个词（比如 DeepSeek 的 ~13 万）。一个 token 的 **one-hot 向量**是长度为 V、**只有一个位置是 1、其余全是 0** 的向量：

```
词表:      [我,  爱,  你,  中,  国, ...]
one-hot:   [ 0,   0,   0,   1,   0, ...]     ← 表示"中"
```

把 one-hot 看成**概率分布**：它表示"100% 的概率选第 4 个词，其他词概率全为 0"。从这样的分布里"采样"，**无论采多少次，结果都是第 4 个词**——随机性已经名存实亡。

T=0 时模型的输出就是这种分布：softmax 的极限形态，全部概率质量压在 argmax 那个词上。

##### 用具体数字把"重采样退化"算一遍

设词表只有 5 个词，拒绝发生（draft 选了词②，target 的 argmax 是词④）：

```
p_target (one-hot):  [0,  0,  0,  1,  0]     ← target 100% 选词④
p_draft  (one-hot):  [0,  1,  0,  0,  0]     ← draft 100% 选词②
```

按通用算法，拒绝后要从修正分布 $\text{norm}(\max(0,\ p_{\text{target}} - p_{\text{draft}}))$ 重采样。逐项算：

```
位置①:  max(0, 0-0)  = 0
位置②:  max(0, 0-1)  = 0     ← draft 的词被"扣掉"了
位置③:  max(0, 0-0)  = 0
位置④:  max(0, 1-0)  = 1     ← target 的词原样留下
位置⑤:  max(0, 0-0)  = 0

求和 = 1，归一化后还是 [0, 0, 0, 1, 0] —— 就是 p_target 本身
```

从这个分布里"采样"，结果**必然是词④ = target 的 argmax**。既然结果在计算前就知道，"采样"这一步就是多余的——kernel 里直接写 `output[k] = target_argmax[k]` 即可。

这就是那句话的完整含义：

> **没有重采样——拒绝处直接用 target 的 argmax。**

再补上接受一侧的对称推导：若 draft 命中（都选词④），接受概率 $\min(1, \frac{p_{\text{target}}(④)}{p_{\text{draft}}(④)}) = \min(1, 1/1) = 1$，必接受；若未命中，$\min(1, 0/1) = 0$，必拒绝。所以"逐位置比较 `draft == target_argmax`"这**一次相等判断，就是通用拒绝采样规则在 T=0 下的精确化身**——不多一步随机，也不少一分正确性。

#### 4 一句话总结

T=0（greedy）是生产中最常见的确定性解码场景；此时草稿与目标的概率分布都塌缩成 one-hot，通用拒绝采样公式里的"按概率接受"退化为一次相等比较、"从修正分布重采样"退化为直接抄 target 的 argmax——draft_token_ids 来自 MTP 小模型 γ 次廉价串行前向的 argmax，target_argmax 来自主模型一次昂贵并行前向的逐位 argmax，验证就是这两个 `[B, γ]` 张量逐元素比相等。

#### 5 追问：主模型 1 次并行前向如何得到 [B, γ]

**原问题：**

> MTP 草稿模块是串行计算我能理解。请问主模型的1次并行，具体是怎么得到[B, γ]的呢？

**解答：**

这是投机解码里最关键的一个"思维转换"：**对主模型来说，验证这一步根本不算 decode，而是一次"迷你 prefill"**。理解了这一点，[B, γ] 就是自然而然的结果。

##### ① 先破一个直觉：decode 和 prefill 的区别只在"一次喂几个 token"

普通自回归 decode 是"一次前向、一个请求只喂 1 个 token、出 1 个预测"。但 Transformer 前向本身并没有"一次只能吃一个 token"的限制——**一次喂多少个位置，就能同时拿到多少个位置的预测**，这正是训练时和 prefill 时干的事（一次性吃几千个 token，每个位置都出 logits）。

验证阶段就是利用这一点：每个请求一次喂 **γ+1 个 token**（上步锚点 + γ 个草稿），主模型一次前向，**每个位置都吐出一份 logits**。所谓"1 次并行"，并行的就是这些位置（以及 batch 里的 B 个请求）。

##### ② 输入怎么组织：B 个请求展平成一条"长序列"

以 B=2、γ=3 为例。请求 A 的草稿是 [a₁, a₂, a₃]，请求 B 的草稿是 [b₁, b₂, b₃]，则这次前向的输入是 8 个 token（vLLM V1 就是按这种扁平 varlen 布局组织的）：

```
[ t_A, a₁, a₂, a₃,  t_B, b₁, b₂, b₃ ]
  └──── 请求A的4个位置 ──┘ └──── 请求B的4个位置 ──┘
   锚点   3份草稿          锚点   3份草稿
```

一次前向后，8 个位置各出一份 logits：

```
logits: [ L_A0, L_A1, L_A2, L_A3,  L_B0, L_B1, L_B2, L_B3 ]   每个 L 都是 [vocab] 维
```

##### ③ 为什么每个位置的 logits 都是"对的"：因果掩码 + teacher forcing

这是整个机制成立的核心。疑问在于：位置 `a₁` 的预测，按理应该建立在"前缀是 [历史， t_A, a₁]"之上，可这次前向里 a₂、a₃ 明明也在输入里，不会污染吗？

不会，因为**因果注意力掩码（causal mask）**：每个位置只允许 attend 到**自己及之前**的位置：

```
L_A0 由 [历史KV, t_A] 算出          → 恰好是"target 看完 t_A 后的预测"  → 用来验证 a₁
L_A1 由 [历史KV, t_A, a₁] 算出       → 恰好是"假设 a₁ 正确后的预测"      → 用来验证 a₂
L_A2 由 [历史KV, t_A, a₁, a₂] 算出   → 恰好是"假设 a₁a₂ 正确后的预测"    → 用来验证 a₃
L_A3 由 [历史KV, t_A, a₁, a₂, a₃] 算出→ 看完最后一份草稿的预测           → bonus_logits
```

关键等式：**"并行喂入 + 因果掩码"和"逐个串行喂入"在数学上完全等价**。位置 i 的 logits 只依赖前缀 ≤ i，后面的草稿它根本"看不见"。所以：

- 串行做法（假想）：先喂 t_A 得预测、再喂 a₁ 得预测、再喂 a₂…… 要 3~4 次前向；
- 并行做法（实际）：一次喂入，靠掩码把"串行的信息依赖"编码进注意力矩阵，一次前向拿到全部预测。

这就是 teacher forcing——和训练时"把标准答案当前缀、每个位置同时算损失"是同一件事。草稿 token 在这里扮演的就是"待批改的标准答案"的角色。

两个请求之间同理：varlen attention 按序列边界隔离，A 的位置永远 attend 不到 B 的 token，互不干扰，但共享同一次 kernel 调用。

##### ④ KV cache 在这中间的角色

这次前向和 prefill 一样会**顺手写 cache**：

- 8 个新位置的 K/V 在过注意力层时被算出，**追加写入各自请求的 PagedAttention KV cache**；
- 每个位置的注意力 = 读"该请求的历史 cache" + 看"本请求内排在自己前面的新 token"；
- 验证结束后，被接受的 token 的 cache 保留，**被拒绝位置之后的 cache 作废**（vLLM 直接按接受的序列长度截断，下次写入覆盖即可，cache 是 append-only 的，代价为零）。

还有一个容易忽略的点：**写 cache 的是"草稿 token"而不是"验证结果"**——这没问题，因为被接受的草稿本来就和 target 的选择一致（greedy 下逐 token 相等）；真正不同的是 recovered 位（第 k 位），它的 K/V 需要按 target_argmax[k] 修正——工程上对应位置会重新处理，细节因实现而异，但不影响主流程的理解。

##### ⑤ 输出怎么切出 [B, γ]：按位置切片 + 逐位 argmax

一次前向得到 `[B·(γ+1), vocab]` 的 logits 后，按"每请求 γ+1 个位置"的规律切片：

```
每请求的前 γ 份:   [L_A0, L_A1, L_A2] / [L_B0, L_B1, L_B2]
                   → 收集成 verification logits [B·γ, vocab]      ← 这就是第十三章的 target_logits
                   → 逐位 argmax over vocab
                   → target_argmax [B, γ]                          ← 本小节的答案

每请求的最后 1 份: [L_A3] / [L_B3]
                   → bonus_logits [B, vocab]
                   → argmax → bonus token（全接受时才用）
```

之后就是第十三章的流程：`target_argmax [B, γ]` 与 `draft_token_ids [B, γ]` 逐元素比相等，找每行首个 False。

##### ⑥ 全程张量形状一览（B=2, γ=3）

```
输入 token ids      [8]            = B·(γ+1)，扁平布局
Embedding/hidden    [8, 7168]      每个位置一份隐状态
逐层 Transformer    (读历史 KV cache + 写新 KV，层内无跨请求交互)
logits              [8, 129k]      每个位置一份词表分布
 ├─ 验证位切片      [6, 129k]      = B·γ  → argmax → target_argmax [2, 3]
 └─ bonus 位切片    [2, 129k]      = B    → argmax → bonus [2]
```

##### ⑦ 一句话总结

主模型的"1 次并行"= 把每个请求的 γ+1 个 token（锚点+草稿）像 prefill 一样**一次喂入**，靠**因果掩码**保证第 i 个位置的 logits 恰好等于"串行喂到第 i 个草稿时"的预测（teacher forcing，与逐次 decode 数学等价）；前向产出的 `[B·(γ+1), vocab]` logits 按位置切片——每请求前 γ 份 argmax 得到 `target_argmax [B, γ]` 用于批改草稿，最后 1 份留作 bonus。一次前向的开销，拿到了串行 decode 需要 γ+1 次前向才能拿到的信息——加速的全部来源就在这里。

#### 6 追问：8 份 logits 是在 decoder 上算出来的吗

**原问题：**

> 上面提到的主模型"一次前向后，8 个位置各出一份 logits"，是在decoder上计算出来的吗？

**解答：**

是的——准确说，DeepSeek-V3 这类大模型**整体就是一个 decoder-only Transformer**，"主模型"和"decoder"是同一个东西。那 8 份 logits 就是这同一个 decoder 栈一次前向算出来的。

##### ① 先厘清名词：decoder-only 模型 = 整个主模型

原始 Transformer（2017 年机器翻译那篇）是 **encoder-decoder** 两半结构；而 GPT 系、LLaMA 系、DeepSeek 系这些生成式大模型**只保留了 decoder 那一半**，所以叫 "decoder-only"。没有独立的 encoder，你看到的主模型——Embedding 层 + L 层 Transformer Block（每层 = 自注意力 + FFN/MoE）+ Output Head——**全部都是 decoder 结构**。

对照第十二章的图：Main Model 虚线框里的 "Transformer Block × L"，每一层就是一个 decoder layer。

##### ② 8 个位置在 decoder 内部是怎么流动的（概览）

关键视角：**8 个位置不是跑 8 次，而是作为激活矩阵的 8 行，一起穿过每一层**。一次前向内部：

```
输入 token ids [8]
     │
     ▼ Embedding 层（查表，每个位置独立）
hidden states [8, 7168]  ← 8 行，每行一个位置的表示
     │
     ▼ Decoder Layer 1
     │   ├─ 自注意力：8 行之间发生交互（唯一的交互点！）
     │   │    每行读"自己请求的 KV cache 历史" + "本请求内排前面的行"
     │   │    受因果掩码约束 → 第 i 行看不到第 i 行之后的内容
     │   ├─ FFN/MoE：逐行独立计算，行与行互不干扰
     │   └─ RMSNorm / 残差：逐行独立
     ▼ Decoder Layer 2 …（同样模式）
     …
     ▼ Decoder Layer 61
hidden states [8, 7168]  ← 每个位置已融合"它允许看到的全部前缀"的信息
     │
     ▼ Output Head（共享的 LM Head，一个 [7168, 129k] 线性层）
logits [8, 129k]  ← 8 个位置各出一份词表分数，就是"8 份 logits"
```

（逐层逐算子的展开见 14.7 节。）

两个要点：

1. **位置之间唯一的交互发生在注意力里**，而且被因果掩码严格限制为"只看前缀"。FFN、Norm、残差都是逐行（逐位置）独立的。所以"8 个位置一起算"和"8 个位置分开算 8 次"结果完全一样——这正是 14.5 节说的并行与串行等价，在层内部的体现。

2. **logits 不是注意力直接吐出来的**，而是最后一层 decoder 输出的隐状态再过一个 **Output Head**（也叫 LM Head / unembedding 矩阵）线性投影到词表得到的。第十二章图里 Main Model 顶部的 "Output Head" 就是它——而且注意它是和 MTP 模块**共享**的，草稿模型最后也是靠同一个 Head 出 logits。

##### ③ 一个位置的一份 logits 的"身世"

以请求 A 的第 2 个验证位（`a₁` 那行）为例，它那 129k 维 logits 的来历：

```
a₁ 的 embedding
 → 第1层：attend 到 [历史KV, t_A, a₁]，更新自己这一行
 → 第2层：同样，读前一层留下的各行表示（仍只看前缀）
 → …
 → 第61层：得到 a₁ 位置的最终隐状态 h [7168]
 → h × W_head [7168, 129k] → logits [129k]
 → argmax → target 对 a₂ 的预测
```

因为每一层都强制"只看前缀"，这份 logits 的信息来源严格等于 `[历史, t_A, a₁]`——和假设串行喂入时一模一样。其余 7 个位置同理，各自拿着各自前缀算出的隐状态，过同一个 Output Head，各得一份 logits。

##### ④ 和历史 KV cache 的关系

decode 场景下历史 token（prompt + 已生成部分）**不再过 decoder**——它们每层的 K/V 早已算好存在 cache 里。这次前向只有 8 个新位置真正穿过 61 层，每层注意力时"当前 8 行的 Q" × "cache 里的历史 K/V + 本批新算出的 K/V"。这也是为什么投机解码的验证开销远小于"重新 prefill 一遍"：序列越长，省得越多。

##### ⑤ 一句话总结

是的，8 份 logits 全部来自同一个 decoder-only 主模型的一次前向：8 个 token 的 embedding 作为激活矩阵的 8 行，一起穿过 L 层 decoder layer（行与行只在带因果掩码的自注意力里交互、且只能看前缀），每层注意力读历史 KV cache；最后一层输出的 8 行隐状态逐行过共享 Output Head 投影到词表，得到 `[8, vocab]` 的 logits——每个位置一份，互不污染。

#### 7 追问：8 个位置在 decoder 内部是怎么流动的

**原问题：**

> "8 个位置在 decoder 内部是怎么流动的", 这部分麻烦再详细解释一下。

**解答：**

下面放大到"逐层、逐算子、逐矩阵"的粒度。设定一个具体场景贯穿始终：**B=2、γ=3**，请求 A 已有 100 个历史 token（存在 KV cache 里），请求 B 已有 50 个历史 token；本步两个请求各喂 4 个新位置（1 锚点 + 3 草稿），共 8 行。

##### ⓪ 总视角：8 行 = 一张 [8, 7168] 的激活矩阵，逐层被"改写"

Decoder 的一次前向，本质是一张矩阵 `H [8, 7168]` 依次穿过 61 层，每层把它**原地更新**一次：

```
H₀ (embedding)  →  Layer1(H₀) = H₁  →  Layer2(H₁) = H₂  →  …  →  H₆₁  →  logits
```

每一行自始至终代表同一个位置（第 0 行永远是 t_A、第 1 行永远是 a₁……），层与层之间**行不会交换、不会合并**，只是每行的 7168 维向量内容被逐层丰富。记住一个核心事实：

> **在 decoder 内部，"行与行的交互"只发生在一个地方——自注意力。其余所有算子（投影、Norm、FFN、残差）都是逐行独立的。**

##### ① 入口：Embedding —— 8 行独立查表

```
token ids [8]  →  查 embedding 表 [129k, 7168]  →  H₀ [8, 7168]
```

纯粹按 token id 查 8 行向量，行与行毫无关系。加上位置信息（RoPE 在注意力里做，这里略）。

##### ② 单个 Decoder Layer 内部：五步走

以 Layer ℓ 为例，输入 `H [8, 7168]`，输出更新后的 `H' [8, 7168]`。

**Step 1：RMSNorm —— 逐行归一化**

```
H_norm[i] = RMSNorm(H[i])      i = 0..7，8 行各归各的
```

**Step 2：QKV 投影 —— 8 行独立过权重矩阵**

```
Q = H_norm × W_q        [8, 7168] × [7168, 128头×192]  →  Q [8, 128头, 192]
K = H_norm × W_k        →  K_new [8, ...]     （MLA 里是压缩后的 [8, 576]）
V = H_norm × W_v        →  V_new [8, ...]
```

注意形态：这是**矩阵乘法**，`[8, 7168] × [7168, out]`。矩阵乘的第 i 行结果只由输入的第 i 行决定——8 个位置在这里是"并行但独立"的。**GPU 喜欢这样**：8 行还是 8000 行，对 GEMM 来说都是同一次调用，这就是"一次前向吃 8 个位置"几乎不比吃 1 个慢的微观原因（decode 本来就是访存受限，多几行激活不增加权重读取量）。

**Step 3：自注意力 —— 全程唯一的"行间交互"，被两道闸门约束**

这是唯一一步"行需要看别的行"的操作。看谁的、怎么看，由两道闸门决定：

*闸门一：序列边界（varlen 隔离）。* 8 行分属两个请求，A 的 4 行永远看不到 B 的 4 行——kernel 按请求的边界分段计算，相当于两张独立的注意力：

```
请求 A:  Q_A [4, 头, 维]  ×  K/V = [A的100个历史cache + 本批4个新K/V]  →  分数矩阵 [4, 104]
请求 B:  Q_B [4, 头, 维]  ×  K/V = [B的50个历史cache  + 本批4个新K/V]  →  分数矩阵 [4, 54]
```

*闸门二：因果掩码（causal mask）。* 每个请求的分数矩阵里，新位置之间还要遵守"只能看自己及之前"。把 A 的分数矩阵 `[4, 104]` 画出来（左边 100 列是历史，右边 4 列是本批新 token）：

```
            ┌── 历史100列（cache）──┐  ┌─ 新4列 ─┐
                全部允许看            t_A   a₁   a₂   a₃
行 t_A      [  ✓ ✓ ✓ … ✓          ] [  ✓    ✗    ✗    ✗ ]
行 a₁       [  ✓ ✓ ✓ … ✓          ] [  ✓    ✓    ✗    ✗ ]
行 a₂       [  ✓ ✓ ✓ … ✓          ] [  ✓    ✓    ✓    ✗ ]
行 a₃       [  ✓ ✓ ✓ … ✓          ] [  ✓    ✓    ✓    ✓ ]
```

✗ 的位置在 softmax 前被置为 −∞（注意力权重归零）。于是：

- 第 0 行（t_A）只能聚合"历史 + 自己"的信息 → 它的输出只含 `[历史, t_A]` 的语义；
- 第 1 行（a₁）能聚合"历史 + t_A + a₁" → 它的输出只含 `[历史, t_A, a₁]` 的语义；
- 以此类推。

**这就是"并行喂入却互不污染"的物理机制**：信息上 a₂、a₃ 也在矩阵里，但掩码把流向前面行的通路全部焊死了。

每行的注意力计算本身：

```
scores[i]  = Q[i] · K_allowed^T           （只看打 ✓ 的列）
weights[i] = softmax(scores[i] / √d)
out[i]     = weights[i] · V_allowed        →  [128头] 拼接投影回 7168
```

**顺手写 cache**：本批 8 行的 K_new/V_new 追加进各自请求的 PagedAttention cache，供未来的层和未来 step 使用（验证被拒的位置后面会被截断覆盖，见 14.5）。

**Step 4：残差连接 + RMSNorm —— 逐行**

```
H_mid[i] = H[i] + AttnOut[i]        第 i 行只加自己的注意力输出
```

这里能看清"残差流"（residual stream）的概念：**每行的 7168 维向量是一条专属的信息通道**，注意力是往这条通道里"注入前缀信息"的唯一入口，其余时候各通道井水不犯河水。

**Step 5：FFN / MoE —— 逐行独立（MoE 里每行独立路由）**

```
H'[i] = H_mid[i] + MoE(RMSNorm(H_mid[i]))
```

MoE 的路由也是**逐 token（逐行）**的：第 0 行的 router 给它选 8 个专家，第 1 行可能选完全不同的 8 个——8 行可能被分发到不同专家上算，算完各自归位。行与行依然没有信息交换。

**一层结束**

```
H' [8, 7168]  →  交给 Layer ℓ+1，重复 Step 1~5，直到第 61 层
```

##### ③ 逐行"信息视野"的演化（把 61 层压缩着看）

随着层数加深，每行能"看见"的内容始终被掩码锁死在自己的前缀内，但**理解深度**在增加：

| 行 | 任何一层允许看到的内容 | 61 层后该行隐状态的含义 |
|---|---|---|
| t_A | 历史 + t_A | "看完 t_A"的深度表示 → 出 logits 验证 a₁ |
| a₁ | 历史 + t_A + a₁ | "假设 a₁ 对"的深度表示 → 出 logits 验证 a₂ |
| a₂ | 历史 + t_A + a₁ + a₂ | "假设 a₁a₂ 对"的深度表示 → 出 logits 验证 a₃ |
| a₃ | 历史 + t_A + a₁ + a₂ + a₃ | "看完最后一份草稿"的表示 → 出 bonus logits |
| （B 的 4 行同理，独立一套） | | |

注意一个微妙但关键的点：**第 1 行（a₁）虽然"看不见" a₂、a₃，但 a₂ 行能看见 a₁**——信息只能沿位置序号单向流动（历史→未来不可逆），这正是自回归的因果结构在矩阵里的形状。

##### ④ 出口：8 行 × Output Head → 8 份 logits

第 61 层出来的 `H₆₁ [8, 7168]`，逐行过共享 Output Head：

```
logits[i] = H₆₁[i] × W_head      [7168] × [7168, 129k] → [129k]     i = 0..7
```

8 行 → 8 份 logits。再按 14.5 节切片：每请求前 γ 行进 `target_argmax` 比较，最后 1 行留作 bonus。

##### ⑤ 和串行 decode 的对照（为什么结果逐 bit 一致）

假想串行版：请求 A 先喂 t_A 跑一次（1 行穿过 61 层），再喂 a₁ 跑一次……区别只在：

| | 并行（实际） | 串行（假想） |
|---|---|---|
| 每行前向时"在场"的行 | 8 行都在矩阵里 | 只有自己 1 行 |
| 但每行实际能看到的内容 | 掩码限制 = 自己的前缀 | 只有自己（= 自己的前缀） |
| GEMM 形状 | [8, 7168] × 权重 | [1, 7168] × 权重 × 跑 8 次 |
| 每行最终结果 | **完全相同** | **完全相同** |

掩码保证了"在场但看不见" ≡ "不在场"，所以两种组织方式每行的隐状态、每份 logits 严格相等——区别纯粹是工程效率：8 次小 GEMM 合并成 1 次大 GEMM，权重只从显存读一遍。

##### ⑥ 一句话总结

8 个位置在 decoder 内部是 **8 行激活，逐层并肩前进**：投影/Norm/FFN 逐行独立（纯 GEMM 行并行），唯一的信息交换发生在每层自注意力里，且被"请求边界 + 因果掩码"两道闸门限制为"只能读自己请求内、自己位置之前的内容（含历史 cache）"；61 层后每行成为"只看自己前缀"的深度表示，逐行过共享 Output Head，一次前向产出 8 份互不污染、与串行 decode 逐 bit 一致的 logits。

#### 8 追问：修正分布 norm(max(0, p_target − p_draft)) 重采样的原理

**原问题：**

> 标准投机解码为了保证输出分布与 target 严格一致（无损），拒绝一个 draft token 后不能简单拿 target 的预测顶上去，而要从修正分布里重采样：
> $$p_{\text{residual}}(x) = \text{norm}\big(\max(0,\ p_{\text{target}}(x) - p_{\text{draft}}(x))\big)$$
> 原理是什么

**解答：**

这条公式是投机解码"无损"的数学基石（Leviathan et al. 2023 / Chen et al. 2023 的 speculative sampling）。它的本质是：**草稿 token 白送了我们一个来自 q 的样本，我们要设计一套"接受/拒绝 + 补救"规则，使最终输出恰好服从 p**。

##### ① 问题设定：手里有什么，要保证什么

记：

- $q(x) = p_{\text{draft}}(x)$：草稿模型的分布（便宜，已经白给了你一个样本 $x \sim q$，即 draft token）；
- $p(x) = p_{\text{target}}(x)$：主模型的分布（贵，但这次验证前向已经算出来了）；
- **目标**：最终输出的 token 必须严格服从 $p$——不多不少、逐点相等。这样整条生成链的分布才和纯 target 自回归完全一致，加速是"无损"的。

朴素想法一："直接接受 draft token"——那输出服从 $q$，分布被草稿污染，有损。❌

朴素想法二："拒绝就采 target 的 argmax / 从 p 重新采一个"——看起来合理，但下面会证明它**多算了 p 与 q 重叠部分的质量**，最终分布 ≠ p。❌

正确构造只有一套：**按概率 $\min(1, p(x)/q(x))$ 接受；拒绝了就从 $\text{norm}(\max(0, p-q))$ 补采**。这条修正分布的形式不是设计出来的巧思，而是被"最终必须等于 p"这个约束**逼出来的唯一解**。

##### ② 推导：证明最终分布恰好是 p

一次验证的完整随机过程：

```
1. x ~ q(x)                      （draft token，已经发生）
2. 以概率 α(x) = min(1, p(x)/q(x)) 接受 x，输出 x，结束
3. 否则（概率 1-α(x)），从 p_res(x) = max(0, p(x)-q(x))/β 采样输出，结束
   其中 β = Σ_x max(0, p(x)-q(x)) 是归一化常数
```

算"最终输出为 x"的总概率，它来自两条互斥路径：

**路径 A：提出 x 且被接受**

$$P(\text{提出 } x \text{ 且接受}) = q(x)\cdot\min\Big(1, \frac{p(x)}{q(x)}\Big) = \min\big(q(x),\, p(x)\big)$$

**路径 B：发生了拒绝（无论是谁被拒），补救采样恰好采到 x**

先算拒绝的总概率：

$$P(\text{拒绝}) = \sum_x q(x)\big(1-\alpha(x)\big) = \sum_x \big(q(x) - \min(q(x),p(x))\big) = \sum_x \max\big(0,\, q(x)-p(x)\big)$$

注意到一个恒等式（因为 $\sum p = \sum q = 1$）：

$$\sum_x \max(0, q-p) = \sum_x \max(0, p-q) = \beta$$

（两边都等于 $1 - \sum_x \min(p,q)$，也就是 p 和 q 的**总变差距离**。）所以拒绝概率恰好等于修正分布的归一化常数 β——这不是巧合，是整个构造能自洽的枢纽。

于是路径 B 的概率：

$$P(\text{拒绝}) \cdot p_{\text{res}}(x) = \beta \cdot \frac{\max(0,\, p(x)-q(x))}{\beta} = \max\big(0,\, p(x)-q(x)\big)$$

**两条路径相加**：

$$P(\text{最终} = x) = \underbrace{\min(p(x), q(x))}_{\text{接受路径}} + \underbrace{\max(0,\, p(x)-q(x))}_{\text{补救路径}}$$

分两种情形验证：

- 若 $p(x) \ge q(x)$：$\min = q(x)$，$\max(0, p-q) = p(x) - q(x)$，相加 $= p(x)$ ✓
- 若 $p(x) < q(x)$：$\min = p(x)$，$\max(0, \cdot) = 0$，相加 $= p(x)$ ✓

$$\boxed{P(\text{最终} = x) = p(x) \quad \forall x}$$

逐点相等，无损证毕。

##### ③ 数值例子：3 个词的小词表跑一遍

设 $p = [0.5,\ 0.3,\ 0.2]$，$q = [0.4,\ 0.4,\ 0.2]$：

**第一步：接受概率**

| 词 | p | q | α = min(1, p/q) | 含义 |
|---|---|---|---|---|
| ① | 0.5 | 0.4 | 1.0 | q 低估了它 → 来了必收 |
| ② | 0.3 | 0.4 | 0.75 | q **高估**了它 → 只收 75%，把多给的削掉 |
| ③ | 0.2 | 0.2 | 1.0 | 恰好一致 → 必收 |

**第二步：接受路径贡献的概率** = min(p, q) = [0.4, 0.3, 0.2]，合计 0.9。

注意词②：q 以 0.4 的频率提出它，但 p 只允许它占 0.3——按 75% 接受恰好把 0.4 × 0.75 = 0.3 削到 p 的水平。**接受规则的本质是"thinning（打薄）"：q 给多了的地方，用拒绝削下去。**

**第三步：补救路径**。拒绝总概率 β = 1 − 0.9 = 0.1；修正分布：

$$\max(0, p-q) = [0.1,\ 0,\ 0] \;\xrightarrow{\text{归一化}}\; p_{\text{res}} = [1,\ 0,\ 0]$$

词①的缺口：p 要 0.5，接受路径只收到 0.4，差的 0.1 全部靠补救补上——补救采样 100% 采词①。**修正分布的本质是"补缺口"：q 给少了的地方（p − q > 0），在拒绝时补回来。**

**第四步：验账**

| 词 | 接受路径 | 补救路径 | 合计 | p |
|---|---|---|---|---|
| ① | 0.4 | 0.1 × 1.0 = 0.1 | **0.5** | 0.5 ✓ |
| ② | 0.3 | 0.1 × 0 = 0 | **0.3** | 0.3 ✓ |
| ③ | 0.2 | 0.1 × 0 = 0 | **0.2** | 0.2 ✓ |

##### ④ 直觉图像：交集 + 补集

把 p 和 q 画成两条曲线，下面的面积各为 1：

```
接受规则拿到的 = min(p, q)  →  两条曲线的【重叠区域】，面积 1−β
补救采样拿到的 = max(0, p−q)/β → p 超出 q 的【缺口区域】，面积 β，归一化后作为分布
两者相加       = min(p,q) + max(0,p−q) = p
```

- draft token 的价值：它以 1−β 的概率直接命中"重叠区"，白捡一个合法 token（q 越接近 p，β 越小，接受率越高——这就是"草稿越准加速越多"的数学表述）；
- 修正分布的角色：拒绝不是"浪费了这次前向"，而是**正好触发一次对缺口的修补**，缺口的形状被精确计算为 p 超出 q 的部分。

##### ⑤ 为什么"不能简单拿 target 的预测顶上去"

现在可以精确回答了。接受路径贡献的概率是 $\min(p, q)$（**不是 p**），所以补救路径必须恰好贡献 $p - \min(p,q) = \max(0, p-q)$ 再归一化——**修正分布的形式是被"总和必须等于 p"唯一锁死的**。

如果拒绝时改成"直接从完整的 p 采一个"（看似自然），最终分布会变成：

$$\min(p, q) + \beta \cdot p \;\neq\; p$$

在 p、q 重叠的区域多算了 β·p 的质量——输出分布被系统性地扭曲了，不再无损。如果改成"取 argmax"就更偏了（把分布压成 one-hot）。

##### ⑥ 两个补充视角

**其一，接受概率为什么是 min(1, p/q)？** 这是经典拒绝采样（rejection sampling）/ 重要性采样的标准形式：q 是提议分布，$p/q$ 是重要性权重，接受率取 min(1, ·) 是保证"接受后的经验分布不超过 p"的**最大可能接受率**——接受率越高草稿越不浪费，而超过 p(x)/q(x) 就会让 x 的输出频率突破 p(x)，破坏无损。它是"最贪心的合法选择"。

**其二，与经典拒绝采样的区别。** 教科书拒绝采样：从 q 采样、按 p/(Mq) 接受、拒绝了就把样本**扔掉重新来**。投机采样的改进在于：拒绝后不扔掉这次机会，而是**从修正分布补采一个**——拒绝事件本身就携带了"p 和 q 差多少"的信息，直接把缺口分布作为补救来源，一次都不浪费。

#### ⑦ 放回全流程，并和 14.1~14.3 的 greedy 退化接上

完整的一步投机采样：draft 连提 γ 个 token → target 并行前向得各位置的 p → **逐位置**执行上述规则：第 i 位以 $\min(1, p_i(x)/q_i(x))$ 接受 $d_i$；首次拒绝发生在第 k 位时，从 $\text{norm}(\max(0, p_k - q_k))$ 采出 recovered token，后面草稿全弃；全部接受则 bonus 位直接采 $p_{\gamma}$。由于每一位的输出都严格服从该位置的 $p_i$，整条链与纯 target 采样**同分布**。

而 greedy（第十三章、14.1~14.3 节）就是这套机制的退化：p、q 都塌缩成 one-hot 后，$\min(p,q)$ 只在"两者同一词"时非零（等价于相等比较），$\max(0, p-q)$ 在拒绝时恰等于 p 本身（等价于抄 target 的 argmax）——修正分布公式一字未改，只是代入退化分布后每项都失去了随机性。

##### ⑧ 一句话总结

原理是**概率质量的收支平衡**：接受规则 $\min(1, p/q)$ 把草稿样本"打薄"到 $\min(p,q)$（削掉 q 多给的），拒绝事件的总概率恰好等于 p、q 的总变差距离 β，此时从修正分布 $\text{norm}(\max(0,p-q))$ 补采恰好填上"p 有而 q 没给到"的缺口——$\min(p,q) + \max(0,p-q) = p$，逐点相等，所以最终输出与 target 分布严格一致；修正分布的具体形式不是技巧，而是"接受路径只贡献了 min(p,q)"这一事实下被迫成立的唯一补救项。

#### 9 追问：流程 B —— 采样投机（temperature>0）的完整数学推导

**原问题：**

> 请继续详细解释下面的句子：
>
> ```
> # 流程 B：采样投机（$temperature>0$）
> 验证阶段走 Path 2（`rejection_random_sample_*` + `sample_recovered_tokens`），
> 存在重采样逻辑：在拒绝位置从残差分布采样一个 token。
> ```
>
> ## 1. Target 前向计算
> $$
> \text{target forward} \rightarrow \text{target\_logits} \rightarrow \boldsymbol{\text{target\_probs}} = \text{softmax}(\text{target\_logits})
> $$
>
> ## 2. 草稿拒绝采样接受准则
> $$
> \text{accept } x_i \iff u_i \le \min\left(1,\;\frac{q_i(x_i)}{p_i(x_i)}\right)
> $$
>
> ## 3. 首个拒绝位置：残差补救分布
> $$
> r_i(x)=
> \begin{cases}
> 0 & x = x_i \\
> \displaystyle \frac{p_i(x)}{1-p_i(x_i)} & x \ne x_i
> \end{cases}
> $$
>
> ## 4. 逐位置执行规则
> - $\text{pos } 0,\dots,k-1$：判定接受 → 直接使用 $\text{draft}[\text{pos}]$
> - $\text{pos } k$：首个拒绝位置 → 停止复用草稿；使用 $\text{recovered}[k]$，由残差分布重采样得到 token
> - $\text{pos } k+1,\dots$：后续草稿 token 全部丢弃
>
> > 边界情况：整段草稿全部接受
> > $$
> > \text{bonus} = \mathop{\text{argmax}}(\text{bonus\_logits})
> > $$
>
> ## 5. 期望连续接受长度
> 单次投机最多预生成 $\gamma$ 个草稿 token，单位置接受概率为 $\alpha$：
> $$
> E[L]=\sum_{i=0}^{\gamma}\alpha^i
> =\frac{1-\alpha^{\gamma+1}}{1-\alpha}
> $$
>
> ## 6. 实测加速比公式
> $c$：草稿模型相对 Target 模型的推理开销系数
> $$
> \text{Speedup}_\text{real}
> =\frac{E[L]}{1+c}
> =\frac{1-\alpha^{\gamma+1}}{(1-\alpha)(1+c)}
> $$

**解答：**

本节接续第十三章（Greedy 投机解码）和 14.8 节（修正分布原理），完整拆解 **流程 B：温度 > 0 时的随机采样投机解码**。这是 vLLM 中 `rejection_random_sample_*` + `sample_recovered_tokens` 两条 kernel 实现的数学基础。

---

##### ① Target 前向计算

$$
\text{target forward} \rightarrow \text{target\_logits} \rightarrow \mathbf{\text{target\_probs}} = \text{softmax}(\text{target\_logits})
$$

验证阶段第一步：主模型（Target Model）一次并行前向，输入 $\gamma+1$ 个 token（1 个锚点 + $\gamma$ 个草稿 token），对每个位置 $i = 0, 1, \dots, \gamma$ 都产出一份 logits。对每份 logits 做带温度 $T$ 的 softmax（即 $\text{softmax}(z/T)$），得到该位置的概率分布 $p_i = \text{target\_probs}_i$。

> 这和 14.5~14.7 节讲的"一次 mini-prefill 出 $\gamma+1$ 份 logits"是同一件事——因果掩码保证了位置 $i$ 的 logits 只看前缀 $\le i$，后面的草稿 token 对它不可见。

---

##### ② 草稿拒绝采样接受准则

$$
\text{accept } x_i \iff u_i \le \min\left(1,\;\frac{q_i(x_i)}{p_i(x_i)}\right)
$$

**记号的对应关系**（与 14.8 节一致）：

| 本文记号 | 14.8 节记号 | 含义 |
|---|---|---|
| $q_i(x)$ | $p_{\text{draft},i}(x)$ | 草稿模型在位置 $i$ 的预测分布 |
| $p_i(x)$ | $p_{\text{target},i}(x)$ | 主模型在位置 $i$ 的预测分布 |
| $x_i$ | $d_i$ | 草稿模型提出的具体 token |

对每个草稿位置 $i$，独立抽取一个均匀随机数 $u_i \sim U(0,1)$。若 $u_i \le \min(1, q_i(x_i)/p_i(x_i))$，则接受该草稿 token；否则拒绝。

**为什么是 $q/p$ 而不是 $p/q$？** 14.8 节推导用的是 $\min(1, p/q)$ 形式，这里是 $\min(1, q/p)$——两者在数学上等价（通过重新定义 $u$ 的角色互相转化）。$q/p$ 形式在 vLLM 代码中更为常见：当 $q \ge p$（草稿比 target 更自信）时比值 $\ge 1$，接受率 = 1；当 $q < p$（target 比草稿更想要这个 token）时比值 < 1，需要随机拒绝来"打薄"。核心逻辑和 14.8 节完全一致。

---

##### ③ 首个拒绝位置：残差补救分布

$$
r_i(x)=
\begin{cases}
0 & x = x_i \\[6pt]
\displaystyle \frac{p_i(x)}{1-p_i(x_i)} & x \ne x_i
\end{cases}
$$

在第一个被拒绝的位置 $k$，草稿提出的 token $x_k$ 被否决了，但不能简单地取 target 的 argmax 来替代（那样会破坏无损性——14.8 节⑤已经论证过）。需要从一个"残差分布"重采样一个 recovered token。

这个残差分布 $r_k$ 的直观含义：

1. **被拒绝的 draft token 不能再选**：$r_k(x_k) = 0$。这份概率质量已被否决，归零。
2. **其余 token 按 target 的相对比例分配**：把 $p_k$ 中被 $x_k$ 占掉的那部分概率质量挖掉（$p_k(x_k)$），剩下的总量 $1-p_k(x_k)$ 作为新的分母，对每个 $x \ne x_k$ 做 renormalize。

> **与 14.8 节标准公式的关系：** 14.8 节推导的完整修正分布是 $\text{norm}(\max(0, p-q))$——它同时考虑 target 和 draft 两个分布的形状。这里的简化版"只刨掉被拒 token、不对 $q$ 其余部分做惩罚"，在计算上更轻量（对 $N$ 元词表只需一次零值覆盖 + renormalize），是某些工程实现采用的近似形式。严格无损的生产级实现仍需用 $\text{norm}(\max(0, p-q))$。

---

##### ④ 逐位置执行规则

这是整个流程 B 的**执行逻辑**，逐位置扫描 $\text{pos } 0, 1, \dots, \gamma-1$：

| 位置 | 判定 | 操作 |
|---|---|---|
| $\text{pos } 0,\dots,k-1$ | $u_i \le \min(1, q_i/p_i)$，**接受** | 直接使用 $\text{draft}[\text{pos}]$ |
| $\text{pos } k$ | **首次拒绝** | 停止复用草稿；从残差分布 $r_k$ 重采样得 $\text{recovered}[k]$ |
| $\text{pos } k+1,\dots,\gamma-1$ | — | **全部丢弃**，不产生任何 token |

**关键理解：rejection 是"一次性"的。** 一旦在第 $k$ 位发生了拒绝，$k$ 之后所有的草稿 token 就都作废了。这是因为位置 $k$ 的 token 变了（从 $x_k$ 变成了 $\text{recovered}[k]$），后续所有草稿 token 都是条件在"$x_k$ 没变"这个假设上生成的——前缀变了，后续自然失效。这和第十三章的 greedy 路径逻辑一模一样（"找到第一个分歧位就截断"），只是判定分歧的方式从"argmax 相等比较"变成了"随机接受判定"。

**边界情况：全部接受 → Bonus Token**

$$
\text{bonus} = \mathop{\text{argmax}}(\text{bonus\_logits})
$$

如果 $\gamma$ 个草稿 token 全部被接受，target 前向还多产出了一份 **bonus_logits**——即位置 $\gamma$ 的 logits（"看完最后一份草稿后的预测"）。此时直接取 bonus_logits 的 argmax 作为额外奖励 token，连同 $\gamma$ 个草稿 token，这一步共产出 $\gamma+1$ 个 token。

> **注意：** 在全部接受的场景下，没有 rejection 发生，也就没有"从残差分布重采样"这一步。bonus token 直接取 target 的 argmax，这继承的是第十三章 bonus 位规则（"不管什么温度，bonus 位都走 greedy"——因为 bonus_logits 所在的位置没有对应的 draft token 可以比较，自然也没有拒绝/接受的二分）。

---

##### ⑤ 期望连续接受长度

$$
E[L]=\sum_{i=0}^{\gamma}\alpha^i
=\frac{1-\alpha^{\gamma+1}}{1-\alpha}
$$

**符号定义：**
- $\gamma$：单次投机的草稿长度上限（DeepSeek-V3 中 $\gamma=1$，只保留 MTP Module 1）
- $\alpha$：**单位置接受概率**，即一个草稿 token 通过接受判定的期望概率，$\alpha = \mathbb{E}[\min(1, q/p)]$
- $L$：单次投机步骤实际产出的有效 token 数

**推导：** 考虑投机过程——从位置 0 开始扫描：

| 场景 | 概率 | 产出 token 数 |
|---|---|---|
| 第 0 位就拒绝 | $1-\alpha$ | $1$（只有 recovered[0]） |
| 第 0 位接受，第 1 位拒绝 | $\alpha(1-\alpha)$ | $2$（draft[0] + recovered[1]） |
| 前 2 位接受，第 3 位拒绝 | $\alpha^2(1-\alpha)$ | $3$ |
| ... | ... | ... |
| 前 $i$ 位接受，第 $i+1$ 位拒绝 | $\alpha^{i}(1-\alpha)$，$i < \gamma$ | $i+1$ |
| 全部 $\gamma$ 位都接受 | $\alpha^{\gamma}$ | $\gamma+1$（含 bonus） |

$$
\begin{aligned}
E[L] &= (1-\alpha) \cdot 1 + \alpha(1-\alpha) \cdot 2 + \alpha^2(1-\alpha) \cdot 3 + \cdots + \alpha^{\gamma-1}(1-\alpha) \cdot \gamma + \alpha^{\gamma} \cdot (\gamma+1) \\[4pt]
&= 1 + \alpha + \alpha^2 + \cdots + \alpha^{\gamma}
= \sum_{i=0}^{\gamma} \alpha^i
= \frac{1-\alpha^{\gamma+1}}{1-\alpha}
\end{aligned}
$$

**验证边界：**
- 若 $\alpha \to 1$（草稿与 target 完全一致，greedy 模式），$E[L] = \gamma+1$——每次都全中 + bonus，收益拉满
- 若 $\alpha \to 0$（草稿全错），$E[L] = 1$——每次第一位就拒，等于没加速，白算了草稿
- 若 $\alpha = 0.85$、$\gamma = 3$：$E[L] = \frac{1-0.85^4}{1-0.85} = \frac{1-0.522}{0.15} = 3.19$，平均每次产出 ~3.2 个 token

---

##### ⑥ 实测加速比公式

$$
\text{Speedup}_\text{real}
= \frac{E[L]}{1+c}
= \frac{1-\alpha^{\gamma+1}}{(1-\alpha)(1+c)}
$$

**符号定义：**
- $c$：草稿模型相对 Target 模型的**推理开销系数**，$c = \dfrac{\text{草稿单 token 推理时间}}{\text{Target 单 token 推理时间}}$

**推导逻辑：** 纯 target 自回归时，每步前向产出一个 token，开销 = $1$（归一化单位）。投机解码时，一步的开销包括：

- **Target 一次并行前向**：验证 $\gamma+1$ 个位置（一次 mini-prefill），开销 ≈ $1$
- **草稿模型 $\gamma$ 次串行前向**：每次开销 = $c$，但草稿生成可与 target 验证流水线并行。在理想流水线下，关键路径上的额外开销量级约为 $c$（而非 $\gamma c$）

因此一步投机总开销 ≈ $1 + c$，产出 = $E[L]$ 个 token：

$$
\text{Speedup}_\text{real} = \frac{\text{产出}}{\text{开销}} = \frac{E[L]}{1+c}
= \frac{1-\alpha^{\gamma+1}}{(1-\alpha)(1+c)}
$$

**数值例子（DeepSeek-V3）：** $\alpha \approx 0.85$，$\gamma = 1$，$c \approx 0.1$（MTP 模块仅 1 层 Transformer）：

$$
E[L] = \frac{1-0.85^2}{1-0.85} = \frac{0.2775}{0.15} = 1.85
$$

$$
\text{Speedup}_\text{real} = \frac{1.85}{1.1} \approx 1.68\times
$$

和 DeepSeek-V3 论文报告的 ~1.8× 吞吐提升基本吻合。

**$\gamma$ 不是越大越好。** 从 $E[L]$ 公式看，增大 $\gamma$ 能线性提升期望产出（上限为 $\frac{1}{1-\alpha}$）。但 $c$ 也随 $\gamma$ 增大（草稿要自回归生成更多 token），且草稿越长越容易"跑偏"（$\alpha$ 本身会下降）。DeepSeek-V3 的实践结论是 $\gamma=1$ 就够（MTP Module 2 直接被丢弃）——第一个额外 token 的接受率已达 85–90%，再加的边际收益太低。

---

##### ⑦ Flow A 与 Flow B 的核心区别

| | Flow A（greedy，第十三章） | Flow B（sampling，本节） |
|---|---|---|
| 判定方式 | `draft[i] == target_argmax[i]` | $u_i \le \min(1, q_i(x_i)/p_i(x_i))$ |
| 恢复方式 | 直接抄 target 的 argmax | 从残差分布重采样 |
| 需要随机数 | 否 | 是（每个位置一个 $u_i$，拒绝位额外一次采样） |
| 性能 | 更快（无随机数、无分布运算） | 稍慢（需算接受率 + 残差采样） |
| 适用条件 | `all_greedy = True` | 任意 $T > 0$ |
| vLLM kernel | `rejection_greedy_sample_*` | `rejection_random_sample_*` + `sample_recovered_tokens` |

---

##### ⑧ 完整流程图

```
┌─────────────────────────────────────────────────────────┐
│  输入: 锚点 token + draft[0..γ-1]（草稿模型 γ 次串行前向）  │
└────────────────────┬────────────────────────────────────┘
                     ▼
┌─────────────────────────────────────────────────────────┐
│  Target 并行前向 → p₀, p₁, ..., p_γ（每位置一份分布）      │
└────────────────────┬────────────────────────────────────┘
                     ▼
       逐位置 i = 0, 1, ..., γ-1:
       ┌──────────────────────────────┐
       │  uᵢ ~ U(0,1)                  │
       │  uᵢ ≤ min(1, qᵢ(xᵢ)/pᵢ(xᵢ))? │
       └──────┬───────────┬───────────┘
              │YES        │NO (首次拒绝发生在位置 k)
              ▼           ▼
        draft[i]    ┌────────────────────────────────┐
        直接使用     │  从 rₖ 重采样 → recovered[k]    │
        继续 i+1    │  丢弃 draft[k+1..γ-1]          │
                    │  产出: draft[0..k-1] +          │
                    │        recovered[k]             │
                    │  共 k+1 个 token                │
                    └────────────────────────────────┘

       全部接受 (i 走到 γ):
       ┌────────────────────────────────┐
       │  bonus = argmax(bonus_logits)  │
       │  产出: draft[0..γ-1] + bonus   │
       │  共 γ+1 个 token               │
       └────────────────────────────────┘
```

---

##### ⑨ 一句话总结

流程 B 是流程 A（greedy 投机解码）在随机采样温度下的泛化：接受判定从"相等比较"推广为"按 $\min(1, q/p)$ 随机接受"，恢复机制从"抄 argmax"推广为"从残差分布重采样"——每一步都保留了温度 > 0 时的概率随机性，使得 $E[L] = \frac{1-\alpha^{\gamma+1}}{1-\alpha}$ 成为只依赖于接受率 $\alpha$ 和草稿长度 $\gamma$ 的几何级数，实际加速比为 $\frac{E[L]}{1+c}$，草稿越准（$\alpha$ 越大）则收益越接近理论上限 $\gamma+1$。

---

## 陆：IndexCache跨层复用top-k索引加速稀疏注意力

**原问题：**

> 请详细解释一下下面有关 Index Cache 技术点的说明：
>
> **为什么需要**：基于智谱的研究 Paper（IndexCache: Accelerating Sparse Attention via Cross-Layer Index Reuse），DSA（如 DeepSeek-V3.2、GLM-5 的 DSA 注意力）的每一层都会独立运行 Lightning Indexer 计算 top-k 索引（k=2048），约 47 层逐层重算 top-k 是可观开销。Z.ai 实测发现相邻层 top-k 索引重叠度高达 70%~100%，逐层重算大量冗余。
>
> **部署配置（本场景）**：1/2 复用率，指定最优复用 Pattern：`--hf-overrides '{"use_index_cache": true, "index_topk_pattern": "FFSFSSSFSSFFFSSSFFFSFSSSSSSFFSFFSFFSSFFFFFFSFFFFFSFFSSSSSSFSFFFSFSSSFSFFSFFSSS"}'`
>
> **实证**：对每一对层 (i, j)，计算 |T(i)∩T(j)| / k（k=2048），在 768 个 200K 长度样本的 calibration set 上平均（Figure 4）。
>
> **机制**：将已算出的 top-k 索引缓存并跨层复用，跳过大部分层的重复计算。`index_topk_freq`（int，默认 1）：每多少层重算一次 top-k，调大则在更多层间复用缓存索引；`index_topk_pattern`：逐层"重算/复用"模式串，在精度与速度间寻优。
>
> **效果**：DSA 模型约 1.2~1.8× 加速；与 DSA-CP 互补（CP 消除跨卡冗余，Index Cache 消除跨层冗余 top-k）。说明：支持均匀复用（不指定 pattern）和指定 Pattern 复用两种模式，本场景采用 1/2 复用 + 原论文最优 pattern 对精度影响最小。

**解答：**

### 1 为什么需要：DSA 的 Lightning Indexer 是长上下文瓶颈

DSA（DeepSeek Sparse Attention，稀疏注意力）由 DeepSeek-V3.2-Exp 提出，GLM-5 沿用，核心思路是「**先选后算**」：每个 query 不必关注全部历史 token，只关注最相关的 top-k 个。这个"选"的动作由 **Lightning Indexer（闪电索引器）** 完成：

- 它是个**每层独立运行**的轻量预打分组件，用 **ReLU**（而非 softmax）给 query 与所有历史 token 打分，选出 top-k（k=2048）个位置作为该层稀疏注意力的 mask；
- 注意力复杂度从 **O(L²) 降到 O(L·k)**（128K 上下文、k=2048 时约降 98%）。

**问题在于**：indexer 本身仍是 O(L²)，长上下文下反而成了新瓶颈——200K 上下文时约占 **prefill 时间 81%**；且它**每层独立重算**（说明里"约 47 层"是说明性数字，实际 GLM-5 为 78 层），冗余极大。这就是 IndexCache 的出发点。

### 2 核心观察：相邻层 top-k 索引高度重叠

Z.ai 实测发现**相邻层的 top-k 索引重叠度高达 70%~100%**。原因直观——top-k 选中的往往是"全局重要"的 token（注意力汇聚点、指令/特殊 token、强信号位置等），这些在深层相对稳定。所以"每层都重新选一遍 top-k"里有大量重复劳动。

### 3 IndexCache 机制：跨层缓存并复用 top-k 索引（F/S pattern）

IndexCache 把各层分成两类，用逐层 **F/S pattern 串**控制：

| 符号 | 含义 | 行为 |
|---|---|---|
| **F**（Full） | 重算 | 运行本层自己的 indexer，算 top-k 并**写缓存** |
| **S**（Shared/Skip） | 复用 | **跳过 indexer**，复用最近一个上游 F 层的缓存 top-k 索引 |

三个关键点（决定它精度损失小）：

1. **缓存的是"索引选择"，不是注意力输出**。S 层只复用"选哪些位置"，仍要拿这些索引去做真实稀疏注意力；因选择在相邻层高度一致，用上游层的选择代替本层，结果几乎不变。
2. **实现极简**：官方描述"一个 `if/else` 分支，零额外显存"，索引只在本 step 内临时存在 MLA KV pool。
3. **收益上限**：论文移除最多 75% 的 indexer 计算（只保留 1/4 层重算），仍几乎无质量损失。

### 4 实证验证：pairwise overlap 与 Figure 4

验证方法：对每一对层 (i, j)，计算 |T(i) ∩ T(j)| / k（k=2048），在 768 个 200K 长度样本的 calibration set 上平均，得到一个"层数 × 层数"的**重叠矩阵**。**Figure 4** 把它画成热力图（30B DSA 模型），并标出贪心搜索得到的共享块——热力图越亮，说明这两层选的 top-k 越像、越值得跨层复用。它是"哪些层可安全复用"的实证依据。

### 5 部署配置逐字段解读

```
--hf-overrides '{"use_index_cache": true, "index_topk_pattern": "FFSFSSS..."}'
```

- `use_index_cache: true`：开启 IndexCache 开关。
- `index_topk_pattern`：78 个字符，**一一对应 78 个 transformer 层**，`F`=重算、`S`=复用。串开头 `FFS...` 是因为**第 0 层必须 F**（上游无索引可复用），后续 F 位置由贪心搜索决定（挑"复用后精度损失最小"的层保留重算）。该串 S 约占一半，即**复用率 ≈ 1/2**，对应注释里的"1/2 复用率"。

### 6 两种复用模式与参数

| 模式 | 参数 | 说明 |
|---|---|---|
| **均匀复用**（不指定 pattern） | `index_topk_freq`（int，默认 1） | 每隔多少层重算一次。`1`=每层重算（无收益）；`2`=隔层重算（约 1/2 复用）；`4`=每 4 层重算（只留 1/4 indexer，论文的激进配置）。调大它 → 更多层复用、更快，但精度风险上升 |
| **指定 Pattern 复用** | `index_topk_pattern` | 逐层显式 F/S 串，非均匀、在精度与速度间寻优 |

pattern 的产出方式，论文给了两种：**训练无关的贪心搜索**（在 calibration set 上逐个移除"最不敏感"的 indexer，最小化 LM loss）与**训练感知的多层蒸馏**（让保留的 indexer 对齐它服务的所有层的平均注意力分布）。

### 7 效果与 DSA-CP 互补

- **加速**：DSA 模型约 **1.2~1.8×**（论文：200K 下 prefill 最高 1.82×、decode 1.48×，端到端 ~1.2×，在 GLM-5 744B 上验证）。
- **互补**：两者消除的是不同维度的冗余——**DSA-CP** 消除**跨卡（GPU）**冗余，**IndexCache** 消除**跨层**冗余 top-k，正交、可叠加使用。

### 8 说明解读：1/2 复用 vs 原论文 1/4 复用

论文主打是激进的 **1/4 复用**（只保留 1/4 indexer）；本部署为**优先保精度**，退回更保守的 **1/2 复用**（保留一半 indexer），并采用论文贪心搜索得到的、同等预算下精度损失最小的 pattern 结构——即"速度少拿一点、精度多保一点"的折中。

> 参考：[IndexCache (THUDM)](https://github.com/THUDM/IndexCache)、[论文页](https://huggingface.co/papers/2603.12201)、[ar5iv 全文](https://ar5iv.labs.arxiv.org/html/2603.12201)。

---

## 柒：图模式静态图编译减少decode调度与kernel启动开销

**原问题：**

> 请详细解释一下下面的话：
> 为什么需要图模式
> 动态图每步都存在算子调度 /kernel 启动开销，且 Python 侧调度易成瓶颈。将稳定的 decode 路径编译为静态图，可减少调度开销、提升执行效率。

**解答：**

核心一句话：**动态图把"每一步"都实时走一遍 Python→dispatch→launch 的流程，decode 时单步计算极小，这个流程的开销反而压过了真正的计算；把它编译成静态图，就是把这套流程从"每步现算"变成"一次录制、反复重放"。**

### 1 背景：动态图 vs 静态图

PyTorch 默认跑在 **eager（动态图 / define-by-run）** 模式：写一行 `x = x @ W`，Python 解释器**立刻**执行——先经过算子调度、再往 GPU 提交一个 kernel，马上出结果。好处是灵活、好调试；代价是每次执行都要付"解释"这一层的开销。

**静态图（图模式）** 相反：先把整条计算路径"录制"成一个图（`torch.compile` 的 FX 图、`CUDA Graph`、或框架手写的 fused kernel），之后每次执行就是**重放这张图**，不再逐算子解释。

### 2 三个开销逐一拆解

原文点名的三个开销，是三个不同层次、层层叠加的成本。

#### 2.1 算子调度开销（operator dispatch overhead）

写一个 `add`，PyTorch 内部要做的远不止"加法"：查这是哪个算子 → 检查输入 dtype/device/layout → 决定走 CPU 还是 CUDA kernel → 找到对应实现 → 再真正调用。这套 **dispatch（分派）** 逻辑每条语句都要走一遍。

- 单个算子这笔开销是微秒级（~5–30µs），对大 GEMM 可忽略；
- 但一个 decode step 有**几千个小算子**，微秒级 × 几千，就变成毫秒级纯开销。

#### 2.2 kernel 启动开销（kernel launch overhead）

即使分派已确定"该跑哪个 CUDA kernel"，把它**提交给 GPU** 也有固定成本：CPU 把 kernel 参数、网格配置写进 GPU 命令队列，GPU 再取出来执行。这个 launch 动作一次约几个微秒，**和 kernel 算多大无关**。

- 一个 1ms 的大 GEMM，付 5µs 启动费无所谓；
- 一个只花 2µs 的小 kernel，启动费却要 5µs——**启动比算还贵**。

#### 2.3 Python 侧调度易成瓶颈（Python-side dispatch）

前两者合起来的更严重后果：**GPU 在等 CPU**。Python 解释器一次只能串行地"解释一条语句 → 提交一个 kernel"，速度有限。当 GPU 上每个 kernel 都很小、很快算完时，GPU 大部分时间**空转等 CPU 喂下一个 kernel**——这就是常说的 **kernel launch bound / GPU starvation**。此时瓶颈不在 GPU 算力，而在 Python 这条"喂料流水线"上。

### 3 为什么 decode 路径尤其痛

三笔开销在 **prefill** 和 **decode** 下严重程度完全不同：

| | prefill（预填充） | decode（逐 token 解码） |
|---|---|---|
| 每步 token 数 | 几百~几千 | **1 个**（或 batch 里几个） |
| 单算子规模 | 大矩阵乘 `[seq, hidden]×W` | 小矩阵向量乘 `[1, hidden]×W` |
| GPU 每步计算量 | 大，ms 级 | **极小，亚 ms 级** |
| 每步算子数量 | 同样几千个 | 同样几千个 |
| 开销/计算比 | 被大计算摊薄，可忽略 | **开销 >> 计算，成为主瓶颈** |

关键点：decode 的算子**数量没变少**（还是几十层 × 每层几十个算子），但每个算子从"大 GEMM"退化成"矩阵×向量"——"调度+启动"固定成本不变，真正计算却缩水几个数量级，开销占比被无限放大。结果就是：decode 一卡一卡的慢，慢的不是 GPU，是 CPU 侧的 Python 调度和 kernel 启动。

### 4 静态图怎么解决

把稳定的 decode 路径编译成静态图，本质是**把"每步现算"改成"一次录制、反复重放"**，从两个层面消掉开销：

#### 4.1 CUDA Graph（消 kernel 启动开销）

NVIDIA 提供的机制：先把一段 kernel 启动序列**录制（capture）**下来存成一张图，之后每次只需**一次 `cudaGraphLaunch`** 就把整张图的所有 kernel 一次性重放。

- 原来：几千个 kernel → 几千次 CPU 提交、几千次启动费；
- 现在：几千个 kernel → **一次** CPU 提交、GPU 连续执行。

kernel 启动开销被从 O(算子数) 压到 O(1)。vLLM 的 decode 就是靠 CUDA Graph 吃到这个收益。

#### 4.2 算子融合 / torch.compile（消调度开销、减算子数）

把图上**相邻的小算子融合成一个大 kernel**（例如 `x @ W + bias` 后紧跟的 RMSNorm、残差、量化都熔进一个 kernel）：

- 算子数量大幅减少（几百上千 → 几十）；
- dispatch 次数随之减少；
- 中间结果不再反复写回/读出显存（省显存带宽）。

`torch.compile` 的 Inductor 后端做的正是这个：把 eager 代码 trace 成 FX 图 → 融合 → 生成更少的 kernel。

**两者的关系是互补的**：`torch.compile` 负责"把图变瘦"（减算子数），CUDA Graph 负责"把剩下的启动费归零"（减启动次数）。

### 5 为什么前提是"稳定"（stable）

静态图有一个硬约束：**录制下来的形状、控制流要基本固定**，因为图里写死了每个 kernel 的网格和内存地址。这正是原文强调"稳定的 decode 路径"的原因：

- **decode 天然稳定**：每步 batch 固定、新 token 恒为 1、hidden 维度不变、层序完全确定——形状和控制流都确定，最适合静态化。所以几乎所有推理框架都优先把 decode 编译成图。
- **prefill 形状可变**（prompt 长度每次不同），没法简单录一张图，通常要靠动态 shape 支持或多张预编译图来覆盖，收益和复杂度都比 decode 更高。

### 6 一句话总结

动态图下每一步都要"Python 解释 → 算子分派 → kernel 启动"三连，decode 单步计算小、算子却一个不少，这三笔固定开销反而成了主瓶颈（GPU 空转等 CPU）；把稳定的 decode 路径编译成静态图，就是用 `torch.compile` 融合减算子、用 CUDA Graph 重放免启动，把"每步现算的调度开销"降成"一次录制、反复重放"，从而提升执行效率。

---

### 追问：入图与捕图 —— NPU 图模式的落地细节

**原问题：**

> 请继续详细解释下面的话：
> 入图 = 把模型的算子序列一次性录制进 NPUGraph, 运行时整图 replay; 优化在于把每步上千次的「host 逐算子下发」压成「一次下发 + 一次重放」, 消除 CPU→NPU 的 launch 开销，让 NPU 不再空等 CPU
> 捕图：提前选好一组固定的桶大小，针对每个桶大小各捕获一张图。然后运行时把实际的 num_tokens 向上取整到最近的桶大小，多出来的部分用 padding 填充。

**解答：**

这两段话是**同一件事（NPU 图模式）的两个层次**：第一段讲"入图"这个动作本身及其收益来源，第二段讲"捕图"这个让图模式落地到可变 batch 场景的具体手段。它是上面 1~6 节所述"图模式"思想在昇腾 NPU 上的具体实现（对标 CUDA Graph）。

#### 一、入图：一次录制，反复重放

**先厘清两个词：host 和"下发"。** NPU/GPU 编程是 host-device 模型：host = CPU（主控方），device = NPU（算力方）；"下发（dispatch/issue）" = CPU 把一个算子的执行命令（kernel 及参数）从 host 提交给 NPU。不入图时，模型前向的每个算子都要走一次完整下发：CPU 侧 Python 先解释分派 → 调运行时 API → 把命令写进 NPU 命令队列 → NPU 取出执行。**一个算子一次下发，一个 decode step 上千个算子就是上千次下发。**

**入图在做什么：把"逐算子下发"变成"录制一张图"。** NPUGraph 是昇腾 NPU 上对标 CUDA Graph 的机制，核心操作叫 capture（捕获/录制）：

1. 进入 capture 模式后，模型完整跑一遍前向；
2. 每个算子这次不立刻下发执行，而是被运行时**记录进一张图**——一个存着所有 kernel 及其参数、依赖关系、内存地址的 DAG；
3. 录制结束得到一张图对象。

之后每次推理不再逐算子下发，而是**一次调用把整张图 replay**，NPU 按录好的顺序把上千个 kernel 一口气连续执行完。

**收益在哪：把 O(上千次) 下发压成 O(1)。**

| 环节 | 入图前 | 入图后 |
|---|---|---|
| Python 解释/分派 | 每算子一次，上千次 | 录制一次，运行时零 |
| host→device 下发（launch） | 每算子一次，上千次 | 一次（整图） |
| NPU 执行 | 边等 CPU 边算，频繁断流 | 连续执行，中间不停 |

关键在最后一行：kernel launch 的固定成本被从"与算子数成正比"压到"常数级"。原来 NPU 每算完一个小 kernel 都要停下来等 CPU 下发下一个（即 launch bound / NPU 空转）；入图后所有 kernel 已在图里串好，NPU 一次拿到全部命令、连续执行、不再空等 CPU。

#### 二、捕图：用"桶"来应对可变的 num_tokens

**为什么需要桶：图要求形状固定。** 入图有一个硬前提——录进图里的张量形状、内存地址是写死的。但实际 `num_tokens`（batch 里所有请求的新 token 总数）是动态的，不可能为每个可能值都录一张图（可能性无穷、且每张图占显存）。所以需要离散化方案，即"桶（bucket）"。

**桶机制三步：**

1. 提前选好一组固定桶大小，如 `[1, 2, 4, 8, 16, 32, 64, 128]`（工程里常用更密集合）；
2. 针对每个桶各捕一张图，每张图只能处理恰好那么多 token；
3. 运行时把实际 `num_tokens` **向上取整**到最近桶，多出来的位置用 padding 补齐。

**具体例子**（桶 = 8，实际 num_tokens = 7）：

```
实际: [t₁ t₂ t₃ t₄ t₅ t₆ t₇]          7 个真实 token
取整: 向上取到桶 8 → 补 1 个 padding
输入: [t₁ t₂ t₃ t₄ t₅ t₆ t₇ <pad>]    8 个位置，跑"8-token 那张图"
```

**padding 如何做到无副作用：** `<pad>` 是假 token，必须保证既不改变真实 token 结果、也不产生脏输出，靠三层：

1. **注意力掩码**：`<pad>` 位置 softmax 前置 `-inf`，权重归零，真实 token attend 不到它、它也不 attend 真实 token；
2. **输出丢弃**：跑完图后 `<pad>` 那几行 logits 直接扔掉，只取前 7 行；
3. **KV cache 隔离**：`<pad>` 不写进真实 KV cache（或写临时块），不污染后续 step。

于是"8 位置跑 8-token 图"对外表现和"7 个真实 token"完全一致——padding 只是让形状对上图，语义透明。

**桶的选择是"显存 vs 浪费"的权衡：**

| 桶的密度 | 好处 | 代价 |
|---|---|---|
| 密（步长小） | padding 少、算力浪费小 | 图数量多、显存大 |
| 疏（步长大） | 图少、显存省 | 取整跨度大、padding 浪费大 |

极值例子：桶 `[1,2,4,8,16,32,64,128]` 下实际 `num_tokens=65` 被取整到 128，近一半算力花在 padding 上。所以工程上常**小尺寸处取密、大尺寸处取疏**（小 batch 相对浪费敏感，大 batch 相对误差被摊薄），vLLM 用的就是一套非均匀桶序列。

**为什么只用于 decode：** decode 每请求恰好 1 个新 token，`num_tokens` 只随并发请求数变化，离散可控、适合桶；prefill 序列长度连续可变、每请求都不同，无法用有限桶覆盖，故 prefill 一般不走固定形状捕图。

**一句话总结：** 入图解决"下发太贵"——上千次 host 逐算子下发录成一张 NPUGraph、运行时整图 replay，CPU→NPU launch 开销从 O(算子数) 降到 O(1)；捕图解决"形状可变"——图要求固定形状，于是预选一组桶、每桶捕一张图，运行时把真实 num_tokens 向上取整到最近桶、用 padding 补齐，靠"掩码 + 丢输出 + 隔离 KV cache"让 padding 语义透明，从而让固定形状图模式也能服务动态 batch。

---

### 追问：捕图 size —— 桶大小具体怎么定

**原问题：**

> 请继续详细解释 捕图 size：针对此场景（max-num-seqs=8,mtp=3）是 [4, 8, 16, 24, 32]
> max 默认是 min (max_num_seqs * decode_query_len, 512)
> 小批次间隔密（步长 8, <=256），大批次间隔稀（步长 16, 大于 > 256）
> 可以通过 "cudagraph_capture_sizes" 自行配置

**解答：**

这一节讲的是"这组桶具体怎么定"——针对 `max-num-seqs=8、mtp=3` 的场景，答案是 `[4, 8, 16, 24, 32]`。

#### 1 先算清两个前提参数

**decode_query_len（每请求一个 decode step 的 query 数）**：开了 MTP/投机解码后，每个请求一步处理的不是 1 个 token，而是「1 个锚点 + γ 个 draft」。mtp=3 即 γ=3，所以：

```
decode_query_len = 1 + 3 = 4
```

**num_tokens 的构成**：一个 decode step 的总 token 数 = 活跃请求数 × decode_query_len = `N × 4`。每个请求固定贡献 4 个 query token，所以 **num_tokens 永远是 4 的整数倍**——这是后面所有推断的根。

#### 2 max 为什么是 32

```
max 默认 = min(max_num_seqs × decode_query_len, 512)
```

代入：`max_num_seqs=8`、`decode_query_len=4` → `8 × 4 = 32`；`min(32, 512) = 32`。

- `8 × 4 = 32` 是**本场景最大可能的 num_tokens**（8 个请求全满、每请求 4 token）；
- `512` 是默认桶序列的**上限封顶**——防止 batch 配得很大时去捕几百张超大的图。

#### 3 桶列表为什么是 [4, 8, 16, 24, 32]

三个特征：

1. **都是 4 的倍数**——num_tokens 永远是 4 的倍数，桶只有取 4 的倍数才不会"每次必 pad"。`1、2` 这种桶永远用不上（最小就是 4），所以被去掉。
2. **最小 4、最大 32**——对应 1 个请求（4 token）和 8 个请求（32 token），正好覆盖本场景 num_tokens 的全部取值区间。
3. **步长 8（8→16→24→32）**——默认"小批次密集、步长 8"规则的体现。

即：`[4, 8, 16, 24, 32]` = `[最小值 4]` + `[按步长 8 递增到 max 32]`。

#### 4 默认生成规则：小批次密、大批次稀

不手动配置时，框架按一条默认规则生成桶序列（vLLM 的 cudagraph 桶序列）：

```
[1, 2, 4]                      ← 极小尺寸，2 的幂
8, 16, 24, ..., 256            ← 步长 8，直到 256（密）
272, 288, ..., 512             ← 步长 16，到 512（稀）
```

即 **≤256 用步长 8（密），>256 用步长 16（稀）**。

为什么"小密大稀"？回到"显存 vs 浪费"权衡：

| | 小批次 | 大批次 |
|---|---|---|
| 相对浪费敏感度 | 高（pad 1 个占比很大） | 低（pad 几个被大 batch 摊薄） |
| 所以步长取 | 小（8），padding 浪费可控 | 大（16），省显存 |

极端例子：`num_tokens=3` 若桶是 `[1,2,4]`，取整到 4，浪费 33%；`num_tokens=300` 若桶是 `[...,288,304]`，取整到 304，浪费仅 1.3%。**小 batch 的相对误差大得多，所以小处要密。**

#### 5 cudagraph_capture_sizes 自行配置

默认规则是"一刀切"的通用解，`cudagraph_capture_sizes` 让你**显式指定桶列表**、覆盖默认生成。本场景 `[4, 8, 16, 24, 32]` 就是把默认规则"过滤+裁剪"到本场景的结果（只取 ≤32、≥4、是 4 倍数的部分）。

也可按需调：

- **想减 padding、显存宽裕**：配 `[4, 8, 12, 16, 20, 24, 28, 32]`（步长 4，覆盖所有请求数）；
- **显存紧张、接受多 pad**：配 `[4, 8, 16, 32]`（桶更少，奇数请求数浪费更大）。

#### 6 本场景的 padding 浪费实例

桶 `[4, 8, 16, 24, 32]` 下，按活跃请求数 N 列出：

| 请求数 N | num_tokens = 4N | 落到哪个桶 | padding | 浪费 |
|---|---|---|---|---|
| 1 | 4 | 4 | 0 | 0% |
| 2 | 8 | 8 | 0 | 0% |
| 3 | 12 | 16 | 4 | 25% |
| 4 | 16 | 16 | 0 | 0% |
| 5 | 20 | 24 | 4 | 17% |
| 6 | 24 | 24 | 0 | 0% |
| 7 | 28 | 32 | 4 | 12.5% |
| 8 | 32 | 32 | 0 | 0% |

**偶数请求数零浪费，奇数请求数（3、5、7）有 padding**——这正是"步长 8 不覆盖 12/20/28"换来的省显存代价。若换成步长 4 的桶，3/5/7 请求也能零浪费，但要多为 12、20、28 各多捕一张图。

**一句话总结：** `[4, 8, 16, 24, 32]` 是三个约束共同作用的必然结果：① mtp=3 使 decode_query_len=4、num_tokens 恒为 4 的倍数 → 桶必须取 4 的倍数；② max = min(8×4, 512) = 32 → 上限封在 32；③ 默认"≤256 步长 8、>256 步长 16"的密集规则 → 内部按步长 8 递增。三约束叠加，落在 `[4, 8, 16, 24, 32]` 这五个桶上。

---

### 追问：为什么不用 [0,1,2,...,32] 全捕？——"零浪费"其实不省显存

**原问题：**

> 我为什么要 [4, 8, 16, 24, 32] 呢，直接 [0,1,2,3,4...30,31,32] 不就得了吗？空间 0 浪费。

**解答：**

关键有两个：**① num_tokens 根本到不了 0,1,2,3 和 5,6,7…；② "零 padding"和"零显存"是两回事，而你要省的是显存。**

#### 1 先纠一个错：num_tokens 恒为 4 的倍数

开了 MTP（mtp=3）后 `decode_query_len=4`，所以 `num_tokens = 4 × 请求数`，**永远是 4 的倍数**。本场景 num_tokens 只可能取这 8 个值：

```
4, 8, 12, 16, 20, 24, 28, 32      ← 对应 1~8 个请求
```

所以 `[0,1,2,3,...,31,32]` 里有 **2/3 是永远命中不了的死值**——`0,1,2,3` 太小（最小请求也贡献 4 token），`5,6,7,9,10,11…` 不是 4 的倍数。给这些死值各捕一张图，纯浪费显存、零收益。

所以"零浪费"的理想列表不是 `[0..32]`，而是 `[4,8,12,16,20,24,28,32]`（8 张图，全 4 的倍数）。**真正要纠结的是它和 `[4,8,16,24,32]`（5 张图）怎么选。**

#### 2 8 张图（零 padding） vs 5 张图（有 padding）

| | 零浪费版 | 本场景版 |
|---|---|---|
| 桶列表 | `[4,8,12,16,20,24,28,32]` | `[4,8,16,24,32]` |
| 图数量 | **8 张** | **5 张** |
| padding 浪费 | 0（任何请求数都精确命中） | 奇数请求数才有（3→25%、5→17%、7→12.5%） |
| 常驻显存 | 多（8 套 buffer） | 少（5 套 buffer） |

差的正好是 `12、20、28` 这 **3 张图**，它们各自要**常驻一套固定地址的 buffer**（单张图 ≈ 几 MB~几十 MB，见前文）。

#### 3 核心：你省的是"算力"，付出的却是"显存"

两种"浪费"性质完全不同：

| | padding 浪费 | 图占用的显存 |
|---|---|---|
| 是什么资源 | **算力**（NPU 多算几个 pad token） | **显存**（常驻） |
| 持续时间 | 瞬时，只在那一步 | **永久**，图存在多久占多久 |
| 大小 | 很小，且只在奇数请求数时发生 | 每张图一套 buffer，大的桶尤其大 |
| 能否容忍 | 容易（batch 小、绝对量微乎其微） | 难（显存是硬约束） |

最关键的一句：**你说"空间 0 浪费"，但"给每个 size 各捕一张图"恰恰是"空间（显存）最大化"**。桶机制的本质，就是**用一点点算力浪费，去换显存的节省**——而这几章从头到尾都在抠显存（CP16 显存与 TP16 相当、冗余权重消除），显存比那点算力金贵得多。

#### 4 再算一笔账：省这 3 张图值不值

- **省下的 padding**：只在 3、5、7 个请求时发生，浪费 12.5%~25%，且都是小 batch（3 个请求 pad 到 4 个请求的量），绝对算力量微乎其微；平均到所有 step 可能连 3% 都不到。
- **多付的显存**：3 张图 × 每张一套常驻 buffer，一直占着；这部分显存本可用来放 KV cache（= 更大的可用并发）。
- **外加**：每张图还要 capture 一次，8 张图启动更慢。

**收益（省 padding）是几个百分点的瞬时算力，代价（多捕图）是永久占用的显存 + 更慢的启动。** 显存受限时，这个交换几乎总是选"少捕图、接受小 padding"。

#### 5 什么时候才值得"零浪费"

如果显存非常宽裕、且对延迟极度敏感（一个 pad token 都嫌多），配 `[4,8,12,16,20,24,28,32]` 也完全合理——`cudagraph_capture_sizes` 就是给你这个自由的。这不是对错，是"显存 ↔ 算力"的旋钮：

- 显存紧 → 桶稀（`[4,8,16,32]`，甚至更稀）
- 显存松、延迟敏感 → 桶密（`[4,8,12,...,32]`，零浪费）

**一句话总结：** `[0..32]` 里大半是死值（num_tokens 恒为 4 的倍数），捕了白捕；即便精简成零浪费的 8 张图，也要多付 3 张图的**常驻显存 + 启动开销**，去省那点**平均不到 3%、且只发生在小 batch 奇数请求数**的瞬时算力。桶机制本质是"用可忽略的算力浪费换显存节省"——显存才是稀缺的那个，所以选 5 张图而不是 8 张。

---

### 追问：四种图模式的区别与比较 —— 图"覆盖范围"的四档

**原问题：**

> 请继续解释：不同模式的区别和比较
> PIECEWISE：把整个 forward 按 attention 切成若干子图，attention 自己跑 eager，其余部分走图。
> FULL_DECODE_ONLY：只在纯 decode 阶段走整图，prefill 批次或者混合批次退回 eager。
> FULL_AND_PIECEWISE（VLLM 默认）：它本质上是一个混合策略：decode 走 FULL，prefill 走 PIECEWISE。
> FULL：整个 forward 就是一张图，attention 也在里面。

**解答：**

这四种模式讲的是图模式的另一个维度——**"图覆盖到模型的哪些部分"**。前面几节（入图/捕图/桶）都在解决"decode 整图怎么抓、batch 怎么桶化"，这一节解决的是"**attention 这个钉子户敢不敢也包进图里**"。

#### 1 先理解核心矛盾：attention 为什么难进图

模型里不是所有算子都适合进图，attention 就是那个"钉子户"：

| 算子类型 | 形状/行为 | 能不能进图 |
|---|---|---|
| GEMM、RMSNorm、量化、残差 | 形状固定、无数据相关控制流 | ✅ 完美适合 |
| Attention（尤其 DSA/稀疏注意力） | **数据相关**：prefill 变长、TopK 选索引、FlashAttention workspace 随 seq 变 | ❌ 难进图 |

attention 难进图，正好是前面几章埋过的伏笔：

- **prefill 序列长度每次不同** → 形状不定 → 录不进固定图；
- **稀疏注意力（贰/叄/陆章）里有 Lightning Indexer 的 TopK 选择**，控制流随输入变，更没法静态化；
- **FlashAttention 的 workspace 大小随 seq_len 变**。

所以"图能覆盖到哪"，本质就是"**敢不敢把 attention 也包进图**"。四种模式就是围绕这个问题的四档答案。

#### 2 四种模式逐一拆解

**① PIECEWISE（分段图）**：把 forward 按 attention 切成若干子图，attention 自己跑 eager，其余部分走图。

```
[input norm → QKV proj] ── attention(eager) ── [O proj → 残差 → MLP] ── attention(eager) ── ...
        ↑ 子图 1                                   ↑ 子图 2
```

- 每个 attention 前后夹着的线性/norm 段，各捕成一张小图；attention 本身退回 eager；
- **收益**：GEMM 密集的部分吃到了"免逐算子下发"的甜头，attention 的变长问题被绕开。

**② FULL（整图）**：整个 forward 就是一张图，attention 也在里面。

```
[ 整个 forward：所有 GEMM + 所有 attention + norm … ] = 一张图
```

- **收益最大**（launch 开销全消），但要求**整个 forward 形状完全固定**——只有纯 decode 满足（batch 由桶固定、每 token seq=1）；prefill 形状变，录不了 FULL。

**③ FULL_DECODE_ONLY（仅 decode 整图）**：只在纯 decode 阶段走整图，prefill/混合批次退回 eager。

```
decode（纯、形状固定）  →  FULL 整图
prefill / 混合批次      →  eager（不图）
```

- 简单、**省显存**（不用为 prefill 捕任何图）；代价：prefill 完全得不到图收益。

**④ FULL_AND_PIECEWISE（默认，混合）**：decode 走 FULL，prefill 走 PIECEWISE。

```
decode（纯、形状固定）  →  FULL 整图
prefill / 混合批次      →  PIECEWISE 分段图（attention 仍 eager）
```

- **两边都吃图收益**：decode 吃最猛的 FULL，prefill 退而求其次吃 PIECEWISE；
- 代价：要同时维护**两套图**（decode 的 FULL 图 + prefill 的 PIECEWISE 图），显存和复杂度更高。

#### 3 四种模式对照

| 模式 | decode 怎么跑 | prefill/混合怎么跑 | 显存 | 收益 |
|---|---|---|---|---|
| PIECEWISE | 分段图（attention eager） | 分段图 | 中 | 中（非 attention 部分免下发） |
| FULL | 整图 | 形状变、录不了，实际只用于纯 decode | 低 | decode 收益最大 |
| FULL_DECODE_ONLY | 整图 | eager | 低 | decode 最大、prefill 无 |
| **FULL_AND_PIECEWISE（默认）** | 整图 | 分段图 | **高**（两套图） | **两边都高** |

#### 4 为什么默认是 FULL_AND_PIECEWISE

真实线上是 **prefill + decode 混合**的：

- **decode 最痛、收益也最大**：单 token 计算小、launch 开销占比高（本章第 3 节），FULL 整图把 launch 压到一次——这是最该图的一边；
- **prefill 也不能全裸奔**：单算子大、launch 占比低，但 GEMM 段多，PIECEWISE 仍能省掉一部分下发开销，且绕开 attention 变长问题。

所以默认"decode 吃 FULL、prefill 吃 PIECEWISE"是收益最均衡的选择，代价是**显存要同时放两套图**。

#### 5 怎么选（结合显存与场景）

| 场景 | 建议 |
|---|---|
| 显存宽裕、prefill+decode 都想加速 | **FULL_AND_PIECEWISE**（默认） |
| 显存紧、主要在乎 decode 吞吐 | **FULL_DECODE_ONLY**（prefill 干脆 eager，省掉 prefill 图显存） |
| 形状极不稳定、连 FULL 都难维持 | **PIECEWISE** |
| 纯 decode 场景、形状完全稳定 | **FULL** |

**一句话总结：** 四种模式是"图覆盖范围"的四档：从"只图非 attention 段"（PIECEWISE），到"整图含 attention"（FULL）；而 FULL_DECODE_ONLY 和 FULL_AND_PIECEWISE 是把这两档**按 prefill/decode 拆开组合**的混合策略——decode 形状固定走 FULL、prefill 形状可变走 PIECEWISE（或干脆 eager）。默认 FULL_AND_PIECEWISE 是"显存换两边收益"的最均衡解，FULL_DECODE_ONLY 是省显存的保守解。

---

### 追问：vLLM 与 vllm-ascend 为何选不同的图模式 —— 两种硬件的"下发成本"差异

**原问题：**

> 请继续详细解释 VLLM（FULL_AND_PIECEWISE）：小批次更容易从图模式中获益，大批次则未必；decode 更容易拿到稳定收益，prefill 则更像是在收益和兼容性之间找平衡
> VLLM-ASCEND（FULL / FULL_DECODE_ONLY）：CPU 下发本身就更弱，因此 attention 这段如果还留在图外，它的 launch overhead 不可忽略。必须把 attention 这一段的下发开销也一起消掉，否则图模式的收益就会少掉一大截。根据性能收益和适配难度，做取舍。

**解答：**

这两段话其实是同一个问题的两个答案：**"attention 该不该留在图外（eager）？"**——vLLM（NVIDIA）说"可以留"，vllm-ascend（昇腾）说"必须也进图"。根源是**两种硬件的 host 下发成本不一样**。

#### 1 先回顾：为什么"小批次 / decode 更吃图模式"

这是 vLLM 那段话的前半句，本质还是 kernel launch bound 的账。

**每个 kernel 的耗时 = 固定的 launch 开销 + 实际计算。** 图模式消掉的是"固定 launch 开销"这一项，所以：

| | 小批次 | 大批次 |
|---|---|---|
| 单 kernel 计算量 | 小（矩阵×向量） | 大（大 GEMM） |
| launch 开销占比 | **高**（计算小、开销显眼） | **低**（被大计算摊薄） |
| 消掉 launch 的收益 | **大** | 小（甚至被 compute-bound 掩盖） |

**decode** 是典型小批次（单 token、launch-bound），所以吃图模式收益稳定且大；**prefill** 是大 GEMM（compute-bound）、且形状随 prompt 变，图收益本来就小，还要解决变长兼容——所以 vLLM 让 prefill 走 PIECEWISE（attention 留 eager），本质是"**收益不大、就别为它啃变长这块硬骨头**"的折中。

#### 2 核心差异：为什么昇腾必须把 attention 也进图

vllm-ascend 那段的逻辑链是：**CPU 下发更弱 → attention 留 eager 的代价不可忽略 → 必须 FULL**。

**① "CPU 下发本身就更弱"是根源。** NVIDIA 的 CUDA 有极其成熟的**异步 launch 流水线**：host 可以飞快地把 kernel 塞进命令队列、driver 高度优化，单次 launch 开销小。昇腾的 host 侧下发（CPU→NPU）相对更弱、更贵——同样的"逐算子下发"，在昇腾上每步付的固定成本比 NVIDIA 高。这直接改变两件事：图模式的**总收益**在昇腾上更高（消掉的 launch 更贵）；反过来，**任何"留图外、走 eager"的部分，在昇腾上付出的代价也更贵**。

**② attention 的 launch 开销"不可忽略"。** 关键认知：**attention 不是"一个 kernel"，而是一串小 kernel**。尤其这几章在聊的 DSA/稀疏注意力，一个 attention 块内部有：

```
Lightning Indexer 打分(GEMM) → TopK 排序 → Select/gather 稀疏 KV → Sparse Flash Attention → 投影
        ↑ 每个都是一次独立下发
```

若按 PIECEWISE 把 attention 留 eager，这一串小 kernel 每个都要走一次"贵下发"。NVIDIA 上尚可容忍（launch 便宜、且 FlashAttention 融合得足够好、kernel 少）；昇腾上 launch 贵，这串开销就从"可忽略"变成"吃掉一大截图收益"。

一句话：**图模式省的是 launch 开销，可你偏偏把 launch 开销最密集的 attention 留在外面——那图模式就白省了一大半。**

**③ 所以必须 FULL / FULL_DECODE_ONLY。** 昇腾的正确姿势是**把 attention 也包进图**（FULL），连 attention 那一串下发一起消掉。但 attention 正是前面的"钉子户"（变长、数据相关 workspace、TopK 控制流），把它包进图，代价是**适配难度陡增**：只能靠**桶化**固定 batch（decode 才行，prefill 变长基本进不了 FULL）；还要把 attention 的 workspace、稀疏索引等**静态化**，实现复杂得多。

#### 3 "根据性能收益和适配难度做取舍"

vllm-ascend 用 FULL / FULL_DECODE_ONLY 而不是纯 FULL，就是在"收益"和"难度"之间取的不同点：

| 模式 | 收益 | 适配难度 | 取舍 |
|---|---|---|---|
| **FULL** | 最大（所有下发全消，含 attention） | 最高（只能纯 decode，要桶化 + attention 静态化） | 咬下最硬的骨头换最大收益 |
| **FULL_DECODE_ONLY** | decode 最大，prefill 无 | 中（prefill 干脆 eager，绕开变长） | 收益和难度各让一步 |
| PIECEWISE（NVIDIA 默认可选） | 中 | 低 | 昇腾上**不可接受**（attention 留 eager 太贵） |

#### 4 把两边对照起来看

| | vLLM（NVIDIA） | vllm-ascend（昇腾） |
|---|---|---|
| host 下发成本 | 低（异步 launch 成熟） | **高**（下发更弱、更贵） |
| attention 留 eager 的代价 | 可容忍 | **不可忽略** |
| 默认图策略 | FULL_AND_PIECEWISE（attention 留 eager） | **FULL / FULL_DECODE_ONLY（attention 必须进图）** |
| 核心矛盾 | 收益 vs 兼容性 | **收益 vs 适配难度**（留 eager 这条路被堵死了） |

**一句话总结：** 两边对"attention 该不该进图"的答案不同，是因为**同一个"留 eager"的选择在两种硬件上成本不同**：NVIDIA 的 launch 便宜，vLLM 可以大方地把 attention 留在 eager（PIECEWISE），用"decode 走 FULL、prefill 走 PIECEWISE"在收益和兼容性之间找平衡；昇腾的下发更贵，attention 那一串小 kernel 留 eager 的开销不可忽略、会吃掉图模式一大半收益，所以 vllm-ascend 必须把 attention 也包进 FULL 图、连它的下发一起消掉——代价是适配难度更高，于是用 FULL（拼收益）和 FULL_DECODE_ONLY（拼折中）两种模式按收益与难度做取舍。

---

### 追问：npugraph_ex 与 static_kernel —— 图模式上的两层加分优化

**原问题：**

> 请继续详细解释 npugraph_ex：在 ACLGraph 捕获之前，对模型的 FX 图做优化 pass。把多个小算子合并成单个 kernel，减少 kernel 数和访存往返
> static_kernel：在图捕获阶段，把固定 shape 的算子预编译成静态二进制，运行时直接调用，跳过运行时的算子选择 / 编译开销

**解答：**

这两个是 vllm-ascend 在 ACLGraph 之上加的两层"加分优化"，都服务于前面反复提到的核心矛盾——**昇腾 host 下发贵**。定位不同：`npugraph_ex` 在**捕获之前**优化"图里的算子"（做融合），`static_kernel` 在**捕获阶段**优化"每个算子的调用"（做静态预编译）。

#### 1 两个优化各在流水线的哪个阶段

```
eager 代码 → FX 图 →【npugraph_ex：优化 pass / 算子融合】→ ACLGraph 捕获 →【static_kernel：静态预编译】→ 图对象
                       ↑ 捕获前，改"图长什么样"                        ↑ 捕获时，改"每个算子怎么调"
```

#### 2 npugraph_ex：捕获前，在 FX 图上做算子融合

**什么是 FX 图、什么是优化 pass。** FX 图是 torch.fx 产出的中间表示（IR）——把 eager 代码 trace 成一张"算子节点 + 数据依赖"的图，每个节点是一个算子调用。优化 pass 就是对这张图做**模式匹配 + 改写**：发现"一串可合并的小算子"，就替换成一个等价的融合算子。

**融合在做什么：把小算子并成大 kernel。** 典型例子：

```
融合前:  x @ W   →  [N,K]·[K,M] 写回 HBM
         + bias  →  再读回、再加                    ← 3 个 kernel，2 次 HBM 往返
融合后:  addmm(x, W, bias)                        ← 1 个 kernel，中间结果留在片上
```

再比如 `RMSNorm` 里的"平方 → 求和 → 开方 → 归一化"这一串逐元素操作，本是好几个小 kernel，可以熔成一个。

**收益：减少 kernel 数 + 减少访存往返。** 这里要澄清一个点：图 replay 已经消掉了 host launch 开销，那"减少 kernel 数"还有什么用？有用，是两个独立的收益：

1. **减少访存往返（最主要）**：不融合时每个中间结果都要**写回 HBM 再读出来**；融合后中间结果留在片上（L2/片上缓存），省的是**显存带宽**，这和 launch 无关。
2. **减少 device 侧 kernel 边界**：即使没有 host launch，device 上 kernel 之间仍有边界开销（栅栏、寄存器/共享内存初始化）。kernel 越少，边界越少，图越小、replay 越快。

**类比**：npugraph_ex ≈ `torch.compile` 里 Inductor 做的算子融合，只是 vllm-ascend 专门在 NPU 图的 FX 阶段做的版本。

#### 3 static_kernel：捕获时，固定 shape 的静态预编译

**先看昇腾算子调用的"运行时开销链"。** 在昇腾（CANN）上，跑一个算子不是"直接执行"，而是要过一条链：

```
算子选择 → tiling 计算 → 查缓存/JIT 编译 → 真正执行
```

- **算子选择**：按 dtype / format / shape 选哪个 kernel 实现；
- **tiling 计算**：按 shape 算怎么把计算切到 AI Core 网格上（tiling 是 shape 相关的）；
- **JIT 编译**：如果这个 shape 的二进制没缓存，就得现场编译——**这一步最贵**（几百 ms 甚至秒级）。

这条链里，tiling 和 JIT 都是**针对具体 shape** 的，shape 变了就要重算/重编。

**static_kernel 的巧思：shape 固定了，这些就都能提前做。** 图捕获阶段，桶已经把 shape 固定死（上一节的 `[4,8,16,24,32]`）。既然 shape 在捕获时已知、且永远不变：

- **tiling 提前算好**、**二进制提前编好**（固定 shape 的"静态二进制"）；
- **运行时直接调用预编译好的二进制**，跳过"算子选择 → tiling → JIT 编译"整条链。

```
不用 static_kernel:  每次调用 → 选择 → tiling → 查缓存/编译 → 执行
用了 static_kernel:  每次调用 → 直接执行预编译的静态二进制
```

**收益**：省掉每个算子每次调用都要付的"准备开销"，尤其 JIT 编译这种大头。对图里成百上千个算子、且 shape 完全确定的 decode 路径，能显著降低**首帧/稳态的算子调用成本**——正好补上昇腾"下发弱"的另一半短板。

#### 4 两者如何配合、和前面的关系

| 优化 | 阶段 | 解决的痛 | 类比 |
|---|---|---|---|
| **npugraph_ex** | 捕获前（FX 图） | kernel 太多、访存往返多 | Inductor 的算子融合 |
| **static_kernel** | 捕获时 | 每个算子的选择/tiling/JIT 开销 | AOT 提前编译（vs JIT） |

它们和"入图/捕图/桶"是**正交叠加**的：

- **入图**消的是"逐算子下发"（host launch）；
- **npugraph_ex** 消的是"kernel 太多 + 中间结果反复过 HBM"（图内融合）；
- **static_kernel** 消的是"每个算子调用前的选择/tiling/JIT"（per-op 准备开销）。

三件事合起来，才是昇腾上"图模式"能真正跑出收益的完整拼图——前面说的"CPU 下发更弱"，这三层正好各自补掉一块。

**一句话总结：** `npugraph_ex` 在 ACLGraph 捕获**之前**对 FX 图做优化 pass，把多个小算子熔成单个 kernel，减少 kernel 数和 HBM 访存往返（图更瘦、带宽更省）；`static_kernel` 在捕获**阶段**把固定 shape 的算子提前编译成静态二进制，运行时直接调用、跳过算子选择/tiling/JIT 这条准备链（每个算子调用更便宜）。前者让图"更少更省带宽"，后者让每个算子"更快被调起来"，是图模式在昇腾上补齐"下发弱"短板的两块拼图。

---

## 捌：FUSED_MC2 MoE通信-计算融合

**原问题：**

> 请详细解释一下下面的技术：FUSED_MC2
> **为什么需要**：少 launch、通信 - 计算更深度 overlap、中间张量不落地省带宽
> **部署配置（本场景）**：export VLLM_ASCEND_ENABLE_FUSED_MC2=1
> **工作原理**：把 MoE 一层的 "dispatch 通信 + gmm1 + swiglu + gmm2 + combine 通信" 融成单个 aclnn 算子，在 kernel 内部用「计算核跑 GMM + 向量核跑 HCCL」实现通算融合（CV 并行）

**解答：**

这是 vllm-ascend 针对 **MoE 专家并行（EP）** 的一个融合优化。它把 MoE 一层里"通信 + 计算"这条本来串行的链，融成单个算子、在算子内部流水起来。

### 1 先回顾：MoE 一层本来要做什么

结合第二章的 EP：MoE 层把大 FFN 换成「路由器 + 多个专家」，专家按卡分片。一个 token 走完一层 MoE，在 EP 下要经过三个阶段：

```
token → Router(top-k) → Dispatch(All-to-All) → [每卡算自己专家的 FFN] → Combine(All-to-All) → 输出
                                                ↑ gmm1 → swiglu → gmm2
```

- **Dispatch（All-to-All）**：把 token 按路由发往"它选中的专家所在卡"；
- **专家计算**：每卡对收到的 token 跑 FFN——`gmm1`（up 投影，分组 GEMM）→ `swiglu`（激活）→ `gmm2`（down 投影）；
- **Combine（All-to-All）**：把专家输出送回原 token 所在卡。

**朴素的实现，这三段是"串行、分多次 kernel、中间张量反复过显存"的**：

```
Dispatch 通信 [等所有 token 全部到位]
   ↓
gmm1 → 写 HBM
   ↓
swiglu → 读 HBM、激活、写 HBM
   ↓
gmm2 → 写 HBM
   ↓
Combine 通信 [等所有专家全算完]
```

### 2 为什么需要 FUSED_MC2（三个痛点）

| 痛点 | 朴素实现的问题 |
|---|---|
| **少 launch** | dispatch、gmm1、swiglu、gmm2、combine 各是独立 kernel，一次 MoE 前向多次下发。回到柒章的背景——昇腾下发贵，这堆 launch 很伤 |
| **通信-计算不 overlap** | dispatch 和 combine 是全员同步点：**等 token 全到齐才开算，等全算完才发回**。通信时计算核闲着，计算时通信核闲着，两边轮流空转 |
| **中间张量落地** | gmm1/swiglu/gmm2 的输出各写一次 HBM、再读一次，多趟显存带宽被白白吃掉 |

### 3 工作原理：融成单个 aclnn 算子 + 内部 CV 并行

核心做法一句话：**把 "dispatch 通信 + gmm1 + swiglu + gmm2 + combine 通信" 融成单个 aclnn 算子**，在算子内部用昇腾的**异构核分工**实现通算融合。

**① 什么是 aclnn 算子。** `aclnn` 是 CANN（昇腾计算架构）的高层算子接口。把整条 MoE 链做成一个 aclnn 融合算子，意味着**对外只暴露一次算子调用**——launch 从"5 次"压到"1 次"。

**② 内部怎么做到"通信-计算 overlap"（CV 并行）。** 关键在昇腾 NPU 有**两类可以同时干活的核**：

- **计算核（Cube 核 / 矩阵核）**：跑 GEMM，即 gmm1 / gmm2；
- **向量核（Vector 核）**：跑 HCCL 通信（昇腾的集合通信库，等价 NCCL）的搬运。

FUSED_MC2 把 token **切成 chunk**，在核之间做**流水线**：

```
时间轴 →
向量核:  [收 chunk₀]  [收 chunk₁]  [发回 chunk₀结果] [发回 chunk₁结果] ...
计算核:              [算 chunk₀]              [算 chunk₁]         ...
                      └── 收到就开算，不等全部到位 ──┘
```

- **向量核**一边收 chunk₁ 的 token（dispatch 通信），**计算核**一边对已经到位的 chunk₀ 跑 GMM；
- chunk 算完，向量核又一边把它 combine 发回，计算核一边接着算 chunk₁。

这就是"**CV 并行**"（Cube-Vector 并行）：通信和计算**在时间上重叠**，把通信延迟藏进计算里，而不是串行等对方。

**③ 中间张量为什么"不落地"。** 融合后，gmm1 的输出不写回 HBM、swiglu 的中间结果不写回 HBM——它们在**片上（L2/高速缓存）之间流转**，直接喂给下一段，只有最后 combine 的最终结果才落地。省掉了朴素实现里"每段一写一读"的带宽。

### 4 三个收益如何兑现

| 收益 | 兑现方式 |
|---|---|
| **少 launch** | 5 次独立 kernel → 1 次 aclnn 算子调用 |
| **通信-计算更深 overlap** | 向量核跑 HCCL 与计算核跑 GMM 按 chunk 流水，通信延迟被计算掩盖 |
| **中间张量不落地省带宽** | gmm1/swiglu/gmm2 的中间结果留片上，只最终结果落 HBM |

三者共同作用，MoE 这层（长上下文、大 batch 时 all-to-all 通信是主瓶颈）的端到端延迟明显下降。

### 5 部署配置

```bash
export VLLM_ASCEND_ENABLE_FUSED_MC2=1
```

一个开关打开融合算子。因为它是把一条很长的链整体替换成单个融合算子，属于"激进优化"，所以用环境变量显式开启——关闭时回退到朴素的分段实现，便于对拍和排查。

### 6 一句话总结

MoE + EP 的一层里，"dispatch 通信 → gmm1 → swiglu → gmm2 → combine 通信"本是一条串行、多次 launch、中间张量反复过显存的链；FUSED_MC2 把它融成单个 aclnn 算子，内部用「计算核跑 GMM + 向量核跑 HCCL」按 chunk 流水（CV 并行），把通信藏进计算、把中间结果留在片上——一次 launch、通信计算重叠、省掉中间张量的显存往返。

---

## 玖：BalanceScheduling DP负载均衡调度

**原问题：**

> 请详细解释一下 Balance Scheduling
> **为什么需要**：分布式 DP 下多个 DP rank 各自调度请求。若各 rank 运行请求数（batch size）不均衡，会出现"忙副本 TPOT 飙升、闲副本算力闲置"，拉低整体吞吐、放大尾延迟。
> **部署配置（本场景）**：--additional-config '{"enable_balance_scheduling": true, ...}'，对应代码 vllm_ascend/patch/platform/patch_balance_schedule.py
> **工作原理**：每个 engine step 结束后，把 Scheduler 换成 BalanceScheduler，all_gather 各 DP rank 的 running 请求数，调度新请求时一旦任何一个 rank 的 running 达到上限，所有 rank 一起停止接新请求——从而让各 DP rank 的并发负载均衡，避免某个 rank 被一堆请求撑爆、其他 rank 空闲。在 BalanceScheduler.schedule() 中，副本从 WAITING 队列接纳新请求前先做判断：
> `balance_flag = max(t.item() for t in self.balance_queue) >= self.max_num_running_reqs`，若 balance_flag 则 break。即只要最忙的副本运行请求数已达 max_num_running_reqs，本副本就暂停接纳新请求。
> **效果**：以"最忙副本"为锚点限制新请求入场，使各 DP 副本的 batch 规模保持在接近的区间，避免单副本打满而其他副本空转。对分布式 DP 场景，可在满足 TPOT SLO 的前提下稳定整体吞吐、抑制尾延迟。
> **收益来源**：vLLM v1 默认开 chunked prefill，prefill 和 decode 在同一次调度里同时处理；prefill 计算重、会和 decode 抢算力，拖累 decode 延迟和整体吞吐。balance 的解法：某 rank 的 decode 满载（running 达上限）时，停止接受新 prefill，让 decode step 少受 prefill 打扰 → TPOT↓、decode 吞吐↑；代价是 prefill TTFT↑。

**解答：**

这是 vllm-ascend 在**分布式 DP（数据并行）多副本**场景下做负载均衡的一个特性。核心思想一句话：**让多个 DP rank 以"最忙的那个"为锚点统一进退，避免有的 rank 被请求撑爆、有的 rank 空转**。

### 1 背景：分布式 DP 服务里，每个 rank 各调各的

大模型线上服务，模型常常是 **TP/EP 切分 + 多份 DP 副本**：同一个模型复制 N 份，每份叫一个 **DP rank（副本）**，各自服务一部分请求。关键点在于——**每个 DP rank 有自己独立的 Scheduler**，各自决定"这一 step 跑哪些请求、接纳多少新请求"。

```
请求 → 路由/负载均衡 → 分到某个 DP rank → 该 rank 的 Scheduler 独立调度
```

如果各个 rank 的调度互不通信、各管各的，就埋下了失衡的隐患。

### 2 问题：负载失衡 → 忙的 TPOT 飙、闲的算力闲置

各 rank 独立调度时，很容易出现**并发（batch size）不均衡**：

| 情况 | 后果 |
|---|---|
| **忙副本**：running 请求数多、batch 大 | 每 step 生成 token 变慢 → **TPOT 飙升**，还可能违反 SLO |
| **闲副本**：running 请求数少、batch 小 | 算得快但**算力闲置**，喂不饱 |

更糟的是，**整体吞吐被最忙的那个 rank 拖住**（木桶效应），而且"尾延迟"被放大——最慢的请求恰好都挤在忙 rank 上。这就是要上 Balance Scheduling 的原因。

### 3 BalanceScheduler 的机制：all_gather + 以"最忙 rank"为锚

**① 每 step 结束后，各 rank 交换自己的 running 数。** 每个 engine step 结束后，把默认 Scheduler 换成 **BalanceScheduler**，然后做一次 **all_gather**：每个 DP rank 把自己当前的 running 请求数广播出去，收齐所有 rank 的 running 数。这笔通信极便宜——每个 rank 只传几个整数，不是传激活/token，几乎零开销。

**② 接纳新请求前，先看"最忙 rank 满没满"。** BalanceScheduler 从 WAITING 队列接纳新请求之前，先做判断：

```python
balance_flag = max(t.item() for t in self.balance_queue) >= self.max_num_running_reqs
if balance_flag:
    break
```

逐行解读：

- `self.balance_queue`：all_gather 后，装的是**每个 DP rank 的 running 请求数**（一个 rank 一个 tensor）；
- `t.item()`：把每个 tensor 里的整数取出来；
- `max(...)`：取**最忙那个 rank** 的 running 数；
- 和 `max_num_running_reqs`（上限）比：**只要最忙的 rank 已经到/超上限**；
- `break`：**本 rank 立刻停止接纳新请求**（已 running 的继续跑，只是不再进新人）。

**③ 为什么用"max"（最忙 rank）当锚点，而不是各自的 running。** 这是整个设计的精髓：

- 如果**各看各的**（只看自己 running 满没满），那一个空闲的 rank 会继续狂接请求，而忙的 rank 已经满——**这恰恰就是失衡本身**；
- 用 **max（最忙 rank）** 当统一锚点，效果是**"最忙的满了，所有人一起停"**——空 rank 也不再进新请求，于是各 rank 的并发被"拉齐"到一个接近的区间，不会出现"一个打满、其余空转"。

```
各看各的:  [满][空]  →  空的一直接，忙的越来越忙  ✗ 越调越失衡
以 max 锚: [满][空]  →  最忙满了，空的一起停      ✓ 拉齐负载
```

### 4 收益来源：chunked prefill 下"保护 decode"

上面是"跨 rank 均衡"这层收益，还有一层更深、也更关键的收益，来自和 **chunked prefill** 的互动。

**前提**：vLLM v1 默认开 **chunked prefill**——prefill 和 decode 在**同一次调度 step 里同时处理**（prefill 切成小 chunk，和 decode token 混排在一个 batch 里）。

**问题**：prefill 是**计算重**的（大 GEMM），decode 是**访存重**的。混排后，prefill chunk 会**和 decode 抢算力**，把同一步里的 decode token 拖慢 → TPOT 上升、decode 吞吐下降。

**balance 的解法**：当某 rank 的 **decode 满载**（running 达到上限）时，就**停止接新 prefill**，让后续的 decode step **少受 prefill 打扰**：

```
不 balance:  decode 满载了还继续塞 prefill → prefill 抢占算力 → decode 变慢 → TPOT↑
balance:     decode 一满载就停接新请求(含 prefill) → decode 被保护 → TPOT↓、decode 吞吐↑
```

**代价**：新 prefill 请求在 WAITING 队列里多排队 → **prefill TTFT↑**（首 token 变慢）。

### 5 效果与代价汇总

| 指标 | 变化 | 原因 |
|---|---|---|
| **TPOT** | ↓ | decode 少受 prefill 打扰；且 batch 被拉齐，不出现单个超大 batch |
| **decode 吞吐** | ↑ | decode step 更干净、并发稳定 |
| **整体吞吐稳定性** | ↑ | 各 rank 负载均衡，消除"忙闲不均"的内耗 |
| **尾延迟** | ↓ | 不再有某个 rank 被一堆请求堆出极端慢的请求 |
| **prefill TTFT** | ↑（代价） | 新 prefill 被"限流"，排队变久 |

所以本质是**一个"解码优先"的调度策略**：用"最忙副本"当限流锚点，牺牲一点 prefill 首 token 延迟，换取 decode 的 TPOT 和整体吞吐的稳定。

### 6 一句话总结

分布式 DP 下各 rank 独立调度会导致并发失衡——忙的 TPOT 飙、闲的算力废。BalanceScheduler 的做法是：每 step 后 all_gather 各 rank 的 running 数，接纳新请求时**以最忙 rank 为锚点**（`max(...) >= max_num_running_reqs` 就 break），最忙的一满、全员一起停接；再叠加 vLLM v1 的 chunked prefill 背景，这个"停"还顺带**保护了 decode 不被 prefill 抢算力**——于是 TPOT↓、decode 吞吐↑、整体吞吐更稳，代价是 prefill TTFT↑。

---

## 拾：AsyncScheduling 异步调度与 num_output_placeholders 占位机制

**原问题：**

> 1. 在 `vllm serve` 上加 `--async-scheduling` 会带来哪些影响、有什么好处？
> 2. 详细解释 `AsyncScheduler._update_after_schedule` 里 `num_output_placeholders += num_sampled_tokens_per_step + cur_num_spec_tokens`、以及 `_update_request_with_output` 里 `num_output_placeholders -= len(new_token_ids)` 这套逻辑。
> 3. 追问：这里说的"token id 还不知道"是什么意思、意味着什么？"真实确认的 token 边界"是否就等于"已调度且知道 token id 的 decode 位置"？

**解答：**

### 1 背景：同步调度器的死结

先说明一点：`--async-scheduling` 在当前 vLLM 里**默认就是开的**（`async_scheduling` 默认 `None`，在 `vllm/config/vllm.py` 里对 generate + 支持的 executor 会自动置 `True`，除非是 pooling、非 EAGLE 的 spec decode、executor 不支持、ROCm DeepEP HT DBO 等不兼容场景）。所以显式传它多数是"确认默认值"，真正要关掉得用 `--no-async-scheduling`。

它做的事是把调度器从 `Scheduler` 换成 `AsyncScheduler`（`vllm/v1/core/sched/async_scheduler.py`，`Scheduler` 的子类）。核心思想是**让 CPU 调度跑在 GPU 执行前面一步**。

先看同步（不开 async）时 decode 的死结：decode 每一步的输入，就是上一步采样出来的那个 token。所以调度器要排下一步，**必须先拿到上一步的采样结果**：

```
schedule(step N) → GPU执行 → 采样 → 更新num_computed_tokens → schedule(step N+1) → ...
```

`schedule(N+1)` 卡在"采样结果回来"之后，CPU 调度和 GPU 执行是串行的，GPU 在"等 CPU 调度 + 等采样"的空档里空转。

async 的关键洞察是：**decode 场景下，调度器其实不需要知道下一个 token 的"值"，只需要知道"下一轮会多 1 个 token 的位置"**。所以可以让调度器"乐观地"先把位置预占下来，token 的真实 id 后面再填。

### 2 核心字段的精确语义（"token id 知道/不知道"是什么意思）

来自 `vllm/v1/request.py`：

| 字段 | 含义 |
|---|---|
| `num_computed_tokens`（记作 C） | 已调度的位置数，**乐观地包含了占位 token**（注释原话 "num_computed_tokens counts them optimistically"） |
| `num_output_placeholders`（记作 O） | 其中"内容待定"的 decode 位置数——位置已占、但 token id 还没采样出来 |
| `num_tokens_with_spec` | `len(all_token_ids) + len(spec_token_ids)`（真实 prompt+output + draft） |
| `num_in_flight_tokens` | 已下发、结果还没回来的 token 数 |

**"token id 知道/不知道"的精确含义：**

"token id"指词表里的具体整数（比如 `12345` 代表 "the"）。调度器每排一个位置，其实要回答两个问题：**排哪个位置**（C 关心的事），以及**这个位置放哪个 token id**（内容层面的事）。

- **prefill 位置**：token id 天生确定 —— 来自用户输入的 prompt，拿到请求时就已知。所以 prefill 不需要猜（这也是 async 只对 decode 预占、对 prefill 直接 `continue` 跳过的原因）。
- **decode 位置**：token id 要由 GPU 跑完上一步、对 logits 采样之后才产生。异步调度器要抢在采样之前把下一步排掉，所以这个位置"位置定了、内容待定"——这就是占位（placeholder）。

"内容待定"意味着三件事：

1. **这个位置的 KV 不能缓存**——KV 存的是"某个具体 token 内容的键值"，内容不知道就没法写。所以 `cache_blocks(request, C - O)` 只缓存到"内容已确定"的边界。
2. **它还不能被当成下一步的输入**——decode 下一步的输入 = 上一步的输出，占位 token 的 id 还不知道，要由 worker 稍后用 `prev_sampled_token_ids` 走 GPU 直接广播补齐。
3. **落地前若被抢占，这个占位输出作废**——抢占时 `_preempt_request` 把 O 置 0，并把已下发的那批标记为 `num_stale_output_tokens`，让它们只交付、不动重置过的计数器。

### 3 _update_after_schedule 逐行拆解

它在 `schedule()` 的**最后**被调用。关键：`super()._update_after_schedule()` 先跑，已经执行了 `C += num_scheduled_token` 并更新了 `is_prefill_chunk`。

```python
def _update_after_schedule(self, scheduler_output):
    super()._update_after_schedule(scheduler_output)   # 先推进 C，算出 is_prefill_chunk
    spec_decode_tokens = scheduler_output.scheduled_spec_decode_tokens
    self._spec_token_placeholders = [-1] * scheduler_output.num_spec_tokens_to_schedule
    for req_id in scheduler_output.num_scheduled_tokens:
        request = self.requests[req_id]
        if request.is_prefill_chunk:
            continue                                    # ① 只有纯 decode 才预占
        cur_num_spec_tokens = len(spec_decode_tokens.get(req_id, ()))  # ② 本步实际 draft 数
        request.num_output_placeholders += (            # ③ 预占：O += 1 + draft数
            self.num_sampled_tokens_per_step + cur_num_spec_tokens
        )
        request.spec_token_ids = self._spec_token_placeholders  # ④ draft 放 [-1,...] 占位
        if self.use_v2_model_runner:
            request.next_decode_eligible_step = self.current_step + self.pp_size  # ⑤ PP 节奏
```

- **① `if request.is_prefill_chunk: continue`**：只有进入纯 decode 阶段的请求才预占。prefill 的输入来自 prompt 是确定的，只有 decode 输出 token 是"下一轮才有"的。
- **② `cur_num_spec_tokens`**：本步给这个请求实际排了多少个 draft/spec token。
- **③ `O += num_sampled_tokens_per_step + cur_num_spec_tokens`**：核心动作。`num_sampled_tokens_per_step` 对普通自回归是 1（diffusion 是 0）。意思是"赌本步执行完，这个请求会多出 1 个真实 token + N 个 draft token，把这几个位置先占上"。
- **④ `spec_token_ids = [-1, ...]`**：draft token 的真实 id 由 worker 侧 drafter 生成，调度器不知道，只填占位符 `-1`，真实 id 由 worker 后续更新。
- **⑤ `next_decode_eligible_step`**：V2 + PP + async 下，同一请求两次 decode 之间强制隔 `pp_size` 步。

### 4 调度公式怎么消费 O + _update_request_with_output 拆解

O 真正起作用在 `schedule()` 的 RUNNING 循环（`scheduler.py`）：

```python
num_new_tokens = (
    request.num_tokens_with_spec
    + request.num_output_placeholders   # ← 加上 O
    - request.num_computed_tokens
)
```

`+O` 这一项就是"假装下一个 token 已经存在"。

`_update_request_with_output` 在 `update_from_output`（采样结果回来）里被调用：

```python
def _update_request_with_output(self, request, new_token_ids, is_stale=False):
    status_before_update = request.status
    new_token_ids, stopped = super()._update_request_with_output(request, new_token_ids)  # ① append 真实 token + 检查 stop

    if not is_stale:                              # ② stale 时不冲销
        request.num_output_placeholders -= len(new_token_ids)  # ③ 用真实 token 冲销占位
        assert request.num_output_placeholders >= 0

    if status_before_update == RequestStatus.RUNNING:
        self.kv_cache_manager.cache_blocks(        # ④ 只缓存真实 token 边界
            request, request.num_computed_tokens - request.num_output_placeholders
        )
    return new_token_ids, stopped
```

- **①** 基类方法把采样得到的真实 token `append_output_token_ids` 进去，并检查 stop。
- **②③ `if not is_stale: O -= len(new_token_ids)`**：真实 token 落地，把预占的位置冲销掉。`is_stale` 时不冲销，因为抢占时已把 O 置 0，再来冲销会下溢（`assert O >= 0` 会炸）。
- **④ `cache_blocks(C - O)`**：把新 token 写进 KV cache，但位置取 `C - O`（真实边界），占位部分内容未知不能缓存。

一句话：**`_update_after_schedule` 负责"发债"（预占下一步的 1 个真实 + N 个 draft 槽位），`_update_request_with_output` 负责"还债"（真实 token 落地后冲销占位、并只缓存到 C-O 的真实边界）。** 这一借一还之间，调度器就不用再傻等采样结果了。

### 5 真实确认边界 C - O 的澄清

> 追问："真实确认的 token 边界"是否就等于"已调度且知道 token id 的 decode 位置"？

**方向正确，精确化一下**：`C - O` = 已调度、且 token id 已确定（内容已知）的位置边界。其中"内容已知"包含**两类**，不只是 decode：

1. **所有 prefill/prompt token**——id 天生已知（来自输入）；
2. **已经采样落地、确认了的 decode token**——id 已被采样出来。

而 O 就是被 C 算进去、但**内容还不知道**的那部分 decode 占位位置。所以更准确地说："边界 = 所有已调度位置中内容已知的那部分"。

**一个诚实的补充（数值上差一个"领先 1 步"的偏移）**：调度器永远"领先真实进度一步"——C 里恒包含"下一个要生成、但还没生成"的那个 token 位置。所以稳态下 `C - O = 已确认token数 - 1`（即最后一个已确认 token 的 0-based 索引），而不是正好等于已确认 token 数。这个 ±1 只是账本偏移，不影响"`C - O` = 内容已确定边界"这个核心语义（`cache_blocks(C-O)`、`_free_encoder_inputs` 都是把它当"确定内容边界"用的）。

### 6 一个完整数字走查（无 spec decode，纯 decode 阶段）

设 prompt P=5，请求已生成 R=3 个真实输出 token，稳态下 run-ahead 深度为 1，所以 `O=1`，`C = num_tokens_with_spec + O - 1 = 8 + 1 - 1 = 8`。

| 事件 | C | O | num_tokens | 说明 |
|---|---|---|---|---|
| S0（稳态） | 8 | 1 | 8 | 已确认 8 个真实 token |
| schedule() 本步 decode | →9 | →2 | 8 | `num_new_tokens = 8+1-8 = 1`，排 1 个 token，再预占 1 个 |
| execute+采样 → update | 9 | →1 | →9 | append 1 个 token，`O-=1`，`cache_blocks(9-1=8)` |

下一轮 schedule 又回到 `num_new_tokens = 9 + 1 - 9 = 1`。观察两点：**O 在 1↔2 之间震荡（有界）**；**`C - O` 始终等于真实 token 数（8→9）**。O 的实际上限由 run-ahead 深度（engine core 的 `max_concurrent_batches`）决定。

### 7 边界情况

- **抢占**：`_preempt_request` 里 `num_output_placeholders = 0`，同时 `num_stale_output_tokens = num_in_flight_tokens`，把已下发的占位输出标记为 stale。
- **spec 拒绝回滚**：draft token 被拒绝时，`C -= num_rejected` 且 `O -= num_rejected`（`num_output_placeholders` 里也含 draft）。
- **提前终止判断**：RUNNING 循环开头那个 `num_output_placeholders > 0` 的检查，用 `C + 2 - O` 判断"确认 token 是否已到 max_tokens"，到了就不再调度，避免 async 下多排一步越过 max_tokens。
- **与 BalanceScheduling 的互斥**：balance 调度的 patch 只替换了 `vllm.v1.core.sched.scheduler.Scheduler` 这个模块级名字，而 `get_scheduler_cls()` 在 `async_scheduling=True` 时是直接 `from ...async_scheduler import AsyncScheduler` 返回 `AsyncScheduler`，不经过被 patch 的那个名字。所以**开了 `--async-scheduling` 时 `BalanceScheduler` 根本不会被实例化**，balance 调度和 async 调度实际上不能同时生效（balance 会静默失效，不报错）。

### 8 一句话总结

异步调度的本质是**用 `num_output_placeholders` 做"先占位置、后补内容"的借还账**：`_update_after_schedule` 抢在采样之前把"下一步会多出的 1 个真实 + N 个 draft 槽位"预占下来（发债），`_update_request_with_output` 在真实 token 落地后冲销占位、并只缓存到 `C - O`（内容已确定的边界，还债）——于是 CPU 调度得以和 GPU 执行重叠，填上 decode 等待采样的空档，换来吞吐/时延提升；代价是抢占、spec 拒绝、max_tokens 等边界情形都要围绕这套占位账本做额外的正确性处理。